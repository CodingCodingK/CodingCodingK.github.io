<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>CodingCodingK Blog</title>
  
  <subtitle>CodingCodingK</subtitle>
  <link href="https://codingcodingk.github.io/atom.xml" rel="self"/>
  
  <link href="https://codingcodingk.github.io/"/>
  <updated>2026-04-05T11:18:51.867Z</updated>
  <id>https://codingcodingk.github.io/</id>
  
  <author>
    <name>CodingCodingK</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Hello Agents - 记忆与检索&amp;RAG</title>
    <link href="https://codingcodingk.github.io/Tech/AI/claude_code/claude_memo/"/>
    <id>https://codingcodingk.github.io/Tech/AI/claude_code/claude_memo/</id>
    <published>2026-04-05T11:17:49.352Z</published>
    <updated>2026-04-05T11:18:51.867Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Claude-Code-源码分析"><a href="#Claude-Code-源码分析" class="headerlink" title="Claude Code 源码分析"></a>Claude Code 源码分析</h1><p>对照 nanobot 逆向理解 Claude Code Agent 架构。</p><hr><h2 id="Ch-00-—-全局流程图"><a href="#Ch-00-—-全局流程图" class="headerlink" title="Ch 00 — 全局流程图"></a>Ch 00 — 全局流程图</h2><p><strong>① [<a href="#ch-04--%E5%90%AF%E5%8A%A8%E6%B5%81%E7%A8%8B">Ch 04</a>] main.tsx 启动</strong><br>   ├─ 并行预取：MDM 企业策略 / API Key / Feature Flags（~40ms，串行需 135ms+）<br>   └─ Commander.js 解析参数，按子命令分发 → launchRepl() 进入主 REPL</p><p><strong>② [<a href="#ch-10--skill-%E7%B3%BB%E7%BB%9F">Ch 10</a>] 用户输入</strong><br>   ├─ 斜杠命令（/commit、/review 等）→ Skill 执行<br>   └─ 普通输入 → 进入 ③</p><p><strong>③ [<a href="#ch-05--agent-loop-%E4%B8%BB%E6%B5%81%E7%A8%8B">Ch 05</a>] QueryEngine.submitMessage()</strong><br>   初始化会话状态、封装 canUseTool 记录拒绝情况 → 进入 queryLoop()</p><p><strong>↻ [<a href="#ch-05--Agent-loop">Ch 05</a>] Agent Loop：queryLoop() 主循环 while(true)，每轮调一次 LLM，有工具就执行再循环</strong></p><p><strong>④ [<a href="#ch-11--compact-%E5%AF%B9%E8%AF%9D%E5%8E%8B%E7%BC%A9">Ch 11</a>] Compact 预处理（每轮必跑）</strong><br>   ├─ Microcompaction → 每轮必跑；移除旧轮次图片，缩小 tool_result 体积<br>   ├─ Autocompact → context 接近上限时触发<br>   │      ├─ 先试 Session Memory Compaction → 直接存 Session Memory，最快，不调 LLM<br>   │      └─ 失败则 Traditional Compaction → fork subAgent 做全量摘要<br>   └─ blockingLimit 超限（Autocompact 之后判断）→ 退出 loop</p><p><strong>⑤ [<a href="#ch-05--llm-%E6%B5%81%E5%BC%8F%E8%B0%83%E7%94%A8%E7%BB%86%E8%8A%82">Ch 05</a>] 调 LLM API，SSE 流式接收响应</strong><br>   ├─ text block → 逐 token yield 给 UI 实时显示<br>   ├─ <strong>[<a href="#ch-08--thinking-%E6%8E%A8%E7%90%86%E6%A8%A1%E5%BC%8F">Ch 08</a>] thinking block</strong> → ThinkingConfig 控制 budget，ultrathink 触发最大 budget<br>   └─ tool_use block → input_json_delta 边收边拼，content_block_stop 后 block 完整<br>          ├─ 默认路径 → 推入 toolUseBlocks[]，等流结束后统一进入 ⑦<br>          └─ <strong>[<a href="#ch-07--%E6%B5%81%E5%BC%8F%E6%89%A7%E8%A1%8C%E5%BC%95%E6%93%8E">Ch 07</a>] StreamingToolExecutor</strong> 开启 → 立即 addTool()，不等流结束直接执行（见 ⑨）</p><p><strong>⑥ LLM流接收结束判断（依据 toolUseBlocks.length，不依赖 stop_reason）</strong><br>   ├─ toolUseBlocks 为空 → executePostSamplingHooks（fire-and-forget）→ [<a href="#ch-14--hooks-%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F%E6%89%A9%E5%B1%95">Ch 14</a>] Stop / SubagentStop Hooks → return ‘completed’  ✓ 结束<br>   ├─ 回复被截断（走同一恢复路径）<br>   │      ├─ max_tokens：本条回复超出单次输出上限（默认 32k）<br>   │      └─ model_context_window_exceeded：④ blockingLimit / Reactive Compaction 未拦住，输出中途撞上下文总限<br>   │      恢复：[默认关此功能] 同轮升至 64k 重试 → 仍超或 flag 关 → 追加隐藏提示让 LLM 续写（最多 3 次）<br>   │      prompt_too_long：先试 context_collapse drain → 再试 reactive compact → 均失败则 return ‘prompt_too_long’<br>   └─ toolUseBlocks 非空 → executePostSamplingHooks（fire-and-forget）→ 进入 ⑦</p><p><strong>⑦ [<a href="#ch-14--hooks-%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F%E6%89%A9%E5%B1%95">Ch 14</a>] PreToolUse Hooks 触发，可修改参数或阻断本次执行</strong></p><p><strong>⑧ [<a href="#ch-06--%E5%B7%A5%E5%85%B7%E8%B0%83%E7%94%A8%E7%B3%BB%E7%BB%9F">Ch 06</a>] 权限判断</strong><br>   ├─ deny 规则命中 → 直接拒绝  ✗<br>   ├─ ask 规则命中 → 弹窗（沙箱自动允许例外）<br>   ├─ tool.checkPermissions() → allow（只读工具在此返回）/ ask / deny<br>   ├─ bypassPermissions 模式 → 直通<br>   ├─ alwaysAllowed 规则命中 → 直通<br>   └─ 其余 → 弹窗等用户确认；denied → 返回 error tool_result，跳过执行  ✗</p><p><strong>⑨ 工具调度 + 执行（feature flag 决定走哪条路）</strong><br>   ├─ 默认 <strong>[<a href="#ch-06--%E5%B7%A5%E5%85%B7%E8%B0%83%E7%94%A8%E7%B3%BB%E7%BB%9F">Ch 06</a>] toolOrchestration</strong>：连续 safe → 并发批（上限 10），unsafe → 串行<br>   ├─ 实验性 <strong>[<a href="#ch-07--%E6%B5%81%E5%BC%8F%E6%89%A7%E8%A1%8C%E5%BC%95%E6%93%8E">Ch 07</a>] StreamingToolExecutor</strong>（默认关闭）<br>   │         LLM 流式中即启动，动态 safe/unsafe，无并发上限<br>   │         出错 → 级联取消同批其他工具<br>   └─ 每个工具经 runToolUse() → tool.call() 执行，返回 tool_result</p><p><strong>⑩ AgentTool 特殊路径</strong><br>   ├─ tool.call() → runAgent() → query() → 子 queryLoop，独立 context 和工具池<br>   └─ <strong>[<a href="#ch-09--task-%E7%B3%BB%E7%BB%9F">Ch 09</a>] Task 系统</strong>：多 Agent 协调基础设施<br>            JSON 文件 + 文件锁存储任务状态，父 Agent / subAgent 通过 TaskCreate/TaskUpdate 工具共享进度</p><p><strong>⑪ [<a href="#ch-14--hooks-%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F%E6%89%A9%E5%B1%95">Ch 14</a>] PostToolUse / PostToolUseFailure Hooks 触发</strong><br>   ├─ PostToolUse（执行成功）→ 可替换 MCP 输出或追加额外消息<br>   └─ PostToolUseFailure（执行失败）→ 可处理错误结果</p><p><strong>⑫ [<a href="#ch-06--%E5%B7%A5%E5%85%B7%E8%B0%83%E7%94%A8%E7%B3%BB%E7%BB%9F">Ch 06</a>] 结果大小检查</strong><br>   ├─ 单条 &gt; 工具自设上限（最高 50k chars）→ 写磁盘，模型收到路径 + 2000 字节预览<br>   │      └─ 例外：Read 工具不设上限，永不落盘<br>   └─ 本轮总量 &gt; 200k chars → 持久化最大几条，降至阈值以下</p><p><strong>⑬ toolResults[] 拼入 messages，作为下轮 LLM 的输入历史</strong><br>   ├─ maxTurns 超限 → 退出 loop<br>   └─ continue ──→ 回到 ④</p><p>退出条件：<br>   ├─ completed              正常结束（无 tool_use）<br>   ├─ aborted_tools          工具执行中用户中断<br>   ├─ aborted_streaming      流式响应中用户中断<br>   ├─ max_turns              超过最大轮次<br>   ├─ blockingLimit          token 超限（Autocompact 后仍超）<br>   ├─ prompt_too_long        context 过长且压缩恢复失败<br>   ├─ image_error            图片大小/resize 错误<br>   ├─ hook_stopped           PostToolUse Hook 阻断（continuation 标志）<br>   ├─ stop_hook_prevented    Stop Hook 阻断<br>   └─ model_error            API 报错</p><p>Reactive Compaction（非预处理步骤）：<br>   └─ LLM 返回 prompt_too_long → 拦截响应 → 触发压缩 → 重试本轮 API 调用</p><hr><h2 id="Ch-01-—-项目概览"><a href="#Ch-01-—-项目概览" class="headerlink" title="Ch 01 — 项目概览"></a>Ch 01 — 项目概览</h2><table><thead><tr><th></th><th>nanobot</th><th>Claude Code</th></tr></thead><tbody><tr><td>语言</td><td>Python</td><td>TypeScript (Bun)</td></tr><tr><td>规模</td><td>小型</td><td>~50万行，生产级</td></tr><tr><td>工具数</td><td>~10</td><td>40+ 内置 + 动态 MCP</td></tr><tr><td>入口</td><td><code>loop.py</code></td><td><code>src/query.ts</code>（1732行）</td></tr><tr><td>UI</td><td>无</td><td>React+Ink 终端（前端，跳过）</td></tr></tbody></table><p><strong>定位</strong>：terminal-native agentic system，核心是 Agent Loop 直接操作本地环境。</p><h3 id="Agent-核心文件"><a href="#Agent-核心文件" class="headerlink" title="Agent 核心文件"></a>Agent 核心文件</h3><table><thead><tr><th>文件</th><th>职责</th></tr></thead><tbody><tr><td><code>src/query.ts</code></td><td>Agent Loop 核心</td></tr><tr><td><code>src/QueryEngine.ts</code></td><td>会话容器，包裹 query()</td></tr><tr><td><code>src/Tool.ts</code></td><td>Tool 类型定义</td></tr><tr><td><code>src/tools.ts</code></td><td>工具注册 &amp; 池化</td></tr><tr><td><code>src/services/tools/toolExecution.ts</code></td><td>权限检查 + hooks + 执行</td></tr><tr><td><code>src/services/tools/toolOrchestration.ts</code></td><td>并发/串行调度</td></tr><tr><td><code>src/utils/tasks.ts</code></td><td>Task 系统</td></tr><tr><td><code>src/services/compact/</code></td><td>对话压缩</td></tr><tr><td><code>src/utils/hooks.ts</code></td><td>Hooks 系统</td></tr><tr><td><code>src/utils/thinking.ts</code></td><td>Thinking 模式</td></tr></tbody></table><p>UI 层只消费 query.ts 的流式事件，核心逻辑全在 query.ts。</p><h3 id="关键文件速查"><a href="#关键文件速查" class="headerlink" title="关键文件速查"></a>关键文件速查</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">Agent Loop       → src/query.ts + src/QueryEngine.ts</span><br><span class="line">工具系统         → src/Tool.ts + src/tools.ts + src/tools/</span><br><span class="line">权限系统         → src/utils/permissions/</span><br><span class="line">沙盒             → src/utils/sandbox/sandbox-adapter.ts</span><br><span class="line">Hooks            → src/utils/hooks/ + src/types/hooks.ts</span><br><span class="line">Compact          → src/services/compact/</span><br><span class="line">记忆系统         → src/memdir/ + src/services/extractMemories/</span><br><span class="line">上下文构建       → src/context.ts</span><br><span class="line">Skill 系统       → src/skills/</span><br><span class="line">Task 系统        → src/tools/AgentTool/ + src/utils/task/</span><br><span class="line">多 Agent 实验    → src/coordinator/ + src/utils/swarm/</span><br></pre></td></tr></table></figure><hr><h2 id="Ch-02-—-技术栈"><a href="#Ch-02-—-技术栈" class="headerlink" title="Ch 02 — 技术栈"></a>Ch 02 — 技术栈</h2><p><strong>Bun</strong>：比 Node.js 启动快 ~3x，内置打包器，一套工具链搞定运行/打包/测试。代价是 Windows 支持弱、生态兼容性差一些。</p><p><strong>Feature Flag（bun:bundle）</strong>：打包时注入 flag 值，false 的分支物理删除。目的是同一份代码打出不同版本（内部/灰度/正式），不用维护多个分支。反编译版里 <code>feature()</code> 永远返回 false，实验功能全关。</p><p><strong>前端</strong>：React + Ink，终端里跑 React 组件。REPL.tsx 调用 QueryEngine，消费流式事件刷新界面。Agent 逻辑与 UI 完全解耦。</p><hr><h2 id="Ch-03-—-目录结构"><a href="#Ch-03-—-目录结构" class="headerlink" title="Ch 03 — 目录结构"></a>Ch 03 — 目录结构</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">src/</span><br><span class="line">├── query.ts / QueryEngine.ts / Tool.ts / tools.ts  ← Agent 核心</span><br><span class="line">├── tools/        ← 40+ 工具，每个独立目录（定义+执行自包含）</span><br><span class="line">├── services/     ← 外部交互（API、MCP、Compact）</span><br><span class="line">├── utils/        ← hooks、tasks、thinking 等基础设施</span><br><span class="line">├── skills/       ← Skill 系统</span><br><span class="line">├── coordinator/  ← 多 Agent 协调（实验性）</span><br><span class="line">└── screens/ components/ hooks/  ← 前端，跳过</span><br></pre></td></tr></table></figure><p><strong>tools/ 结构</strong>：每个工具一个独立目录，<code>tools.ts</code> 统一注册。工具列表排序固定，保证 prompt cache 命中率稳定。</p><p><code>src/tools.ts</code> 三个关键函数：</p><ul><li><code>getAllBaseTools()</code> — 返回所有内置工具</li><li><code>getTools(context)</code> — 按权限/feature flag 过滤</li><li><code>assembleToolPool(context, mcpTools)</code> — 内置 + MCP 合并，去重排序</li></ul><hr><h2 id="Ch-04-—-启动流程"><a href="#Ch-04-—-启动流程" class="headerlink" title="Ch 04 — 启动流程"></a>Ch 04 — 启动流程</h2><p><strong>并行预取</strong>：main.tsx 顶部利用模块加载副作用，在其他 import 还在执行时就并行触发：MDM 企业策略、Keychain（API Key / OAuth）、GrowthBook feature flags。串行需 135ms+，并行只要 ~40ms。</p><p><strong>命令分发</strong>：Commander.js 解析参数后，所有路径最终都汇入 <code>launchRepl()</code>，差别只是传入的初始消息和配置不同。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">main.tsx</span><br><span class="line">  ├─ 子命令（/mcp、/doctor、/auth…）→ 各自处理</span><br><span class="line">  └─ 主命令 → 按 flag 路由 → launchRepl()</span><br><span class="line">       ├─ --resume    恢复历史会话</span><br><span class="line">       ├─ --bridge    IDE 桥接模式</span><br><span class="line">       └─ 默认        普通交互 REPL</span><br></pre></td></tr></table></figure><p><strong>CoordinatorMode</strong>：<code>src/coordinator/</code> 是多 Agent 团队协作模式，在启动时通过 feature flag 加载。默认<strong>双重关闭</strong>：① 构建时 <code>feature(&#39;COORDINATOR_MODE&#39;) = false</code>，代码不进二进制；② 即使代码存在，还需环境变量 <code>CLAUDE_CODE_COORDINATOR_MODE=true</code> 才能激活。对外版本根本进不了第一层。</p><p><strong>多 Agent 两种方式对比</strong>：</p><table><thead><tr><th></th><th>AgentTool</th><th>CoordinatorMode</th></tr></thead><tbody><tr><td>默认状态</td><td>开启</td><td>关闭（实验性，内部专用）</td></tr><tr><td>触发</td><td>LLM 自己决定调用</td><td>专职 Coordinator 主导</td></tr><tr><td>定位</td><td>普通多 Agent</td><td>结构化团队协作，AgentTool 进阶版</td></tr></tbody></table><p>日常多 Agent 走 AgentTool，CoordinatorMode 是内部实验功能，对外不可用。</p><hr><h2 id="Ch-05-—-LLM-流式调用细节"><a href="#Ch-05-—-LLM-流式调用细节" class="headerlink" title="Ch 05 — LLM 流式调用细节"></a>Ch 05 — LLM 流式调用细节</h2><h3 id="Agent：QueryEngine-queryLoop-State"><a href="#Agent：QueryEngine-queryLoop-State" class="headerlink" title="Agent：QueryEngine | queryLoop | State"></a>Agent：QueryEngine | queryLoop | State</h3><p><strong>没有 Agent 类</strong>，但是概念存在<code>queryLoop()</code>。运行时只有三层：</p><table><thead><tr><th>层</th><th>实体</th><th>跨越范围</th></tr></thead><tbody><tr><td>Session 层</td><td><code>QueryEngine</code>（类）</td><td>跨多次用户输入，持有 messages / 工具列表 / 权限</td></tr><tr><td>Loop 层</td><td><code>query()</code> → <code>queryLoop()</code>（函数）</td><td>单次用户输入，多轮 LLM 调用直到无 tool_use</td></tr><tr><td>迭代层</td><td><code>State</code>（数据包）</td><td>单轮 LLM 调用结束到下一轮开始之间</td></tr></tbody></table><p><code>State</code>（query.ts:204）是 ReAct 每轮迭代间传递的数据包，不是枚举状态机——字段可同时激活：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">工作记忆（每轮必传）</span><br><span class="line">  ├─ messages          完整对话历史，compact 后缩小，工具结果每轮追加</span><br><span class="line">  ├─ toolUseContext    工具池、权限回调、agent ID</span><br><span class="line">  └─ turnCount         当前第几轮，超 maxTurns → 退出 loop</span><br><span class="line"></span><br><span class="line">异常书签（出问题时才激活，正常流程忽略）</span><br><span class="line">  ├─ maxOutputTokensOverride      LLM 输出截断 → 本轮升至 64k 重试，下轮重置 undefined</span><br><span class="line">  ├─ maxOutputTokensRecoveryCount 升 token 无效 → 注入&quot;请续写&quot;消息重跑，累计到 3 次放弃</span><br><span class="line">  ├─ hasAttemptedReactiveCompact  context 超限 → 触发一次压缩重试，之后置 true 防死循环</span><br><span class="line">  ├─ stopHookActive               Stop Hook 有 blocking error → 携带错误重跑 LLM</span><br><span class="line">  ├─ autoCompactTracking          主动压缩跟踪数据（已压缩次数、连续失败计数），compact 后重置</span><br><span class="line">  └─ pendingToolUseSummary        工具摘要 Promise，下轮开始时 await 并注入 messages</span><br><span class="line"></span><br><span class="line">transition（调试用）</span><br><span class="line">  └─ 记录&quot;为什么进入这一轮&quot;（next_turn / reactive_compact_retry 等），供测试断言用</span><br></pre></td></tr></table></figure><p><strong>异常书签触发场景：</strong></p><ul><li><strong>maxOutputTokensOverride</strong>：stop_reason = max_tokens（LLM 输出被截断）→ 临时升至 64k 重试当前轮，下轮重置。</li><li><strong>maxOutputTokensRecoveryCount</strong>：升 64k 仍不够 → 注入隐藏消息 <code>&quot;Output token limit hit. Resume directly...&quot;</code> 让 LLM 续写，最多 3 次，超过放弃。</li><li><strong>hasAttemptedReactiveCompact</strong>：API 返 prompt_too_long → 压缩历史重试一次，置 true 后再超限直接以 <code>&#123; reason: &#39;prompt_too_long&#39; &#125;</code> 退出 loop（用户看到提示，程序不卡死）。退出时不跑 Stop Hook，防死循环。</li><li><strong>stopHookActive</strong>：Stop Hook 返回 blocking error（如检测到危险命令）→ 把错误注入 messages 让 LLM 重新回复；标志在重试期间保留，防 compact 清掉。</li><li><strong>autoCompactTracking</strong>：主动压缩连续失败 3 次触发熔断——曾有 session 失败 3272 次、每天浪费 25 万次 API 调用。</li></ul><h3 id="Agent-Loop"><a href="#Agent-Loop" class="headerlink" title="Agent Loop"></a>Agent Loop</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">Agent Loop (query.ts)</span><br><span class="line"></span><br><span class="line">接收到 AssistantMessage</span><br><span class="line">  ├─ 推入 assistantMessages[]</span><br><span class="line">  ├─ block.type === &#x27;tool_use&#x27; → 推入 toolUseBlocks[]</span><br><span class="line">  └─ 流结束</span><br><span class="line">       ├─ toolUseBlocks 为空 → loop 结束</span><br><span class="line">       └─ 非空 → 进入工具执行</span><br><span class="line">       </span><br><span class="line">       </span><br><span class="line">  ├─ 放入 assistantMessages[]   // 本轮 assistant 输出暂存</span><br><span class="line">  ├─ 若 block.type === &#x27;tool_use&#x27;</span><br><span class="line">  │    └─ 推入 toolUseBlocks[]  // 进入 Ch 06 的“完整工具执行路径”</span><br><span class="line">  └─ 继续向外层转发</span><br><span class="line">       ├─ REPL / UI         → 追加到消息列表</span><br><span class="line">       └─ SDK / QueryEngine → 输出给调用方 + 写 transcript</span><br><span class="line">  ↓</span><br><span class="line">本轮结束</span><br><span class="line">  ├─ toolResults[] 追加进 messages</span><br><span class="line">  └─ 调API传messages = [...messagesForQuery, ...assistantMessages, ...toolResults]</span><br></pre></td></tr></table></figure><h3 id="Agent-Message：AssistantMessage"><a href="#Agent-Message：AssistantMessage" class="headerlink" title="Agent Message：AssistantMessage"></a>Agent Message：AssistantMessage</h3><p>AssistantMessage 是 LLM =&gt;  Agent 这一流向的信息层最终包装（当然还有其他流向比如tool和user都是属于UserMessage ）。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">claude.ts 拼好的 block ，最终大概是这样：</span><br><span class="line">  第 1 个 block 收齐 → yield 1 条 AssistantMessage（thinking）</span><br><span class="line">  第 2 个 block 收齐 → yield 1 条 AssistantMessage（text）</span><br><span class="line">  第 3 个 block 收齐 → yield 1 条 AssistantMessage（tool_use）</span><br><span class="line">当封装成 1 条 AssistantMessage 之后，就 yield 给 query.ts</span><br></pre></td></tr></table></figure><p>所以这里的 <code>AssistantMessage</code> 不是“整轮回复最终版”，而是“<strong>某一个 block 收齐后的出队结果</strong>”。<br>如果一轮里依次生成了 <code>thinking → text → tool_use</code>，外层就会依次收到 3 条 <code>AssistantMessage</code>，每条里的 <code>message.content</code> 都只放当前这个 block。</p><p>外层字段 <code>type / uuid / timestamp / requestId</code> 是 Claude Code 自己补的运行时信息；<br>里面的 <code>message</code> 基本还是 Anthropic 原生 message 结构，只是 <code>content</code> 此时只装当前这个刚收齐的 block。</p><p>AssistantMessage样例：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">type</span>: <span class="string">&#x27;assistant&#x27;</span>,</span><br><span class="line">  <span class="attr">uuid</span>: <span class="string">&#x27;msg-uuid-1&#x27;</span>,</span><br><span class="line">  <span class="attr">timestamp</span>: <span class="string">&#x27;2025-...&#x27;</span>,</span><br><span class="line">  <span class="attr">requestId</span>: <span class="string">&#x27;req_xxx&#x27;</span>,</span><br><span class="line">  <span class="attr">message</span>: &#123;</span><br><span class="line">    <span class="attr">id</span>: <span class="string">&#x27;api-msg-id&#x27;</span>,</span><br><span class="line">    <span class="attr">role</span>: <span class="string">&#x27;assistant&#x27;</span>,</span><br><span class="line">    <span class="attr">model</span>: <span class="string">&#x27;claude-opus-4-6-...&#x27;</span>,</span><br><span class="line">    <span class="attr">stop_reason</span>: <span class="literal">null</span>,</span><br><span class="line">    <span class="attr">usage</span>: &#123;...&#125;,</span><br><span class="line">    <span class="attr">content</span>: [</span><br><span class="line">      &#123;</span><br><span class="line">        <span class="attr">type</span>: <span class="string">&#x27;tool_use&#x27;</span>,   <span class="comment">// 或 text / thinking</span></span><br><span class="line">        <span class="attr">id</span>: <span class="string">&#x27;toolu_xxx&#x27;</span>,</span><br><span class="line">        <span class="attr">name</span>: <span class="string">&#x27;Read&#x27;</span>,</span><br><span class="line">        <span class="attr">input</span>: &#123; ... &#125;      <span class="comment">// 在 content_block_stop 时已经拼完整</span></span><br><span class="line">      &#125;</span><br><span class="line">    ]</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里的 <code>AssistantMessage</code> 还是“模型产出的消息块”；到了 Ch 06，它里面的 <code>tool_use block</code> 才会被当成真正的“待执行工具调用”继续往下处理。</p><p>和nanobot 对比：nanobot 用适配器把 Anthropic 原生格式转成统一 <code>LLMResponse</code> 抹平结构；Claude Code 基本直接沿用原生 message/block 结构，只在外层补本地元数据，并按 block 粒度逐条 yield。</p><h3 id="AssistantMessage-组装流程"><a href="#AssistantMessage-组装流程" class="headerlink" title="AssistantMessage 组装流程"></a>AssistantMessage 组装流程</h3><p><strong>Block 是什么</strong>：Anthropic API 一次回复的 content 是数组，每个元素是一个 block（thinking / text / tool_use），一次回复可同时包含多个 block。SSE 流式时每个 block 分批到达，claude.ts 用 <code>contentBlocks[]</code> 数组逐步拼装，收齐才封包。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line">经 SDK 3 层处理后到达本处，见「SSE 分层传输机制」</span><br><span class="line">  ↓</span><br><span class="line">SSE event 到达 claude.ts，准备开始拼装，刚进来样子大概是 &#123;event, content&#125; 这样</span><br><span class="line">  ↓</span><br><span class="line">event [content_block_start] 新 block 开始，按类型在 contentBlocks[index] 创建空槽等待填充</span><br><span class="line">  ├─ text      → &#123; text: &#x27;&#x27; &#125;</span><br><span class="line">  ├─ tool_use  → &#123; input: &#x27;&#x27; &#125;（工具参数是 JSON，后续 delta 逐步拼成完整字符串）</span><br><span class="line">  └─ thinking  → &#123; thinking: &#x27;&#x27;, signature: &#x27;&#x27; &#125;（signature 是 API 签发的加密签名）</span><br><span class="line">  ↓</span><br><span class="line">event [content_block_delta]  增量内容，token by token 追加到对应槽位</span><br><span class="line">  ├─ text_delta        → text += delta.text</span><br><span class="line">  ├─ input_json_delta  → input += delta.partial_json</span><br><span class="line">  └─ thinking_delta    → thinking += delta.thinking</span><br><span class="line">  ↓</span><br><span class="line">event [content_block_stop]  block 拼装完毕，从槽位取出，封装成 AssistantMessage yield 给 query.ts</span><br><span class="line">本层任务结束</span><br><span class="line">─────────────────────────────</span><br><span class="line">工具执行完成后，结果写回 messages 前做大小检查：</span><br><span class="line"></span><br><span class="line">[单条结果]</span><br><span class="line">  ├─ ≤ 50k chars  → 直接放入 messages</span><br><span class="line">  └─ &gt; 50k chars  → 写磁盘（session/tool-results/&#123;id&#125;.txt）</span><br><span class="line">                    模型收到：2000字节预览 + 文件路径（&lt;persisted-output&gt; 包裹）</span><br><span class="line">                    模型可用 FileReadTool 读完整内容</span><br><span class="line"></span><br><span class="line">[单轮所有工具结果总量]</span><br><span class="line">  ├─ ≤ 200k chars → 不处理</span><br><span class="line">  └─ &gt; 200k chars → 最大的几条持久化，直到低于阈值</span><br></pre></td></tr></table></figure><h3 id="SSE-分层传输机制"><a href="#SSE-分层传输机制" class="headerlink" title="SSE 分层传输机制"></a>SSE 分层传输机制</h3><p>A社的 Anthropic API 服务端返回每条消息长这样（event+data）：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">event: content_block_start</span><br><span class="line">data: &#123;&quot;type&quot;:&quot;content_block_start&quot;,&quot;index&quot;:0,&quot;content_block&quot;:&#123;&quot;type&quot;:&quot;thinking&quot;,&quot;thinking&quot;:&quot;&quot;&#125;&#125;</span><br><span class="line"></span><br><span class="line">event: content_block_delta</span><br><span class="line">data: &#123;&quot;type&quot;:&quot;content_block_delta&quot;,&quot;index&quot;:0,&quot;delta&quot;:&#123;&quot;type&quot;:&quot;thinking_delta&quot;,&quot;thinking&quot;:&quot;我来分析&quot;&#125;&#125;</span><br><span class="line"></span><br><span class="line">event: content_block_stop</span><br><span class="line">data: &#123;&quot;type&quot;:&quot;content_block_stop&quot;,&quot;index&quot;:0&#125;</span><br></pre></td></tr></table></figure><p>SDK 在 claude.ts 和 TCP 之间做了 <strong>3 层处理</strong>，每层自己做缓冲，不依赖下层，最终确保每一个 block 都是完整的：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">TCP 字节流（可在任意字节截断）</span><br><span class="line">  ↓</span><br><span class="line">  第1层 iterSSEChunks</span><br><span class="line">    字节缓冲区累积，扫到 \n\n 才 yield 一条完整 SSE 块，否则继续等</span><br><span class="line">  ↓</span><br><span class="line">  第2层 SSEDecoder</span><br><span class="line">    逐行读：event: 行存类型，data: 行存内容</span><br><span class="line">    遇到空行（\n\n 的第二个 \n）→ 攒好的字段组成 &#123;event, data&#125; yield 出去</span><br><span class="line">  ↓</span><br><span class="line">  第3层 fromSSEResponse</span><br><span class="line">    按 event 类型白名单过滤 + 反序列化 JSON.parse(data) → yield &#123;type, index, ...&#125;</span><br><span class="line">  ↓</span><br><span class="line">claude.ts 收到完整事件对象，进入 block 拼装（见「Message 组装」）</span><br></pre></td></tr></table></figure><p>每层自己做缓冲保证完整性，不依赖下层。claude.ts 拿到的是<strong>事件流</strong>，不是字节流，也不是组装好的 block。</p><p><strong>与 nanobot 对比</strong>：</p><table><thead><tr><th>维度</th><th>nanobot</th><th>Claude Code</th><th>优劣</th></tr></thead><tbody><tr><td>文字输出</td><td>等整个响应结束才返回</td><td>每个 token 立即 yield</td><td>CC ✅ 体验更好</td></tr><tr><td>工具参数传输</td><td>响应结束后整体解析</td><td>input_json_delta 边收边拼</td><td>CC ✅ 更早可用</td></tr><tr><td>工具结果超限</td><td>直接截断，内容丢失</td><td>持久化磁盘，模型可完整读取</td><td>CC ✅ 信息不丢失</td></tr><tr><td>Thinking 支持</td><td>作为字段返回，无流式</td><td>thinking block 独立流式累加</td><td>CC ✅</td></tr><tr><td>复杂度</td><td>简单直接</td><td>SSE 状态机，多 block 并行</td><td>nanobot ✅ 更易理解</td></tr><tr><td>多 block 回复</td><td>单次响应只有一段内容</td><td>一次回复可含 thinking+text+多工具</td><td>CC ✅ 表达能力强</td></tr></tbody></table><hr><h2 id="Ch-06-—-工具调用系统"><a href="#Ch-06-—-工具调用系统" class="headerlink" title="Ch 06 — 工具调用系统"></a>Ch 06 — 工具调用系统</h2><h3 id="Tool-接口设计"><a href="#Tool-接口设计" class="headerlink" title="Tool 接口设计"></a>Tool 接口设计</h3><p>定义在 <code>src/Tool.ts</code>：</p><table><thead><tr><th>字段</th><th>用途</th><th>nanobot 有？</th></tr></thead><tbody><tr><td><code>name</code></td><td>工具名，LLM 按名字调用</td><td>✅</td></tr><tr><td><code>prompt()</code></td><td>生成给 LLM 的工具说明，注入 API 的 tools 字段</td><td>✅ 但静态字符串</td></tr><tr><td><code>description()</code></td><td>生成给用户看的操作描述，用于权限弹窗展示</td><td>❌</td></tr><tr><td><code>inputSchema</code> (Zod)</td><td>参数结构校验</td><td>✅ JSON Schema</td></tr><tr><td><code>call()</code></td><td>实际执行函数</td><td>✅</td></tr><tr><td><code>isConcurrencySafe()</code></td><td>调度层用，决定能否并发执行</td><td>❌</td></tr><tr><td><code>isReadOnly()</code></td><td>权限判断，只读命令不需用户确认</td><td>❌</td></tr><tr><td><code>isDestructive()</code></td><td>UI 警示用，MCP 列表标红，不影响执行</td><td>❌</td></tr><tr><td><code>interruptBehavior()</code></td><td>Ctrl+C 时 cancel 立刻停 / block 等完成</td><td>❌</td></tr><tr><td><code>maxResultSizeChars</code></td><td>结果大小上限，超限持久化磁盘</td><td>❌</td></tr></tbody></table><p>nanobot 工具接口扁平，调度层不知道工具”性格”，只能串行。Claude Code 工具自己声明行为，调度层据此决定并发还是串行。</p><h3 id="完整工具执行路径（LLM-返回-→-结果追回）"><a href="#完整工具执行路径（LLM-返回-→-结果追回）" class="headerlink" title="完整工具执行路径（LLM 返回 → 结果追回）"></a>完整工具执行路径（LLM 返回 → 结果追回）</h3><p><strong>[1] SSE 流识别 tool_use（claude.ts）</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">前面已经讲过</span><br></pre></td></tr></table></figure><p><strong>[2] query.ts 收集（query.ts）</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">前面已经讲过，存在AssistantMessage 的 block.type === &#x27;tool_use&#x27; → 推入 toolUseBlocks[]</span><br></pre></td></tr></table></figure><p><strong>[3] 分批调度（toolOrchestration.ts）</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">runTools(toolUseBlocks)</span><br><span class="line">  → partitionToolCalls()  按 isConcurrencySafe 分批</span><br><span class="line">       ├─ [Grep, Glob] → 并发（连续 safe，最大并发 10）</span><br><span class="line">       ├─ [Bash]       → 串行（unsafe）</span><br><span class="line">       └─ [Grep]       → 并发</span><br><span class="line">  → 每批结果 yield → query.ts 收集进 toolResults[]</span><br></pre></td></tr></table></figure><p><code>isConcurrencySafe</code> 由工具自己实现，三种模式：</p><ul><li>纯读工具 → 硬编码 <code>true</code>；状态变更工具 → 硬编码 <code>false</code>；未实现 → 默认 <code>false</code></li><li>BashTool 特殊：解析实际命令动态判断，<code>git log</code> 为 true，<code>rm</code> 为 false</li><li>调度层只调用，不主观判断</li></ul><p><strong>[4] 单工具执行（toolExecution.ts）</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">runToolUse(单个 block)</span><br><span class="line">  ├─ 按 name 查找工具</span><br><span class="line">  ├─ Zod 校验参数</span><br><span class="line">  ├─ PreToolUse hooks → 可修改 input，或 block 执行</span><br><span class="line">  ├─ canUseTool() 权限判断</span><br><span class="line">  │    ├─ isReadOnly() → 静态白名单（git log/grep/ls 等），不弹窗</span><br><span class="line">  │    └─ 非只读 → 弹窗让用户确认</span><br><span class="line">  ├─ denied → 返回 error tool_result</span><br><span class="line">  ├─ allowed → tool.call() 真正执行</span><br><span class="line">  └─ PostToolUse hooks</span><br><span class="line">       ├─ MCP tool → 可替换 toolOutput</span><br><span class="line">       └─ 普通 tool → 追加额外消息</span><br></pre></td></tr></table></figure><p><strong>[5] 结果追回（query.ts）</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">toolResults[] 追加进 messages → continue → 下一轮调 LLM</span><br></pre></td></tr></table></figure><p><strong>nanobot 对比</strong>：无权限系统、无 hooks、无并发调度，工具直接串行 call()。</p><hr><h2 id="Ch-07-—-流式执行引擎（默认关闭）"><a href="#Ch-07-—-流式执行引擎（默认关闭）" class="headerlink" title="Ch 07 — 流式执行引擎（默认关闭）"></a>Ch 07 — 流式执行引擎（默认关闭）</h2><p>实验性引擎（<code>config.gates.streamingToolExecution</code>，<strong>默认关闭</strong>），LLM 流式返回期间 tool_use block 一到手就立即入队执行，不等流结束。</p><h3 id="StreamingToolExecutor-完整流程"><a href="#StreamingToolExecutor-完整流程" class="headerlink" title="StreamingToolExecutor 完整流程"></a>StreamingToolExecutor 完整流程</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">创建：queryLoop 开始时 new StreamingToolExecutor()</span><br><span class="line">  ↓</span><br><span class="line">LLM 流式期间：</span><br><span class="line">├─ 收到 tool_use block → addTool() 入队（status = &#x27;queued&#x27;）</span><br><span class="line">├─ 工具后台并发执行（按并发规则）</span><br><span class="line">└─ query.ts 定期调用 getCompletedResults() 输出已完成的（非阻塞）</span><br><span class="line">  ↓</span><br><span class="line">LLM 流式结束：</span><br><span class="line">└─ query.ts 调用 getRemainingResults()（阻塞）</span><br><span class="line">    └─ 内部循环调用 getCompletedResults() 直到所有工具完成</span><br></pre></td></tr></table></figure><h3 id="单个Tool状态机"><a href="#单个Tool状态机" class="headerlink" title="单个Tool状态机"></a>单个Tool状态机</h3><p><strong>ToolStatus</strong>：</p><table><thead><tr><th>状态</th><th>含义</th><th>进入条件</th><th>流转到</th></tr></thead><tbody><tr><td><code>queued</code></td><td>已入队，等待调度</td><td><code>addTool()</code> 调用</td><td><code>executing</code></td></tr><tr><td><code>executing</code></td><td>执行中</td><td><code>canExecuteTool()</code> 为 true</td><td><code>completed</code></td></tr><tr><td><code>completed</code></td><td>执行结束，结果就绪</td><td><code>collectResults()</code> 完成（含合成错误）</td><td><code>yielded</code></td></tr><tr><td><code>yielded</code></td><td>结果已消费，终态</td><td><code>getCompletedResults()</code> 输出后</td><td>无</td></tr></tbody></table><p>无失败状态——失败工具收到合成 error tool_result 后直接进入 <code>completed</code>。</p><p><strong>并发规则</strong>（<code>canExecuteTool</code>）：无 executing 工具时任何工具可执行；有 executing 时，新工具和所有 executing 都是 safe 才可并发，否则等待。unsafe 工具独占执行。</p><h3 id="工具执行与输出流程"><a href="#工具执行与输出流程" class="headerlink" title="工具执行与输出流程"></a>工具执行与输出流程</h3><p>确保流式执行的主要的逻辑在 <code>getCompletedResults</code>这个方法里。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line">核心总结：</span><br><span class="line">├─ unsafe 工具独占执行 + 阻塞后续输出（保证执行顺序和安全）</span><br><span class="line">├─ safe 工具可并发 + 按完成顺序输出（可乱序）</span><br><span class="line">└─ 外部拼接 tool 结果时，通过 tool_use_id 将 tool_result 匹配到对应的 tool_use，并不依赖输出顺序</span><br><span class="line"></span><br><span class="line">详细流程：</span><br><span class="line">getRemainingResults() 主循环</span><br><span class="line">├─ while (还有未完成的工具)</span><br><span class="line">│   ├─ processQueue() — 按并发规则启动 queued 工具</span><br><span class="line">│   │   └─ for (tool of tools 按接收顺序)</span><br><span class="line">│   │       ├─ if (status !== &#x27;queued&#x27;) → continue</span><br><span class="line">│   │       ├─ 并发规则检查：</span><br><span class="line">│   │       │   executingTools = 筛选 status === &#x27;executing&#x27; 的工具</span><br><span class="line">│   │       │   canExecute = executingTools 为空</span><br><span class="line">│   │       │             || (tool 是 safe &amp;&amp; 所有 executingTools 都是 safe)</span><br><span class="line">│   │       ├─ if (canExecute)</span><br><span class="line">│   │       │   ├─ status = &#x27;executing&#x27;</span><br><span class="line">│   │       │   └─ 启动 tool.call()</span><br><span class="line">│   │       └─ else if (!tool.isConcurrencySafe)</span><br><span class="line">│   │           └─ break — unsafe 工具等待，阻塞后续调度</span><br><span class="line">│   │</span><br><span class="line">│   ├─ getCompletedResults() — 非阻塞（每次循环都调用）</span><br><span class="line">│   │   └─ for (tool of tools 按接收顺序)</span><br><span class="line">│   │       ├─ 优先输出进度消息（while pendingProgress 非空 → yield）</span><br><span class="line">│   │       ├─ if (status === &#x27;yielded&#x27;) → continue</span><br><span class="line">│   │       ├─ if (status === &#x27;completed&#x27;)</span><br><span class="line">│   │       │   ├─ yield tool.results</span><br><span class="line">│   │       │   └─ status = &#x27;yielded&#x27;</span><br><span class="line">│   │       └─ else if (status === &#x27;executing&#x27; &amp;&amp; !tool.isConcurrencySafe)</span><br><span class="line">│   │           └─ break — unsafe 工具执行中，阻塞后续输出</span><br><span class="line">│   │</span><br><span class="line">│   └─ if (有工具在执行 &amp;&amp; 没有完成的 &amp;&amp; 没有进度)</span><br><span class="line">│       └─ await Promise.race(所有执行中的 promise) — 等待任一完成</span><br><span class="line">│</span><br><span class="line">└─ 最后再调一次 getCompletedResults() — 输出剩余结果</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="三层-AbortController-架构"><a href="#三层-AbortController-架构" class="headerlink" title="三层 AbortController 架构"></a>三层 AbortController 架构</h3><p><strong>重要</strong>：StreamingToolExecutor 由 <code>config.gates.streamingToolExecution</code> 控制，<strong>默认关闭</strong>。关闭时走 <code>runTools()</code> 路径，只有第 1 层 <code>toolUseContext.abortController</code>，无三层结构。</p><p><strong>概念层级与 AbortController 对应</strong>（父子单向传播：父 abort → 子自动 abort；子 abort → 父不受影响）：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">Agent 层（整个对话轮次）</span><br><span class="line">├─ query.ts: queryLoop()</span><br><span class="line">├─ AbortController: toolUseContext.abortController</span><br><span class="line">└─ 生命周期：整个 query 循环</span><br><span class="line">  ↓ 创建子级</span><br><span class="line"></span><br><span class="line">工具编排层（兄弟工具批次：多个工具可能共享一个批次）</span><br><span class="line">├─ StreamingToolExecutor.ts: 构造函数</span><br><span class="line">├─ AbortController: siblingAbortController</span><br><span class="line">└─ 生命周期：单批次工具（单次 assistant message）</span><br><span class="line">  ↓ 创建子级</span><br><span class="line"></span><br><span class="line">单个工具层（单个工具执行）</span><br><span class="line">├─ StreamingToolExecutor.ts: processQueue()</span><br><span class="line">├─ AbortController: toolAbortController</span><br><span class="line">└─ 生命周期：单个工具执行</span><br><span class="line">  ↓ 传递 signal</span><br><span class="line"></span><br><span class="line">Tool 实现（BashTool/GrepTool）</span><br><span class="line">└─ 接收 signal（只读），监听 signal.aborted</span><br></pre></td></tr></table></figure><p><strong>为什么分三层</strong>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">用户 Ctrl+C → 停所有（第 1 层 abort，全链自动 abort）</span><br><span class="line">Bash 出错 → 停兄弟工具，query 继续（第 2 层 abort，第 1 层不受影响）</span><br><span class="line">权限拒绝 → 停整个 query（第 3 层主动冒泡到第 1 层）</span><br></pre></td></tr></table></figure><hr><h2 id="Ch-08-—-Thinking-推理模式"><a href="#Ch-08-—-Thinking-推理模式" class="headerlink" title="Ch 08 — Thinking 推理模式"></a>Ch 08 — Thinking 推理模式</h2><h3 id="Thinking-Config-与模型支持"><a href="#Thinking-Config-与模型支持" class="headerlink" title="Thinking Config 与模型支持"></a>Thinking Config 与模型支持</h3><p>定义在 <code>src/utils/thinking.ts</code>，三种类型：</p><table><thead><tr><th>类型</th><th>含义</th><th>适用</th></tr></thead><tbody><tr><td><code>adaptive</code></td><td>模型自决推理量</td><td>新模型默认</td></tr><tr><td><code>enabled + budgetTokens</code></td><td>固定 token 预算</td><td>旧模型</td></tr><tr><td><code>disabled</code></td><td>关闭 thinking</td><td>—</td></tr></tbody></table><p><strong>模型支持范围</strong>：</p><ul><li>thinking：1P/Foundry 全部 Claude 4+；3P（Bedrock/Vertex）仅 Sonnet 4+ / Opus 4+</li><li>adaptive 模式：仅 opus-4-6 / sonnet-4-6（未知新 1P 模型默认 true，向前兼容）</li></ul><p><strong>API 构造</strong>：支持 adaptive 的模型发 <code>&#123; type: &#39;adaptive&#39; &#125;</code>；否则发 <code>&#123; budget_tokens: min(maxOutput-1, budgetTokens) &#125;</code>。</p><h3 id="发送给LLM的Thinking-Block-约束（3条）"><a href="#发送给LLM的Thinking-Block-约束（3条）" class="headerlink" title="发送给LLM的Thinking Block 约束（3条）"></a>发送给LLM的Thinking Block 约束（3条）</h3><p><strong>给LLM发的 API 硬约束</strong>（违反返回 400）：</p><ol><li>若上下文含 thinking block 的历史，那么本次 budget 必须 &gt; 0</li><li>thinking block 不能是 AssistantMessage 的最后一个 block</li><li>有工具调用时，<code>thinking → tool_use → tool_result → 下轮 assistant</code> 整链不能断</li></ol><p><strong>签名机制</strong>：thinking block 内嵌加密签名，绑定模型 + API Key，不可跨模型/跨账号复用。</p><p><strong>清除时机</strong>：切换 fallback 模型、<code>/login</code> 换账号 → <code>stripSignatureBlocks()</code> 清除历史 thinking block；streaming 中途重试 → yield tombstone 从 UI 和历史中删除。</p><p><strong>服务端保留</strong>：<code>context-management-2025-06-27</code> beta，Claude 4+ 且 provider 为 firstParty/Foundry 时启用；Bedrock/Vertex 不触发；compaction 时由服务端保留 thinking block。</p><hr><h2 id="Ch-09-—-Task-系统"><a href="#Ch-09-—-Task-系统" class="headerlink" title="Ch 09 — Task 系统"></a>Ch 09 — Task 系统</h2><h3 id="持久化任务看板"><a href="#持久化任务看板" class="headerlink" title="持久化任务看板"></a>持久化任务看板</h3><p>不同于 <code>toolUseBlocks[]</code>（当轮执行即丢弃），Task 是<strong>跨轮次、跨进程</strong>的持久化协调机制。核心用途：LLM 将复杂请求拆解为多条 Task，由多个 subAgent 自主认领并行执行。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Task 数据结构（src/utils/tasks.ts）</span><br><span class="line">  ├─ id          自增数字（文件名即 ID）</span><br><span class="line">  ├─ subject     任务标题</span><br><span class="line">  ├─ description 详细说明（跨 Agent 传递上下文的唯一载体）</span><br><span class="line">  ├─ status      pending → in_progress → completed</span><br><span class="line">  ├─ owner       认领它的 agent ID</span><br><span class="line">  ├─ blocks      本任务阻塞的其他任务 ID[]</span><br><span class="line">  ├─ blockedBy   阻塞本任务的任务 ID[]（DAG 依赖图）</span><br><span class="line">  └─ metadata    任意附加数据</span><br></pre></td></tr></table></figure><p>High Water Mark：<code>max(文件最大ID, 历史记录ID)</code>，防止 reset 后 ID 复用。</p><h3 id="taskListId-优先级"><a href="#taskListId-优先级" class="headerlink" title="taskListId 优先级"></a>taskListId 优先级</h3><p>存储：<code>~/.claude/tasks/&lt;taskListId&gt;/</code>，每条 Task 一个 JSON 文件，session 结束不清理。taskListId 对 LLM 透明，框架在每次工具调用时内部静默解析。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">优先级（高→低）</span><br><span class="line">  ├─ CLAUDE_CODE_TASK_LIST_ID 环境变量    外部强制指定</span><br><span class="line">  ├─ in-process teammate 上下文           同进程 teammate 用 leader teamName</span><br><span class="line">  ├─ CLAUDE_CODE_TEAM_NAME 环境变量       进程外 teammate（tmux/iTerm2 注入）</span><br><span class="line">  ├─ leaderTeamName（内存变量）           leader 调 TeamCreate 时设置</span><br><span class="line">  └─ sessionId                           兜底，单 Agent 独立隔离</span><br></pre></td></tr></table></figure><h3 id="两级锁保证任务原子性"><a href="#两级锁保证任务原子性" class="headerlink" title="两级锁保证任务原子性"></a>两级锁保证任务原子性</h3><p>锁完全靠磁盘竞争实现，内存中无锁变量。<code>proper-lockfile</code> 在目标文件旁创建 <code>.lock</code> 文件，OS 保证创建原子性，进程崩溃后有 stale lock 检测（检查 mtime）。</p><table><thead><tr><th>锁粒度</th><th>锁的目标文件</th><th>用于</th></tr></thead><tbody><tr><td>任务级锁</td><td><code>&lt;id&gt;.json</code>（旁边创建 <code>&lt;id&gt;.json.lock</code>）</td><td>updateTask / 普通认领</td></tr><tr><td>列表级锁</td><td><code>.lock</code>（tasks 目录下预建空文件）</td><td>createTask / resetTask / 严格认领</td></tr></tbody></table><p>失败时指数退避重试，最多 30 次：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">等待时间 = min(5 × 2^n, 100) ms</span><br><span class="line">第1次 10ms → 第2次 20ms → … → 第5次起 100ms（触顶）</span><br><span class="line">总上限 ≈ 2.6s，目标：支撑 10+ 并发 swarm Agent</span><br></pre></td></tr></table></figure><h3 id="Agent-领取任务执行流程"><a href="#Agent-领取任务执行流程" class="headerlink" title="Agent 领取任务执行流程"></a>Agent 领取任务执行流程</h3><p>涉及方法：</p><table><thead><tr><th>方法</th><th>工具</th><th>作用</th></tr></thead><tbody><tr><td><code>createTask()</code></td><td>TaskCreateTool</td><td>创建，初始 pending，blocks/blockedBy 为空</td></tr><tr><td><code>listTasks()</code></td><td>TaskListTool</td><td>列出所有 Task（无锁读）</td></tr><tr><td><code>getTask()</code></td><td>TaskGetTool</td><td>读取单条完整 Task</td></tr><tr><td><code>updateTask()</code></td><td>TaskUpdateTool</td><td>更新字段（status/owner/description 等）| 内部可调 deleteTask 删除</td></tr><tr><td><code>blockTask()</code></td><td>TaskUpdateTool(addBlocks)</td><td>双向写 blocks/blockedBy 建立依赖</td></tr><tr><td><code>claimTask()</code></td><td>—</td><td>tasks.ts 内部函数，swarm 严格认领用，含原子 checkAgentBusy；TaskUpdateTool 日常直接用 updateTask 不走此路径</td></tr></tbody></table><p>以 Task #2 依赖 Task #1 为例：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line">LLM 拆解请求 → 决定创建两个串行 Task</span><br><span class="line">  ↓</span><br><span class="line">TaskCreateTool × 2</span><br><span class="line">  ├─ createTask() → 获列表锁 → 写 ~/.claude/tasks/&lt;id&gt;/1.json &#123; status:pending, blockedBy:[] &#125;</span><br><span class="line">  └─ createTask() → 获列表锁 → 写 2.json &#123; status:pending, blockedBy:[&quot;1&quot;] &#125;</span><br><span class="line">  ↓</span><br><span class="line">TaskUpdateTool → blockTask() → 双向写 1.blocks=[&quot;2&quot;], 2.blockedBy=[&quot;1&quot;]</span><br><span class="line">  ↓</span><br><span class="line">AgentTool 派出 Agent A、Agent B 并发启动</span><br><span class="line">  │</span><br><span class="line">  ├─ Agent A</span><br><span class="line">  │    TaskListTool → listTasks()（无锁读所有 .json）</span><br><span class="line">  │    找到 Task #1（pending，blockedBy 空）</span><br><span class="line">  │    TaskUpdateTool → updateTask() → 获任务级锁 → 写 in_progress + owner=A</span><br><span class="line">  │    [执行工作：toolUseBlocks 多轮 Read/Grep/Edit/Bash]</span><br><span class="line">  │    结果写入文件（FileWriteTool）</span><br><span class="line">  │    TaskUpdateTool → updateTask() → 写 completed</span><br><span class="line">  │</span><br><span class="line">  └─ Agent B</span><br><span class="line">       TaskListTool → listTasks()</span><br><span class="line">       找到 Task #2，blockedBy=[&quot;1&quot;]，Task #1 未 completed → 阻断，轮询等待</span><br><span class="line">       Task #1 completed 后：</span><br><span class="line">       TaskUpdateTool → updateTask() → 获任务级锁 → 写 in_progress + owner=B</span><br><span class="line">       TaskGetTool → getTask(&quot;2&quot;) → 读 description（含 A 写的文件路径）</span><br><span class="line">       Read(文件路径) → 拿到 Task #1 输出</span><br><span class="line">       [执行工作]</span><br><span class="line">       TaskUpdateTool → updateTask() → 写 completed</span><br></pre></td></tr></table></figure><p><strong>框架只查验 blockedBy，不传递结果</strong>：跨 Agent 输出靠 description 里约定路径 + 主动读文件，语义层全托管给 LLM。</p><hr><h2 id="Ch-10-—-Skill-系统"><a href="#Ch-10-—-Skill-系统" class="headerlink" title="Ch 10 — Skill 系统"></a>Ch 10 — Skill 系统</h2><p><strong>Skill 是 Markdown 模板 + 执行器。</strong>用户输入 <code>/simplify</code>，框架把模板展开成 <code>ContentBlockParam[]</code>，以 <code>role: user</code> 注入对话——等价于用户手动粘贴一段长 prompt。代码类型是 <code>Command.type === &#39;prompt&#39;</code>（<code>PromptCommand</code>），关键字段：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">PromptCommand 关键字段</span><br><span class="line">  ├─ name              斜杠命令名（/simplify → name: &#x27;simplify&#x27;）</span><br><span class="line">  ├─ description       typeahead 显示文字</span><br><span class="line">  ├─ source            来源：bundled / userSettings / projectSettings / mcp / plugin</span><br><span class="line">  ├─ loadedFrom        加载方式：bundled / skills / mcp / plugin / managed</span><br><span class="line">  ├─ allowedTools      此 skill 额外授权的工具列表</span><br><span class="line">  ├─ context           执行模式：&#x27;inline&#x27;（默认）/ &#x27;fork&#x27;</span><br><span class="line">  ├─ agent             fork 时使用的subAgent 类型（默认 &#x27;general-purpose&#x27;）</span><br><span class="line">  ├─ model             指定模型，不填继承当前会话</span><br><span class="line">  ├─ userInvocable     false → 仅模型可调用，用户 /skill 报错</span><br><span class="line">  ├─ hooks             skill 激活时挂载的 hooks</span><br><span class="line">  ├─ effort            传给subAgent 的 effort 级别</span><br><span class="line">  └─ getPromptForCommand(args, ctx) → ContentBlockParam[]</span><br></pre></td></tr></table></figure><p><code>/simplify</code> 触发后往 messages 插 3 条：标题行（用户可见）、Skill 正文（<code>isMeta: true</code>，仅 LLM 可见）、权限包（allowedTools / model）。</p><p><strong>三种来源</strong>（<code>loadedFrom</code>）：bundled（编译进二进制，启动注册）、disk（<code>.claude/skills/</code> Markdown 文件）、mcp（stub，跳过）。disk skill 三层优先级：policy &gt; user（<code>~/.claude/skills/</code>）&gt; project（<code>.claude/skills/</code>）。</p><p><strong>SkillTool</strong> 是模型主动触发的路径：LLM 像调工具一样 <code>&#123; name: &#39;Skill&#39;, input: &#123; name: &#39;simplify&#39; &#125; &#125;</code>，走 fork subAgent，结果以 tool_result 返回。<code>user-invocable: false</code> 的 Skill 只走此路径，用户直接 <code>/skill</code> 报错。</p><table><thead><tr><th></th><th>nanobot</th><th>Claude Code</th></tr></thead><tbody><tr><td>可复用工作流</td><td>无</td><td>Skill 系统</td></tr><tr><td>subAgent 触发</td><td>手动构造</td><td>fork 自动 / SkillTool</td></tr><tr><td>模型主动触发</td><td>不支持</td><td>SkillTool</td></tr></tbody></table><h3 id="Skill-自定义（disk-skill）"><a href="#Skill-自定义（disk-skill）" class="headerlink" title="Skill 自定义（disk skill）"></a>Skill 自定义（disk skill）</h3><p>Markdown 文件放入 <code>.claude/skills/</code> 即可，文件名即命令名：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">---</span><br><span class="line">description: 代码审查工具          # typeahead 显示，必填</span><br><span class="line">allowed-tools: Bash, Read, Grep   # 额外授权工具</span><br><span class="line">argument-hint: &lt;PR号&gt;             # 输入占位提示</span><br><span class="line">when_to_use: PR 合并前运行        # 告诉模型何时自动触发</span><br><span class="line">context: fork                     # inline（默认）/ fork</span><br><span class="line">model: claude-opus-4-6            # 不填则继承当前会话</span><br><span class="line">user-invocable: true              # false → 仅模型可调用</span><br><span class="line">---</span><br><span class="line"></span><br><span class="line"># 正文是给 LLM 的指令</span><br><span class="line"></span><br><span class="line">你的任务是审查 $ARGS 这个 PR...</span><br><span class="line"></span><br><span class="line">!`git log --oneline -10`          # 内联 shell，加载时执行并内联进 prompt</span><br></pre></td></tr></table></figure><p>变量替换：<code>$ARGS</code> / <code>$1 $2 …</code>（用户参数）、<code>$&#123;CLAUDE_SKILL_DIR&#125;</code>（skill 所在目录）、<code>$&#123;CLAUDE_SESSION_ID&#125;</code>（当前 session）。内联 shell MCP skill 禁用。</p><h3 id="执行模式"><a href="#执行模式" class="headerlink" title="执行模式"></a>执行模式</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">用户输入 /simplify</span><br><span class="line">  ↓</span><br><span class="line">processSlashCommand() 找到 Command（type: &#x27;prompt&#x27;）</span><br><span class="line">  ↓</span><br><span class="line">  ├─ 分支1 context: &#x27;inline&#x27;（默认）</span><br><span class="line">  │    ↓</span><br><span class="line">  │   getPromptForCommand(args) → ContentBlockParam[]</span><br><span class="line">  │    ↓</span><br><span class="line">  │   插入 messages（标题 + 正文 + 权限包）</span><br><span class="line">  │    ↓</span><br><span class="line">  │   shouldQuery: true → 进入正常 queryLoop</span><br><span class="line">  │</span><br><span class="line">  └─ 分支2 context: &#x27;fork&#x27; 在注册skill时就配置好用fork或者前端显式调用</span><br><span class="line">       ↓</span><br><span class="line">      prepareForkedCommandContext() 准备subAgent 上下文</span><br><span class="line">       ↓</span><br><span class="line">      runAgent() → 独立 queryLoop（独立 context + token budget）</span><br><span class="line">       ↓</span><br><span class="line">      extractResultText() → 只取最后一条 AssistantMessage 文本</span><br><span class="line">      （中间所有 tool_use / tool_result / thinking 全丢）</span><br><span class="line">       ↓</span><br><span class="line">      包进 &lt;scheduled-task-result&gt;</span><br><span class="line">       ↓</span><br><span class="line">      isMeta 消息重进主会话队列，主 LLM 下一轮可见</span><br></pre></td></tr></table></figure><hr><h2 id="Ch-11-—-Compact：对话压缩"><a href="#Ch-11-—-Compact：对话压缩" class="headerlink" title="Ch 11 — Compact：对话压缩"></a>Ch 11 — Compact：对话压缩</h2><p><code>state.messages</code> 随对话积累，发给 API 前经 <code>getMessagesAfterCompactBoundary()</code> 从最后一个 compact boundary 切片，控制实际 token 量。超限时触发压缩，把旧历史替换为摘要 + boundary marker，之后 LLM 只看到摘要和近期对话。</p><h3 id="四层压缩结构"><a href="#四层压缩结构" class="headerlink" title="四层压缩结构"></a>四层压缩结构</h3><p><strong>第1层 Microcompaction — 每轮 API 调用前静默运行，不生成摘要</strong></p><p>公开版：时间触发路径仍运行；ant-only 路径和 legacy 路径已移除。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">时间触发（距上次 assistant 消息超阈值，cache 已冷）</span><br><span class="line">  └─ 把旧工具结果替换为占位符 [Old tool result content cleared]</span><br><span class="line">     保留最近 N 条不动，直接修改 messages 内容</span><br><span class="line"></span><br><span class="line">计数触发（ant-only cached 路径）</span><br><span class="line">  └─ 用 cache_edits API 从服务端删除旧工具结果</span><br><span class="line">     不修改本地 messages，只改服务端 cache，不失效 prompt cache</span><br><span class="line"></span><br><span class="line">公开版：legacy path 已移除（源码注释明确），直接 return &#123; messages &#125;，什么都不做</span><br></pre></td></tr></table></figure><p><strong>第2-3层 Autocompact — token 超阈值时触发，生成摘要</strong></p><p>触发阈值（以 200k 模型为例）：</p><p>水位线：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">147k warningThreshold     ← 显示用量警告</span><br><span class="line">167k autoCompactThreshold ← 触发 Autocompact</span><br><span class="line">177k blockingLimit        ← 阻断新轮次，须手动 /compact</span><br></pre></td></tr></table></figure><p>推导来源（均基于 effectiveContextWindow = 200k 总值 - 20k 回复预留 = 180k）：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">180k - 20k（WARNING_THRESHOLD_BUFFER_TOKENS） = 160k（Autocompact 关时）</span><br><span class="line">                                                147k（Autocompact 开时，基准为 167k）</span><br><span class="line">180k - 13k（AUTOCOMPACT_BUFFER_TOKENS）       = 167k</span><br><span class="line">180k -  3k（MANUAL_COMPACT_BUFFER_TOKENS）    = 177k</span><br></pre></td></tr></table></figure><blockquote><p>为什么减 20k：200k context_window 是读写共用总额度（<code>input_tokens + max_tokens ≤ 200k</code>），摘要调用需预留 20k 给输出，故 input 上限收缩至 180k。</p></blockquote><p>正常流程：147k warningThreshold 触发警告 → 167k Autocompact 介入 → 连续失败 3 次熔断（<code>MAX_CONSECUTIVE_AUTOCOMPACT_FAILURES</code>）→ 之后跳过压缩直到撞 177k blockingLimit → 退出 loop，须手动 <code>/compact</code>。</p><p>触发方式：自动（token 用量超阈值）或手动 <code>/compact</code>（可附自定义摘要指令）。</p><p>压缩路径：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">├─ 先试 Session Memory Compaction</span><br><span class="line">│    └─ 直接读 Session Memory 文件组装摘要，不调 LLM，最快</span><br><span class="line">│       ❌ feature flag 默认 false，公开版跳过</span><br><span class="line">└─ Traditional Compaction（公开版实际走这里）</span><br><span class="line">     ├─ ① PreCompact hooks</span><br><span class="line">     ├─ ② fork subAgent 调 LLM 生成摘要</span><br><span class="line">     │      └─ 摘要请求本身撞 prompt_too_long</span><br><span class="line">     │           → 丢最老 API round 重试，最多 3 次（MAX_PTL_RETRIES）</span><br><span class="line">     │              API round = 一次 LLM 调用的 [assistant回复 + tool_results]</span><br><span class="line">     ├─ ③ snapshot readFileState → clear()</span><br><span class="line">     │      readFileState：LRU Map（path → content/timestamp），FileReadTool 读文件后写入</span><br><span class="line">     │      compact 后历史变了，旧缓存失效故清空；先 snapshot 供后续恢复</span><br><span class="line">     ├─ ④ 并行准备 post-compact 附件</span><br><span class="line">     │      ├─ 最近读过的文件（≤5个）：从 snapshot 取路径重新读，防 LLM&quot;失忆&quot;</span><br><span class="line">     │      ├─ plan / plan mode 附件：恢复 /plan 状态</span><br><span class="line">     │      └─ deferred tools / MCP instructions：重新声明被压缩吃掉的工具列表</span><br><span class="line">     ├─ ⑤ 创建 boundaryMarker（SystemCompactBoundaryMessage）+ summaryMessages（UserMessage）</span><br><span class="line">     │      summaryMessages：isCompactSummary=true，isVisibleInTranscriptOnly=true（REPL 不显示）</span><br><span class="line">     │      内容：&quot;This session is being continued...\nSummary:\n[摘要]\n\ntranscript 路径&quot;</span><br><span class="line">     ├─ ⑥ SessionStart hooks 重跑 / PostCompact hooks</span><br><span class="line">     └─ ⑦ 返回 CompactionResult</span><br></pre></td></tr></table></figure><p><strong>第4层 Reactive Compaction — API 真正返回 413 后触发</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">LLM 返回 prompt_too_long（HTTP 413）</span><br><span class="line">  ├─ 先尝试 context_collapse drain（❌ feature flag，公开版关）</span><br><span class="line">  └─ tryReactiveCompact()</span><br><span class="line">       ├─ hasAttemptedReactiveCompact = false → 执行压缩 → 重试当前轮</span><br><span class="line">       └─ hasAttemptedReactiveCompact = true  → return &#123; reason: &#x27;prompt_too_long&#x27; &#125;</span><br><span class="line"></span><br><span class="line">只试 1 次</span><br><span class="line">❌ feature(&#x27;REACTIVE_COMPACT&#x27;) = false，公开版整层 stub</span><br></pre></td></tr></table></figure><h3 id="压缩后结构"><a href="#压缩后结构" class="headerlink" title="压缩后结构"></a>压缩后结构</h3><p><code>buildPostCompactMessages()</code> 把 <strong>CompactionResult</strong> 组装成新的 <code>state.messages</code>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">[boundaryMarker]     type=&#x27;system&#x27;，getMessagesAfterCompactBoundary 的切片锚点</span><br><span class="line">[summaryMessages]    type=&#x27;user&#x27;，LLM 摘要</span><br><span class="line">[messagesToKeep]     近期保留原始消息（Session Memory 路径才有）</span><br><span class="line">[attachments]        文件内容 / plan / tools 声明</span><br><span class="line">[hookResults]        SessionStart hooks 重跑的结果</span><br></pre></td></tr></table></figure><p>下一轮 <code>normalizeMessagesForAPI</code> 过滤后，<strong>LLM 实际收到</strong>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&#123; role: &#x27;user&#x27;, content: &#x27;This session is being continued...\nSummary:\n[摘要]&#x27; &#125;</span><br><span class="line">&#123; role: &#x27;user&#x27;, content: [...文件内容...] &#125;</span><br><span class="line">&#123; role: &#x27;user&#x27;, content: [...tools delta...] &#125;</span><br><span class="line">... messagesToKeep 里的原始 user/assistant 消息 ...</span><br><span class="line">&#123; role: &#x27;user&#x27;, content: &#x27;新的用户输入&#x27; &#125;</span><br></pre></td></tr></table></figure><p>boundary marker（type=’system’）被过滤掉，LLM 完全看不到，只感知到对话从一条摘要 user 消息开始。</p><p><strong>完整对话历史</strong>同时实时 append 到磁盘 transcript 文件（<code>~/.claude/projects/&lt;项目&gt;/&lt;sessionId&gt;.jsonl</code>），compact 不删除它。summaryMessage 里注明了路径，LLM 需要时可主动读取找回压缩前细节。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">state.messages    内存，compact 后替换为摘要</span><br><span class="line">transcript.jsonl  磁盘，只 append，压缩不影响</span><br></pre></td></tr></table></figure><hr><h2 id="Ch-12-—-记忆系统"><a href="#Ch-12-—-记忆系统" class="headerlink" title="Ch 12 — 记忆系统"></a>Ch 12 — 记忆系统</h2><p>不管怎么样，记忆系统想做到的永远是跨Session的上下文保存。</p><p>三种记忆共同点：用文件存储，靠 prompt 注入读取。写的触发方式各不同——Auto Memory 和 Agent Memory 靠 LLM 自主决定，Session Memory 靠框架 PostSampling hook 自动提取；读的筛选由 Sonnet 做语义匹配，框架负责注入。</p><h3 id="三种记忆对比"><a href="#三种记忆对比" class="headerlink" title="三种记忆对比"></a>三种记忆对比</h3><table><thead><tr><th></th><th>Auto Memory</th><th>Session Memory</th><th>Agent Memory</th></tr></thead><tbody><tr><td>作用域</td><td>一个 git repo</td><td>一次会话</td><td>user / project / local</td></tr><tr><td>存储</td><td><code>~/.claude/projects/&lt;repo&gt;/memory/</code></td><td><code>~/.claude/session-memory/&lt;sessionId&gt;.md</code></td><td>user: <code>~/.claude/agent-memory/&lt;type&gt;/</code> project: <code>.claude/agent-memory/&lt;type&gt;/</code> local: <code>.claude/agent-memory-local/&lt;type&gt;/</code></td></tr><tr><td>记什么</td><td>关于用户和项目的知识（偏好、约定、外部系统）</td><td>当前会话关键进展（纯笔记，无 frontmatter）</td><td>该类型 Agent 执行任务中积累的专属经验</td></tr><tr><td>写</td><td>LLM 自主用 Write 工具</td><td>PostSampling hook 自动 fork subAgent 提取</td><td>subAgent 自主用 Write 工具</td></tr><tr><td>读</td><td>每轮 Sonnet 筛选注入（≤5 个）</td><td>Compact 时 Session Memory Compaction 路径读取</td><td>Agent spawn 时一次性注入系统 prompt</td></tr><tr><td>公开版</td><td>✅</td><td>❌</td><td>✅</td></tr></tbody></table><p>Auto Memory 和 Agent Memory 用同一套文件格式（frontmatter + 正文），共用 <code>buildMemoryLines</code> prompt 模板；内容视角不同——前者记”关于用户/项目的知识”，后者记”这类 Agent 执行任务时学到的东西”。</p><h3 id="触发流程"><a href="#触发流程" class="headerlink" title="触发流程"></a>触发流程</h3><p><strong>① 会话启动（一次性）</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">CLAUDE.md 文件加载 → getUserContext()</span><br><span class="line">  Managed → User → Project/Local（从根到 CWD）→ AutoMem(MEMORY.md)</span><br><span class="line">  └─ 注入 userContext，以第一条 user 消息固定在对话头部，全程不变</span><br><span class="line"></span><br><span class="line">Agent Memory Snapshot 检查（仅 user scope agent，loadAgentsDir.ts）</span><br><span class="line">  ├─ action: &#x27;none&#x27;          → 跳过</span><br><span class="line">  ├─ action: &#x27;initialize&#x27;    → 从 snapshot 覆盖复制初始化</span><br><span class="line">  └─ action: &#x27;prompt-update&#x27; → 仅提示，不自动覆盖</span><br></pre></td></tr></table></figure><p><strong>② 每次用户输入（每轮开始）</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">startRelevantMemoryPrefetch() 异步发起（不阻塞）</span><br><span class="line">  └─ Sonnet 扫 memory 目录所有文件的 filename + description</span><br><span class="line">     选 ≤5 个相关文件；已展示过的排除；单词输入跳过</span><br><span class="line">     结果挂在 pendingMemoryPrefetch 等待消费</span><br><span class="line"></span><br><span class="line">MEMORY.md 索引已在 userContext，无需额外处理</span><br></pre></td></tr></table></figure><p><strong>③ 工具执行循环中（每轮内部）</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">pendingMemoryPrefetch settled？</span><br><span class="line">  ├─ 是 → filterDuplicateMemoryAttachments（过滤 readFileState 中已读文件）</span><br><span class="line">  │        → 注入为 attachment 消息，LLM 下一个 iteration 可见</span><br><span class="line">  └─ 否 → 跳过</span><br><span class="line"></span><br><span class="line">Auto Memory 写（LLM 自主）</span><br><span class="line">  └─ Write 工具 → ~/.claude/projects/&lt;repo&gt;/memory/&lt;topic&gt;.md</span><br><span class="line">     同步在 MEMORY.md 加一行索引（两步缺一不可）</span><br></pre></td></tr></table></figure><p><strong>④ 每次 LLM 响应后（PostSampling hook）</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">extractSessionMemory()（❌ 公开版 feature flag 关）</span><br><span class="line">  shouldExtractMemory() 判断（同时满足 token 增量阈值，再看下面任一）：</span><br><span class="line">    ├─ 工具调用数 &gt;= 阈值           → 触发</span><br><span class="line">    └─ 上一轮无工具调用（自然断点） → 触发</span><br><span class="line"></span><br><span class="line">  触发 → fork subAgent 读 session 文件 → 提取关键信息 → 写回</span><br></pre></td></tr></table></figure><p><strong>⑤ subAgent spawn 时</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">loadAgentMemoryPrompt(agentType, scope)（同步，在 getSystemPrompt 回调里）</span><br><span class="line">  └─ 读 &lt;agentMemoryDir&gt;/MEMORY.md + 目录内所有文件</span><br><span class="line">     注入系统 prompt，spawn 后固定不变</span><br><span class="line"></span><br><span class="line">Agent Memory 写（subAgent 自主，同 Auto Memory 写法）</span><br></pre></td></tr></table></figure><h3 id="四类文件格式"><a href="#四类文件格式" class="headerlink" title="四类文件格式"></a>四类文件格式</h3><p>Auto Memory 和 Agent Memory 均使用相同格式。两步保存：① 写 <code>.md</code> 文件 ② 在 <code>MEMORY.md</code> 加索引；MEMORY.md 超 200 行截断，每条约 150 字符。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><span class="line"># MEMORY.md（索引，始终加载）</span><br><span class="line">- [用户背景](user_background.md) — 后端出身，React 新手</span><br><span class="line">- [回复风格](feedback_response.md) — 不加末尾总结</span><br><span class="line">- [合并冻结](project_freeze.md) — 冻结至 2026-04-10</span><br><span class="line">- [Bug 追踪](reference_bugs.md) — Linear INGEST 项目</span><br><span class="line"></span><br><span class="line"># user_background.md</span><br><span class="line">---</span><br><span class="line">name: 用户技术背景</span><br><span class="line">description: 用户后端出身，React 新手</span><br><span class="line">type: user</span><br><span class="line">---</span><br><span class="line">深度 Go 经验，首次接触 React。解释前端概念时用后端类比。</span><br><span class="line"></span><br><span class="line"># feedback_response.md</span><br><span class="line">---</span><br><span class="line">name: 回复风格</span><br><span class="line">description: 不在回复末尾加总结</span><br><span class="line">type: feedback</span><br><span class="line">---</span><br><span class="line">不加总结。Why: 用户说&quot;我能看懂 diff&quot;。How to apply: 任何回复均不加总结段落。</span><br><span class="line"></span><br><span class="line"># project_freeze.md</span><br><span class="line">---</span><br><span class="line">name: 合并冻结</span><br><span class="line">description: 冻结非关键合并至 2026-04-10</span><br><span class="line">type: project</span><br><span class="line">---</span><br><span class="line">冻结至 2026-04-10。Why: 移动端切 release 分支。How to apply: 建议合并 PR 时先确认是否关键。</span><br><span class="line"></span><br><span class="line"># reference_bugs.md</span><br><span class="line">---</span><br><span class="line">name: Bug 追踪位置</span><br><span class="line">description: pipeline bug 在 Linear INGEST 项目追踪</span><br><span class="line">type: reference</span><br><span class="line">---</span><br><span class="line">Linear 项目 &quot;INGEST&quot; 追踪所有 pipeline bug。</span><br></pre></td></tr></table></figure><p>使用前验证：memory 里提到的文件/函数可能已不存在，先 grep/check 再引用。</p><h3 id="Snapshot-机制"><a href="#Snapshot-机制" class="headerlink" title="Snapshot 机制"></a>Snapshot 机制</h3><p>团队协作用途：将预置的 Agent 记忆提交到 git，新成员 clone 后 spawn 该类 Agent 时自动初始化，无需手动配置。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"># 团队在 git 里提交预置记忆</span><br><span class="line">.claude/agent-memory-snapshots/&lt;agentType&gt;/snapshot.json  ← 版本戳</span><br><span class="line">.claude/agent-memory-snapshots/&lt;agentType&gt;/*.md           ← 预置记忆文件</span><br><span class="line"></span><br><span class="line"># Agent spawn 时 checkAgentMemorySnapshot()</span><br><span class="line">  ├─ action: &#x27;none&#x27;          本地已是最新，跳过</span><br><span class="line">  ├─ action: &#x27;initialize&#x27;    本地无记忆 + snapshot 存在 → 覆盖复制到本地</span><br><span class="line">  └─ action: &#x27;prompt-update&#x27; snapshot 比本地新 → 仅提示，不自动覆盖</span><br><span class="line"></span><br><span class="line"># 本地同步标记（不提交 git）</span><br><span class="line">.snapshot-synced.json  →  &#123; &quot;syncedFrom&quot;: &quot;2026-04-01T10:00:00Z&quot; &#125;</span><br></pre></td></tr></table></figure><p><code>replaceFromSnapshot</code> 是覆盖不是合并——先删光本地所有 <code>.md</code> 再复制，所以 <code>prompt-update</code> 不自动执行，避免覆盖成员自己积累的记忆。</p><hr><h2 id="Ch-13-—-上下文管理"><a href="#Ch-13-—-上下文管理" class="headerlink" title="Ch 13 — 上下文管理"></a>Ch 13 — 上下文管理</h2><p>每次调用 LLM，Claude Code 把上下文分成三个区域组装进请求——system 字段、首条特殊消息、对话历史——不同类型的上下文注入位置不同。</p><h3 id="完整请求结构"><a href="#完整请求结构" class="headerlink" title="完整请求结构"></a>完整请求结构</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line">── API Request ──────────────────────────────────</span><br><span class="line"></span><br><span class="line">  [system]</span><br><span class="line">  ① attribution header       版本指纹，x-anthropic-billing-header 开头</span><br><span class="line">  ② CLI sysprompt prefix     身份声明，三选一：</span><br><span class="line">                               交互模式            → &quot;You are Claude Code…&quot;</span><br><span class="line">                               非交互 + 有外部 SP  → &quot;…running within Agent SDK&quot;</span><br><span class="line">                               非交互 + 无外部 SP  → &quot;You are a Claude agent…&quot;</span><br><span class="line">  ③ ...systemPrompt          调用方传入的主体（已含 systemContext）</span><br><span class="line">  ④ ADVISOR_INSTRUCTIONS     按需追加（advisor 模式）</span><br><span class="line">  ⑤ CHROME_INSTRUCTIONS      按需追加（tool search + Chrome 工具）</span><br><span class="line"></span><br><span class="line">  [messages[0]]  仅含特殊消息，prependUserContext() 注入</span><br><span class="line">  ⑥ userContext，&lt;system-reminder&gt; 包裹</span><br><span class="line">     ├─ claudeMd    所有层级 CLAUDE.md 拼合</span><br><span class="line">     └─ currentDate 今天的日期</span><br><span class="line"></span><br><span class="line">  [messages[1..N]]  对话历史 （1 = 最新一条）</span><br><span class="line">  ⑦ 历史消息（user/assistant 轮次）</span><br><span class="line">  ⑧ 工具结果（tool_result block）</span><br><span class="line">  ⑨ deferred tools 列表（可选，tool search 模式）</span><br><span class="line"></span><br><span class="line">─────────────────────────────────────────────────</span><br><span class="line"></span><br><span class="line">上面提到的systemContext 和 userContext：</span><br><span class="line"></span><br><span class="line">systemContext → appendSystemContext() → system 字段末尾</span><br><span class="line">  └─ gitStatus   当前分支、主分支、git user、文件状态、最近5条 commit（会话开始快照，全程不变）</span><br><span class="line"></span><br><span class="line">userContext → prependUserContext() → messages[0] &lt;system-reminder&gt;</span><br><span class="line">  ├─ claudeMd    所有层级 CLAUDE.md 拼合</span><br><span class="line">  └─ currentDate &quot;Today&#x27;s date is 2026-04-05.&quot;</span><br></pre></td></tr></table></figure><h3 id="CLAUDE-md-加载机制"><a href="#CLAUDE-md-加载机制" class="headerlink" title="CLAUDE.md 加载机制"></a>CLAUDE.md 加载机制</h3><p><code>getMemoryFiles()</code> 按优先级从低到高依次加载，越靠后加载的文件 LLM 越关注：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">① Managed   /etc/claude-code/CLAUDE.md + .claude/rules/*.md   企业统一策略</span><br><span class="line">② User      ~/.claude/CLAUDE.md + ~/.claude/rules/*.md         用户全局配置</span><br><span class="line">③ Project   CLAUDE.md / .claude/CLAUDE.md + .claude/rules/*.md 项目规范，检入 git</span><br><span class="line">④ Local     CLAUDE.local.md                                     个人项目配置，不检入 git</span><br><span class="line"></span><br><span class="line">Project / Local 从 git root 向 CWD 逐层发现，CWD 更近的优先级更高</span><br></pre></td></tr></table></figure><blockquote><p>这里的 User / Project / Local 和 Ch 12 Agent Memory 的 scope 是同一套作用域模型：User 全局、Project 团队共享、Local 个人私有。</p><p><strong>区别</strong>是 CLAUDE.md 是开发者预先写好的静态指令，框架没有任何机制引导 LLM 自动写入（对比 Auto Memory 有完整的自动写入路径）。</p></blockquote><p><strong><code>@include</code> 指令</strong>：文件内用 <code>@path</code> 引用其他文件，被引用文件插入到引用文件之前。代码块内无效，循环引用有保护，支持相对/绝对/<code>~/</code> 路径。</p><p><code>getClaudeMds()</code> 把所有文件拼合，格式为 <code>Contents of &lt;path&gt; (&lt;类型描述&gt;):\n\n&lt;内容&gt;</code>，整体注入 <code>userContext.claudeMd</code>。</p><h3 id="工具权限上下文"><a href="#工具权限上下文" class="headerlink" title="工具权限上下文"></a>工具权限上下文</h3><p><code>ToolPermissionContext</code> 挂在 <code>AppState</code> 上，把所有来源的权限配置汇总成一个快照，工具调用前统一查这里。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">ToolPermissionContext</span><br><span class="line">  ├─ mode              全局权限模式</span><br><span class="line">  │    ├─ default          敏感操作弹窗询问</span><br><span class="line">  │    ├─ acceptEdits       自动接受文件编辑</span><br><span class="line">  │    ├─ bypassPermissions 跳过所有检查（--dangerously-skip-permissions）</span><br><span class="line">  │    ├─ dontAsk           自动允许，不弹窗</span><br><span class="line">  │    ├─ plan              只读，/plan 命令进入，退出时从 prePlanMode 恢复</span><br><span class="line">  │    └─ bubble            subAgent 向上冒泡权限请求</span><br><span class="line">  │</span><br><span class="line">  ├─ alwaysAllowRules   &quot;永远允许&quot;规则集，按来源分桶存储</span><br><span class="line">  ├─ alwaysDenyRules    &quot;永远拒绝&quot;规则集，同结构</span><br><span class="line">  ├─ alwaysAskRules     &quot;强制询问&quot;规则集，同结构</span><br><span class="line">  │</span><br><span class="line">  │    结构：&#123; [source]: string[] &#125;，例：</span><br><span class="line">  │    alwaysAllowRules = &#123;</span><br><span class="line">  │      session:         [&quot;Bash(git *)&quot;],   ← 对话中点&quot;永远允许&quot;</span><br><span class="line">  │      projectSettings: [&quot;Write(src/*)&quot;],  ← .claude/settings.json</span><br><span class="line">  │    &#125;</span><br><span class="line">  │    source: userSettings / projectSettings / localSettings /</span><br><span class="line">  │            policySettings / flagSettings / cliArg / command / session</span><br><span class="line">  │</span><br><span class="line">  ├─ additionalWorkingDirectories  允许操作的额外目录</span><br><span class="line">  └─ shouldAvoidPermissionPrompts  true 时自动拒绝弹窗（后台 Agent 用）</span><br></pre></td></tr></table></figure><p>对话中点「永远允许」→ 往 <code>alwaysAllowRules.session</code> 追加；<code>/plan</code> 进入只读模式，原 mode 存入 <code>prePlanMode</code> 退出时恢复。</p><h3 id="subAgent-的上下文裁剪与隔离"><a href="#subAgent-的上下文裁剪与隔离" class="headerlink" title="subAgent 的上下文裁剪与隔离"></a>subAgent 的上下文裁剪与隔离</h3><p>AgentTool spawn subAgent 时，对上下文做一系列裁剪和隔离：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line">父 Agent 上下文</span><br><span class="line">↓</span><br><span class="line">① userContext 处理</span><br><span class="line">   ├─ 只读标志位omitClaudeMd=true 的 Agent（当前只有 Explore / Plan）</span><br><span class="line">   │    └─ 去掉 claudeMd，省 ~5-15 Gtok/周（34M+ 次 spawn）</span><br><span class="line">   └─ 其他 Agent → 完整传递</span><br><span class="line"></span><br><span class="line">② systemContext 处理</span><br><span class="line">   ├─ Explore / Plan 类型的 Agent</span><br><span class="line">   │    └─ 去掉 gitStatus（过期快照，需要时自己跑 git status）</span><br><span class="line">   └─ 其他 Agent → 完整传递</span><br><span class="line"></span><br><span class="line">③ 权限模式隔离</span><br><span class="line">   ├─ Agent 定义了专属权限模式？</span><br><span class="line">   │    ├─ 父级是 bypassPermissions / acceptEdits → 父级优先，不覆盖</span><br><span class="line">   │    └─ 其他 → 用 Agent 定义的模式覆盖</span><br><span class="line">   └─ 后台异步 Agent → 自动禁止弹权限窗（无 UI）</span><br><span class="line">      后台异步但可弹窗 → 等自动检查完再弹（避免无谓打扰用户）</span><br><span class="line"></span><br><span class="line">④ allowedTools 范围限制（有传入时）</span><br><span class="line">   └─ 清掉父 Agent 会话级规则，换成传入的工具白名单</span><br><span class="line">      保留 cliArg 规则（SDK 显式授权，应对所有subAgent 生效）</span><br><span class="line"></span><br><span class="line">⑤ AbortController</span><br><span class="line">   ├─ 同步 Agent → 共享父 AbortController（父取消子也取消）</span><br><span class="line">   └─ 异步 Agent → 独立 AbortController（独立运行）</span><br></pre></td></tr></table></figure><hr><h2 id="Ch-14-—-Hooks-扩展"><a href="#Ch-14-—-Hooks-扩展" class="headerlink" title="Ch 14 — Hooks 扩展"></a>Ch 14 — Hooks 扩展</h2><p><strong>Hook 是什么</strong>：在约定时机启动子进程，主流程把上下文序列化成 JSON 传入，读取子进程输出决定是否继续——本质是”可执行文件 + stdin/stdout 协议”的旁路回调机制。CC 扩展了一种 <strong>prompt hook</strong>：触发时机相同，但执行体换成自然语言条件，<code>$ARGUMENTS</code> 替换为工具调用 JSON 后整体作为 UserMessage 发给 Haiku，返回 <code>&#123;ok: true/false&#125;</code> 决定放行或阻断。</p><p><strong>来源</strong>：settings.json（用户配置）/ Agent·Skill frontmatter（subAgent 启动时注册）/ FunctionHook（CC 内部 TS 回调，不走 shell，session 级临时）</p><p><strong>执行器</strong>：command（子进程）/ prompt（Haiku）/ agent（subAgent）/ http（外部服务）</p><p><strong>事件 × 返回值</strong>：</p><p>事件挂在工具调用前后（PreToolUse / PostToolUse / PostToolUseFailure）、会话生命周期（SessionStart / Stop）、subAgent 生命周期（SubagentStart / SubagentStop）、权限（PermissionRequest / PermissionDenied）、文件变更、MCP 交互等节点。</p><p>返回 JSON 控制主流程：<code>&#123;decision:&quot;block&quot;&#125;</code> 阻断工具 / <code>&#123;continue:false&#125;</code> 终止 Agent / <code>&#123;async:true&#125;</code> 不阻塞后台跑 / hookSpecificOutput 可修改入参或 MCP 输出。**<code>approve</code> 不能绕过 settings.json deny 规则。**</p><p><strong>与MAS 交叉部分</strong>：subAgent 的 <code>Stop</code> frontmatter hook 自动转为 <code>SubagentStop</code>，可在subAgent 结束后做验证。</p><hr><h2 id="Ch-15-—-沙盒与权限系统"><a href="#Ch-15-—-沙盒与权限系统" class="headerlink" title="Ch 15 — 沙盒与权限系统"></a>Ch 15 — 沙盒与权限系统</h2><h3 id="Agent-为什么比普通程序更需要沙盒"><a href="#Agent-为什么比普通程序更需要沙盒" class="headerlink" title="Agent 为什么比普通程序更需要沙盒"></a>Agent 为什么比普通程序更需要沙盒</h3><p>普通程序攻击面固定可静态分析；Agent 多了一条链路：<strong>提示注入 → LLM 行为改变 → 工具产生真实副作用</strong>。读一个恶意文件就能触发 <code>rm -rf</code>，这在普通程序里不存在。</p><p>三个根源：LLM 输出不可预测 / 工具有真实副作用 / 任意外部内容都能变成”指令”。</p><p>权限系统可被说服绕过，沙盒是不可绕过的 OS 级硬边界。</p><h3 id="沙盒的两个核心维度与安全兜底"><a href="#沙盒的两个核心维度与安全兜底" class="headerlink" title="沙盒的两个核心维度与安全兜底"></a>沙盒的两个核心维度与安全兜底</h3><p>Agent 能造成破坏的只有两条路：写坏文件 或 发出网络请求。</p><p><strong>文件系统</strong>：进程启动时重新挂载，<code>allowWrite</code> / <code>denyWrite</code> / <code>denyRead</code> / <code>allowRead</code> 四向控制，deny 永远优先。</p><p><strong>网络</strong>：默认拒绝所有出站，<code>allowedDomains</code> + <code>WebFetch(domain:x)</code> 权限规则合并成白名单。</p><p>CC 只做规则收集，实际执行由 <code>@anthropic-ai/sandbox-runtime</code> 负责。</p><p><strong>安全兜底：</strong></p><ul><li><strong>硬编码 denyWrite</strong>：settings.json / <code>.claude/skills/</code> / bare git repo 特征文件写死只读，防止 Agent 改权限配置或植入恶意 skill</li><li><strong>命令后清理</strong>：<code>core.fsmonitor</code> 是 git 的合法 hook，git 运行时自动执行并继承 CC 权限，可被提示注入利用。对策：启动时记录 cwd 中不存在的特征文件，沙盒命令结束后立刻删除新增的，在 unsandboxed git 运行前关掉时间窗口</li><li><strong>failIfUnavailable</strong>：沙盒启不来默认静默降级——安全系统失败要吵闹不能静默</li><li><strong>autoAllowBashIfSandboxed</strong>：沙盒启用时跳过 Bash 权限弹窗，OS 已在执行，再弹窗是多余摩擦</li></ul><h3 id="权限系统-vs-沙盒：两层防御"><a href="#权限系统-vs-沙盒：两层防御" class="headerlink" title="权限系统 vs 沙盒：两层防御"></a>权限系统 vs 沙盒：两层防御</h3><p><strong>沙盒的 OS 实现</strong>：限制进程系统调用的内核机制。macOS 用 <code>sandbox-exec</code>，Linux 用 <code>bwrap + socat</code>，Windows 不支持。CC 底层交给闭源的 <code>@anthropic-ai/sandbox-runtime</code>，adapter 只做配置转换。</p><p><strong>两层的本质区别</strong>：权限系统是 CC 应用层软拦截（可被社会工程学绕过——LLM 说服用户在弹窗点允许）；沙盒是 OS 层硬拦截，启动后固化在内核，运行时无法修改。</p><p><strong>权限系统的执行形态</strong>：工具调用触发时 CC 弹出 3 选 1 交互：<code>Yes</code>（放行一次）/ <code>Yes, during this session</code>（本 session 内全部放行）/ <code>No</code>（拒绝）。用户确认后产生一条 allow 规则，存入五个 destination 之一：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">├─ session         → 纯内存，session 结束消失</span><br><span class="line">├─ localSettings   → 写入 .claude/settings.local.json（gitignored）</span><br><span class="line">├─ projectSettings → 写入项目 .claude/settings.json</span><br><span class="line">├─ userSettings    → 写入 ~/.claude/settings.json（全局）</span><br><span class="line">└─ cliArg          → 来自命令行参数，只读</span><br></pre></td></tr></table></figure><p><strong>两层联动</strong>：权限规则自动同步进沙盒——<code>Edit(/src/**)</code> → <code>allowWrite</code>，<code>WebFetch(domain:x)</code> → 网络白名单。一套规则，两层执行。</p><p><strong>默认关闭</strong>（<code>sandbox.enabled ?? false</code>）：开启需在 settings.json 加 <code>&quot;sandbox&quot;: &#123;&quot;enabled&quot;: true&#125;</code>，仅支持 macOS / Linux / WSL2。</p><h3 id="自建-MAS-沙盒的实现路径"><a href="#自建-MAS-沙盒的实现路径" class="headerlink" title="自建 MAS 沙盒的实现路径"></a>自建 MAS 沙盒的实现路径</h3><p><strong>CC 用的方案</strong>：Linux/WSL2 用 bubblewrap（bwrap）+ socat，macOS 用 sandbox-exec，Windows 不支持。</p><p><strong>MAS 特有的问题：沙盒粒度</strong></p><p>单 Agent 沙盒粒度清晰（一个进程一个沙盒），MAS 里需要额外决定：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">├─ 每个subAgent 独立沙盒</span><br><span class="line">│  优点：隔离彻底，一个 Agent 被攻击不影响其他</span><br><span class="line">│  缺点：Agent 间共享文件需要跨沙盒通信，复杂度高</span><br><span class="line">│</span><br><span class="line">└─ 共享沙盒 + 应用层权限分层</span><br><span class="line">   优点：Agent 间通信简单</span><br><span class="line">   缺点：一个 Agent 逃逸可能影响整个沙盒</span><br></pre></td></tr></table></figure><p><strong>CC 的选择</strong>：共享沙盒。subAgent 运行在父 Agent 的沙盒内部（<code>enableWeakerNestedSandbox</code> 配置项印证嵌套沙盒存在），AgentTool 启动subAgent 是同进程内调用，不创建新沙盒实例。subAgent 的隔离靠应用层权限上下文，不是 OS 层。</p><hr><h2 id="Ch-16-—-MAS-转向-Harness-Engineering"><a href="#Ch-16-—-MAS-转向-Harness-Engineering" class="headerlink" title="Ch 16 — MAS 转向 Harness Engineering"></a>Ch 16 — MAS 转向 Harness Engineering</h2><p>CC 用 <code>bun:bundle</code> 的 <code>feature()</code> 在构建时判断 flag，为 false 的代码直接从 bundle 里删掉。<strong>对外发布版根本不含这些代码</strong>，不是运行时 if/else。以下特性均默认关闭，仅内部版本可用。</p><h3 id="Agent-分工的三个维度"><a href="#Agent-分工的三个维度" class="headerlink" title="Agent 分工的三个维度"></a>Agent 分工的三个维度</h3><p><strong>协调模式（多 Agent 如何分工）</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">├─ 单 Agent 顺序执行   → 默认 REPL</span><br><span class="line">├─ 主从委托            → AgentTool 基础用法</span><br><span class="line">├─ Coordinator/Worker  → COORDINATOR_MODE（默认关闭）</span><br><span class="line">└─ 对等并行            → Swarm tmux（默认关闭）</span><br></pre></td></tr></table></figure><p><strong>执行机制（Agent 怎么跑）</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">├─ 同进程同步   → AgentTool 默认</span><br><span class="line">├─ 同进程异步   → AgentTool async 模式</span><br><span class="line">└─ 独立进程     → Swarm tmux</span><br></pre></td></tr></table></figure><p><strong>Agent 特化（角色是什么）</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">├─ 通用          → general-purpose</span><br><span class="line">├─ 探索专用      → explore（feature-gated）</span><br><span class="line">├─ 规划专用      → plan（feature-gated）</span><br><span class="line">├─ 验证专用      → verification（feature-gated）</span><br><span class="line">├─ 调度者        → Coordinator（COORDINATOR_MODE）</span><br><span class="line">├─ 执行者        → Worker（COORDINATOR_MODE）</span><br><span class="line">└─ 自定义        → .claude/agents/ 目录加载</span><br></pre></td></tr></table></figure><h3 id="Coordinator-Worker-详解"><a href="#Coordinator-Worker-详解" class="headerlink" title="Coordinator / Worker 详解"></a>Coordinator / Worker 详解</h3><p><strong>是什么</strong>：把”思考调度”和”动手执行”拆给两个角色，防止同一 Agent 上下文被执行细节淹没。</p><p><strong>能力边界是物理强制的，不靠 prompt 约定</strong>：</p><ul><li>Coordinator：无执行工具（无 Bash/Edit/Read），只有 AgentTool / SendMessageTool / TaskStopTool，物理上无法直接操作</li><li>Worker：无全局对话上下文，只收到一个自包含任务，无法影响整体方向</li></ul><p><strong>实现方式</strong>：同一二进制，<code>CLAUDE_CODE_COORDINATOR_MODE=1</code> 切换角色，注入不同 system prompt + 工具集。</p><p><strong>执行流程</strong>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">用户 → Coordinator 拆解任务，并行派 Worker</span><br><span class="line">Worker 异步执行，结果以 &lt;task-notification&gt; XML 注入为&quot;用户消息&quot;</span><br><span class="line">Coordinator 读取、自己综合（不能转包理解），决定 Continue 或 Spawn 新 Worker</span><br><span class="line">所有任务完成 → Coordinator 汇总告知用户</span><br></pre></td></tr></table></figure><p><strong>Continue vs Spawn</strong>：本质是”Worker 的上下文值不值得复用”</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">探索文件 = 要修改文件     →  Continue</span><br><span class="line">探索范围广但实现窄        →  Spawn（避免噪音）</span><br><span class="line">验证另一个 Worker 的代码  →  Spawn（需要全新视角）</span><br><span class="line">失败重试                  →  Continue（错误上下文有用）</span><br></pre></td></tr></table></figure><p><strong>并发规则</strong>：读操作随意并行 / 写操作同组文件同时只跑一个 Worker / 验证可与不同文件区域的实现并行。</p><p><strong>MAS 启示</strong>：调度者不执行是约束不是建议；Worker 结果异步事件驱动不是同步调用；综合是 Coordinator 唯一不可外包的职责；上下文是资源，Continue/Spawn 是复用决策。</p><h3 id="Swarm：tmux-多进程并行"><a href="#Swarm：tmux-多进程并行" class="headerlink" title="Swarm：tmux 多进程并行"></a>Swarm：tmux 多进程并行</h3><p>我认为这是Harness Engineering的方向。</p><p>创建专属 tmux session（<code>claude-swarm</code>），每个 Agent 对应一个 tmux pane，<code>tmux split-window</code> 切分，每个 pane 是独立 Claude 进程。pane 颜色不同便于区分，创建时有互斥锁防并发竞争，shell 初始化后等 200ms 再启动 Agent。Agent 间通过 SendMessageTool 通信。</p><h3 id="KAIROS：事件驱动唤醒"><a href="#KAIROS：事件驱动唤醒" class="headerlink" title="KAIROS：事件驱动唤醒"></a>KAIROS：事件驱动唤醒</h3><p>Agent 不被用户主动触发，而是订阅事件后自动唤醒。CC 里两种实现：</p><ul><li><strong>PR 事件</strong>：Coordinator 调用 <code>subscribe_pr_activity</code>，GitHub PR 评论 / CI 结果以”用户消息”形式注入对话</li><li><strong>时间门控</strong>（autoDream）：满足”距上次执行超过 N 小时 + 积累 N 个 session”自动触发后台记忆整合</li></ul><hr><h2 id="Ch-17-—-MCP-集成"><a href="#Ch-17-—-MCP-集成" class="headerlink" title="Ch 17 — MCP 集成"></a>Ch 17 — MCP 集成</h2><p><strong>MCP（Model Context Protocol）</strong> 是 Anthropic 主导的开放协议，规范 LLM 与外部工具/数据源之间的通信格式。CC 是纯 MCP Client，不托管 Server。</p><h3 id="传输层"><a href="#传输层" class="headerlink" title="传输层"></a>传输层</h3><p>6 种传输类型，按使用场景分：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">├─ stdio      本地进程，CC spawn 常驻子进程，管道通信，90% 场景</span><br><span class="line">├─ sse        远程 HTTP，Server-Sent Events 单向推送</span><br><span class="line">├─ sse-ide    IDE 插件专用 SSE 变体</span><br><span class="line">├─ http       StreamableHTTP，新标准，支持双向流</span><br><span class="line">├─ ws         WebSocket，双向实时</span><br><span class="line">└─ sdk        进程内直接引用，测试/嵌入</span><br></pre></td></tr></table></figure><p>stdio 本质：CC 用 <code>child_process</code> spawn 子进程（如 <code>npx some-mcp-server</code>），往其 stdin 写 JSON 请求，从 stdout 读响应，会话结束才关闭——常驻进程，不是跑完退出的命令。</p><h3 id="通信格式"><a href="#通信格式" class="headerlink" title="通信格式"></a>通信格式</h3><p>底层协议 <strong>JSON-RPC 2.0</strong>（所有传输层通用）。工具命名规则 <code>mcp__&#123;serverName&#125;__&#123;toolName&#125;</code>（双下划线），server name 经 <code>normalizeNameForMCP()</code> 净化（非 <code>[a-zA-Z0-9_-]</code> 字符转 <code>_</code>）。工具描述上限 2048 字符，超出截断（OpenAPI 生成的 server 常有 60KB 描述）。</p><p>调用示例（CC → github server 的 list_repos）：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">请求 CC → stdin:   &#123; &quot;jsonrpc&quot;:&quot;2.0&quot;, &quot;id&quot;:1, &quot;method&quot;:&quot;tools/call&quot;,</span><br><span class="line">                     &quot;params&quot;: &#123; &quot;name&quot;:&quot;list_repos&quot;, &quot;arguments&quot;:&#123;&quot;org&quot;:&quot;anthropics&quot;&#125; &#125; &#125;</span><br><span class="line">响应 stdout → CC:  &#123; &quot;jsonrpc&quot;:&quot;2.0&quot;, &quot;id&quot;:1,</span><br><span class="line">                     &quot;result&quot;: &#123; &quot;content&quot;:[&#123;&quot;type&quot;:&quot;text&quot;,&quot;text&quot;:&quot;[...]&quot;&#125;], &quot;isError&quot;:false &#125; &#125;</span><br></pre></td></tr></table></figure><p>CC 内部用 <code>mcp__github__list_repos</code> 路由，但下发给 server 时用原始名 <code>list_repos</code>，LLM 对来源无感知。</p><h3 id="工具注册与合并"><a href="#工具注册与合并" class="headerlink" title="工具注册与合并"></a>工具注册与合并</h3><p>启动时 CC 并发连接所有配置 server，各 server 执行 <code>ListTools</code> 后封装为 <code>MCPTool</code> 注入工具池，与内置 <code>Bash</code>/<code>Write</code> 同级，<strong>LLM 感知不到来源差异</strong>。</p><p>配置来源（ConfigScope 6 个）：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">├─ local       项目本地（不提交 git）</span><br><span class="line">├─ project     项目共享（提交 git，团队共用）</span><br><span class="line">├─ user        用户全局</span><br><span class="line">├─ enterprise  企业下发</span><br><span class="line">├─ claudeai    claude.ai 云端</span><br><span class="line">└─ managed     程序动态注入</span><br></pre></td></tr></table></figure><p>连接状态：<code>pending → connected / failed / needs-auth / disabled</code>，各 server 独立，互不影响。</p><h3 id="权限与认证"><a href="#权限与认证" class="headerlink" title="权限与认证"></a>权限与认证</h3><p><strong>Channel 权限</strong>（Telegram/iMessage/Discord）：CC 触发权限对话框时同步向 channel server 推送审批请求，人类在手机上回复 <code>yes xxxxx</code> / <code>no xxxxx</code>，server 解析后发结构化事件 <code>notifications/claude/channel/permission</code>，CC 收到后完成审批。批准方是人类，CC 无法自批准。若 channel server 被攻破，攻击者可伪造审批——已知可接受风险（被攻破的 channel 本就能无限注入对话，自批准只是更快，能力上限不变）。</p><p><strong>OAuth / XAA</strong>：OAuth 用于远程 server 认证，CC 触发浏览器授权流，token 存 keychain，过期自动刷新。XAA（Cross-App Access）用于 claude.ai 托管 server，基于 Claude 账号做跨应用授权，不走标准 OAuth 流。</p><hr><h2 id="Ch-18-—-总结：Claude-Code-架构全景"><a href="#Ch-18-—-总结：Claude-Code-架构全景" class="headerlink" title="Ch 18 — 总结：Claude Code 架构全景"></a>Ch 18 — 总结：Claude Code 架构全景</h2><p><strong>1. MAS 调度</strong><br>CC 支持四种协调模式：单 Agent / 主从委托 / Coordinator-Worker / Swarm 对等并行。Coordinator 模式通过物理约束分离调度与执行——Coordinator 无执行工具，Worker 无全局上下文，分工靠工具集而非 prompt 约定。Swarm 用 tmux 实现真正的独立进程并行，是目前最重的多 Agent 方案。</p><p><strong>2. Agent Loop</strong><br>核心是 <code>query.ts</code> 的 <code>while(true)</code> ReAct 循环，每轮调一次 LLM，有工具调用就执行再循环，无工具则结束。所有扩展能力（Hooks、Thinking、Compact、工具调度）都叠加在这个循环上，不替代它。退出条件有 10 种，覆盖正常结束、用户中断、token 超限等全部场景。</p><p><strong>3. 上下文与 Compact</strong><br>CC 把上下文视为有限资源，设计了四层压缩策略：Microcompaction（每轮必跑，移除图片缩小结果）/ Session Memory（直接存记忆，不调 LLM）/ Traditional（fork subAgent 做全量摘要）/ Reactive（LLM 报错时实时触发）。四种策略按成本从低到高依次尝试，压缩失败才升级。</p><p><strong>4. 持久化 Memory</strong><br>记忆系统分两层：CLAUDE.md 是项目级静态知识，每次请求注入 system prompt；auto memory 是跨会话动态积累的用户/项目偏好，存在 <code>~/.claude/projects/</code> 目录下，按 MEMORY.md 索引。两者都是纯文件，LLM 通过读写文件工具维护，没有向量数据库。</p><p><strong>6. Task 系统</strong><br>Task 系统是多 Agent 协作的状态共享基础设施，用 JSON 文件 + 文件锁实现跨进程任务状态同步。父 Agent 通过 TaskCreate 创建任务，subAgent 通过 TaskUpdate 更新进度，父 Agent 轮询感知完成情况。文件锁保证并发写入安全，是轻量级的进程间通信方案。</p><p><strong>7. Tool 系统</strong><br>工具是 CC 的能力单元，每个工具自包含（定义、schema、权限检查、执行、UI 渲染）。调度层支持两种模式：默认 toolOrchestration（safe 工具并发批处理，unsafe 串行，上限 10）和实验性 StreamingToolExecutor（流式中即启动，无并发上限，支持级联取消）。MCP 工具通过标准协议动态接入，与内置工具统一调度。</p><p><strong>8. Skill 系统</strong><br>Skill 是可复用的提示词工作流，以 Markdown 文件形式存储，frontmatter 定义元数据。用户通过斜杠命令触发，CC 读取文件内容展开为完整 prompt 注入对话。Skill 可以定义自己的 Hooks，在subAgent 启动时自动注册，实现工作流级别的生命周期控制。</p><p><strong>9. MCP 系统</strong><br>MCP（Model Context Protocol）是 CC 的工具扩展协议，允许外部服务以标准化方式向 CC 注册工具。CC 作为 MCP 客户端，在启动时连接配置的 MCP 服务器，动态获取工具列表并合并进工具池。MCP 工具与内置工具的权限系统、Hooks、调度机制完全统一。</p><p><strong>10. 权限系统与沙盒</strong><br>权限系统是应用层软拦截，工具调用前检查 settings.json 规则，不符合则弹窗询问用户，决策结果存入五个 destination（session / localSettings / projectSettings / userSettings / cliArg）。沙盒是 OS 层硬拦截，macOS 用 sandbox-exec，Linux 用 bwrap，两者通过规则自动同步联动——权限规则驱动沙盒配置，一套规则两层执行。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;Claude-Code-源码分析&quot;&gt;&lt;a href=&quot;#Claude-Code-源码分析&quot; class=&quot;headerlink&quot; title=&quot;Claude Code 源码分析&quot;&gt;&lt;/a&gt;Claude Code 源码分析&lt;/h1&gt;&lt;p&gt;对照 nanobot 逆向理</summary>
      
    
    
    
    <category term="AI" scheme="https://codingcodingk.github.io/categories/AI/"/>
    
    <category term="Agent" scheme="https://codingcodingk.github.io/categories/AI/Agent/"/>
    
    
    <category term="AI" scheme="https://codingcodingk.github.io/tags/AI/"/>
    
    <category term="AI Agent" scheme="https://codingcodingk.github.io/tags/AI-Agent/"/>
    
  </entry>
  
  <entry>
    <title>Hello Agents - 记忆与检索&amp;RAG</title>
    <link href="https://codingcodingk.github.io/Tech/AI/claw/nanobot_memo/"/>
    <id>https://codingcodingk.github.io/Tech/AI/claw/nanobot_memo/</id>
    <published>2026-04-05T11:17:42.935Z</published>
    <updated>2026-04-05T11:18:27.219Z</updated>
    
    <content type="html"><![CDATA[<h1 id="nanobot-技术要点"><a href="#nanobot-技术要点" class="headerlink" title="nanobot 技术要点"></a>nanobot 技术要点</h1><h2 id="1-适配器模式实现多-LLM-统一响应"><a href="#1-适配器模式实现多-LLM-统一响应" class="headerlink" title="1. 适配器模式实现多 LLM 统一响应"></a>1. 适配器模式实现多 LLM 统一响应</h2><p><strong>核心思想</strong>：不同 LLM 提供商的 API 返回格式各异，通过适配器模式统一转换为 <code>LLMResponse</code> 格式。</p><p><strong>实现方式</strong>：</p><ul><li>每个 Provider 实现 <code>_parse_response()</code> 方法</li><li>将原始 API 响应转换为统一的 <code>LLMResponse</code> 数据结构</li><li>上层代码（AgentRunner）无需关心底层使用哪个 LLM</li></ul><p><strong>支持的提供商</strong>：</p><ul><li>AnthropicProvider 处理 Anthropic 格式</li><li>OpenAICompatProvider 处理 OpenAI 格式</li><li>DeepSeekProvider 处理 DeepSeek 格式</li></ul><p><strong>统一数据结构</strong>：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@dataclass</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ToolCallRequest</span>:</span></span><br><span class="line">    <span class="string">&quot;&quot;&quot;工具调用请求&quot;&quot;&quot;</span></span><br><span class="line">    <span class="built_in">id</span>: <span class="built_in">str</span>                    <span class="comment"># 工具调用 ID</span></span><br><span class="line">    name: <span class="built_in">str</span>                  <span class="comment"># 工具名称</span></span><br><span class="line">    arguments: <span class="built_in">dict</span>[<span class="built_in">str</span>, <span class="type">Any</span>]  <span class="comment"># 工具参数</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@dataclass</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">LLMResponse</span>:</span></span><br><span class="line">    <span class="string">&quot;&quot;&quot;LLM 统一响应格式&quot;&quot;&quot;</span></span><br><span class="line">    content: <span class="built_in">str</span> | <span class="literal">None</span>                      <span class="comment"># 文本内容</span></span><br><span class="line">    tool_calls: <span class="built_in">list</span>[ToolCallRequest] = []   <span class="comment"># 工具调用列表</span></span><br><span class="line">    finish_reason: <span class="built_in">str</span> = <span class="string">&quot;stop&quot;</span>              <span class="comment"># 停止原因</span></span><br><span class="line">    usage: <span class="built_in">dict</span>[<span class="built_in">str</span>, <span class="built_in">int</span>] = &#123;&#125;               <span class="comment"># Token 使用量</span></span><br><span class="line">    reasoning_content: <span class="built_in">str</span> | <span class="literal">None</span> = <span class="literal">None</span>     <span class="comment"># 推理内容（某些模型）</span></span><br><span class="line">    thinking_blocks: <span class="built_in">list</span>[<span class="built_in">dict</span>] | <span class="literal">None</span> = <span class="literal">None</span> <span class="comment"># 思考块（Anthropic）</span></span><br></pre></td></tr></table></figure><hr><h2 id="2-消息处理全流程（LLM-调用前后）"><a href="#2-消息处理全流程（LLM-调用前后）" class="headerlink" title="2. 消息处理全流程（LLM 调用前后）"></a>2. 消息处理全流程（LLM 调用前后）</h2><p><strong>核心思想</strong>：用户消息到达 LLM 前经过 5 个阶段处理，LLM 回复后保存时还有一轮清洗。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">用户消息到达</span><br><span class="line">  → 记忆压缩（旧消息摘要进 MEMORY.md）</span><br><span class="line">  → 裁剪历史（取合法的近期消息）</span><br><span class="line">  → 拼 system prompt（组装 Agent 人格）</span><br><span class="line">  → 合并当前消息（runtime context + 用户内容）</span><br><span class="line">  → 附加工具定义，调用 LLM</span><br><span class="line">  → 保存清洗（剥掉临时数据，存入 session）</span><br></pre></td></tr></table></figure><h3 id="记忆压缩"><a href="#记忆压缩" class="headerlink" title="记忆压缩"></a>记忆压缩</h3><p>估算当前 prompt token 量，超出预算则循环切割旧消息段交给 LLM 压缩，写入 MEMORY.md / HISTORY.md，指针前移。压缩后这些旧消息不再出现在历史中。详见<a href="#8-%E8%AE%B0%E5%BF%86%E7%B3%BB%E7%BB%9Fmemory-consolidation">第 8 节：记忆系统</a>。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># loop.py</span></span><br><span class="line"><span class="keyword">await</span> self.memory_consolidator.maybe_consolidate_by_tokens(session)</span><br></pre></td></tr></table></figure><h3 id="裁剪历史"><a href="#裁剪历史" class="headerlink" title="裁剪历史"></a>裁剪历史</h3><p>从 session 中取指针之后的消息，跳过开头的非 user 消息和孤儿 tool result，保证消息结构合法。纯数据操作，不涉及 LLM。详见<a href="#11-%E4%BC%9A%E8%AF%9D%E7%AE%A1%E7%90%86session">第 11 节：会话管理</a>。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># loop.py</span></span><br><span class="line">history = session.get_history(max_messages=<span class="number">0</span>)</span><br></pre></td></tr></table></figure><h3 id="拼-system-prompt"><a href="#拼-system-prompt" class="headerlink" title="拼 system prompt"></a>拼 system prompt</h3><p>把 identity、bootstrap 文件（AGENTS/SOUL/USER/TOOLS.md）、MEMORY.md、always skills、skills 目录拼成一段完整的 system prompt。详见<a href="#3-%E4%B8%8A%E4%B8%8B%E6%96%87%E6%9E%84%E5%BB%BAcontextbuilder">第 3 节：上下文构建</a>。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># context.py — build_messages() 内部调用</span></span><br><span class="line">build_system_prompt()  <span class="comment"># identity → bootstrap → memory → skills</span></span><br></pre></td></tr></table></figure><h3 id="合并当前消息"><a href="#合并当前消息" class="headerlink" title="合并当前消息"></a>合并当前消息</h3><p>生成 runtime context（当前时间 + channel + chat_id，标记为元数据非指令），处理用户内容（纯文本或图片 base64 多模态），两者合并为一条 user message，避免连续同角色消息被 provider 拒绝。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># context.py — build_messages() 内部调用</span></span><br><span class="line">_build_runtime_context()  <span class="comment"># 时间 + channel + chat_id</span></span><br><span class="line">_build_user_content()     <span class="comment"># 文本或图片 base64</span></span><br><span class="line"><span class="comment"># 合并后产出最终 messages:</span></span><br><span class="line">[system_prompt, ...history, merged_user_message]</span><br></pre></td></tr></table></figure><h3 id="保存清洗"><a href="#保存清洗" class="headerlink" title="保存清洗"></a>保存清洗</h3><p>LLM 回复后，新消息存入 session 前做清洗：user 消息剥掉 runtime context 前缀（下次会重新生成），tool result 超长内容截断，空 assistant 消息跳过不存，所有消息补 timestamp。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># loop.py</span></span><br><span class="line">self._save_turn(session, all_msgs, skip)</span><br></pre></td></tr></table></figure><h3 id="各阶段增减总览"><a href="#各阶段增减总览" class="headerlink" title="各阶段增减总览"></a>各阶段增减总览</h3><table><thead><tr><th>阶段</th><th>做了什么</th><th>增加了什么</th><th>去掉了什么</th></tr></thead><tbody><tr><td>记忆压缩</td><td>旧消息交给 LLM 提炼</td><td>MEMORY.md 内容变大</td><td>指针前的旧消息从视野消失</td></tr><tr><td>裁剪历史</td><td>取合法的近期消息</td><td>无</td><td>孤儿 tool result、开头非 user 消息</td></tr><tr><td>拼 system prompt</td><td>组装 Agent 人格</td><td>identity + bootstrap（AGENTS/SOUL/USER/TOOLS.md） + memory + skills</td><td>无</td></tr><tr><td>合并当前消息</td><td>拼用户输入</td><td>runtime context、图片 base64</td><td>无</td></tr><tr><td>保存清洗</td><td>存入 session</td><td>timestamp</td><td>runtime context 前缀、空 assistant、超长 tool result</td></tr></tbody></table><hr><h2 id="3-上下文构建（ContextBuilder）"><a href="#3-上下文构建（ContextBuilder）" class="headerlink" title="3. 上下文构建（ContextBuilder）"></a>3. 上下文构建（ContextBuilder）</h2><p><strong>核心思想</strong>：ContextBuilder 把 5 层信息拼成 LLM 的完整输入，层次从稳定到动态。</p><p><strong>拼接信息与来源</strong>：</p><table><thead><tr><th>层次</th><th>来源</th><th>稳定性</th><th>内容</th></tr></thead><tbody><tr><td>identity</td><td>硬编码在 <code>_get_identity()</code></td><td>最稳定，改代码才能变</td><td>Agent 名字、运行时环境、workspace 路径、平台策略、行为指南</td></tr><tr><td>bootstrap 文件</td><td>workspace 下的 AGENTS/SOUL/USER/TOOLS.md</td><td>用户手动编辑</td><td>Agent 人设、性格、用户画像、工具说明</td></tr><tr><td>memory</td><td>workspace/memory/MEMORY.md</td><td>运行时自动积累</td><td>长期事实（偏好、项目背景）</td></tr><tr><td>skills</td><td>workspace/skills/ + nanobot/skills/</td><td>用户可随时增删</td><td>always 技能全文 + 其余技能的 XML 目录</td></tr><tr><td>runtime context</td><td>每次请求动态生成</td><td>每次都不同</td><td>当前时间、channel、chat_id</td></tr></tbody></table><p><strong>拼接流程</strong>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">build_system_prompt()</span><br><span class="line">  → identity                          # 硬编码底层人格</span><br><span class="line">  → _load_bootstrap_files()           # AGENTS/SOUL/USER/TOOLS.md（存在才加载）</span><br><span class="line">  → memory.get_memory_context()       # MEMORY.md 长期事实</span><br><span class="line">  → skills.get_always_skills()        # always=true 的技能全文</span><br><span class="line">  → skills.build_skills_summary()     # 所有技能的 XML 目录</span><br><span class="line">  → 各部分用 --- 分隔，拼成 system prompt</span><br><span class="line"></span><br><span class="line">build_messages()</span><br><span class="line">  → [system_prompt, ...history, merged_user_message]</span><br><span class="line">  → runtime context + 用户内容合并为一条 user message</span><br></pre></td></tr></table></figure><p><strong>最终结构</strong>：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 送给 LLM API 的两个关键参数</span></span><br><span class="line">messages = [</span><br><span class="line">    &#123;<span class="string">&quot;role&quot;</span>: <span class="string">&quot;system&quot;</span>,  <span class="string">&quot;content&quot;</span>: system_prompt&#125;,   <span class="comment"># 5 层拼接结果</span></span><br><span class="line">    *history,                                         <span class="comment"># 裁剪后的近期对话</span></span><br><span class="line">    &#123;<span class="string">&quot;role&quot;</span>: <span class="string">&quot;user&quot;</span>,    <span class="string">&quot;content&quot;</span>: runtime_ctx + 用户消息&#125;,</span><br><span class="line">]</span><br><span class="line">tools = spec.tools.get_definitions()                  <span class="comment"># 工具定义，独立于 messages</span></span><br></pre></td></tr></table></figure><hr><h2 id="4-Tool-参数处理"><a href="#4-Tool-参数处理" class="headerlink" title="4. Tool 参数处理"></a>4. Tool 参数处理</h2><p><strong>核心思想</strong>：LLM 传来的参数可能格式不规范，通过两步处理确保参数正确。</p><p><strong>处理流程</strong>：</p><ol><li><p><strong>cast_params()</strong> - 类型转换（容错）</p><ul><li>将字符串转为正确类型：<code>&quot;10&quot;</code> → <code>10</code>、<code>&quot;true&quot;</code> → <code>True</code></li><li>支持 string → integer/number/boolean 的自动转换</li></ul></li><li><p><strong>validate_params()</strong> - 参数验证（严格）</p><ul><li>基于 JSON Schema 递归验证参数</li><li>检查类型、必填字段、范围、长度、枚举值</li><li>返回错误列表（空列表表示验证通过）</li></ul></li></ol><p><strong>验证内容</strong>：</p><ul><li>类型检查：integer, number, string, boolean, array, object</li><li>必填字段检查：required 字段是否存在</li><li>数值范围检查：minimum, maximum</li><li>字符串长度检查：minLength, maxLength</li><li>枚举检查：enum 值是否在允许列表中</li><li>递归验证：支持嵌套对象和数组</li></ul><p><strong>设计特点</strong>：</p><ul><li>返回错误列表而非抛异常，可一次性收集所有错误</li><li>路径追踪机制，错误信息精确定位（如 <code>user.age should be integer</code>）</li><li>递归验证支持任意深度的嵌套结构</li></ul><p><strong>类型映射</strong>：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">_TYPE_MAP = &#123;</span><br><span class="line">    <span class="string">&quot;string&quot;</span>: <span class="built_in">str</span>,</span><br><span class="line">    <span class="string">&quot;integer&quot;</span>: <span class="built_in">int</span>,</span><br><span class="line">    <span class="string">&quot;number&quot;</span>: (<span class="built_in">int</span>, <span class="built_in">float</span>),</span><br><span class="line">    <span class="string">&quot;boolean&quot;</span>: <span class="built_in">bool</span>,</span><br><span class="line">    <span class="string">&quot;array&quot;</span>: <span class="built_in">list</span>,</span><br><span class="line">    <span class="string">&quot;object&quot;</span>: <span class="built_in">dict</span>,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="5-Tool-系统（含MCP）"><a href="#5-Tool-系统（含MCP）" class="headerlink" title="5. Tool 系统（含MCP）"></a>5. Tool 系统（含MCP）</h2><p><strong>核心思想</strong>：将 Agent 的外部能力抽象为统一的 Tool 接口，LLM 通过 Function Calling 调用，nanobot 负责参数转换、验证、执行和错误处理。</p><p><strong>完整调用流程</strong>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">用户消息</span><br><span class="line">  ↓</span><br><span class="line">AgentLoop 构建 messages + 从 ToolRegistry 获取所有工具定义</span><br><span class="line">  ↓</span><br><span class="line">AgentRunner 调用 LLM（携带工具定义）</span><br><span class="line">  ↓</span><br><span class="line">LLM 返回 tool_calls（结构化，非自由文本）</span><br><span class="line">  ↓</span><br><span class="line">ToolRegistry.execute(name, arguments)</span><br><span class="line">  ↓</span><br><span class="line">cast_params() → validate_params() → execute()</span><br><span class="line">  ↓</span><br><span class="line">工具结果加入 messages，继续调用 LLM</span><br><span class="line">  ↓</span><br><span class="line">无 tool_calls 时返回最终回复</span><br></pre></td></tr></table></figure><p><strong>错误处理</strong>：失败自动追加 <code>[Analyze the error above and try a different approach.]</code>，引导 LLM 换策略。</p><p><strong>MCP vs 原生 Tool</strong>：原生 Tool 进程内直接调用零开销；MCP Tool 连接外部服务器进程，适合接入第三方生态。</p><p><strong>工具列表</strong>：</p><table><thead><tr><th>Tool</th><th>用途</th><th>边界</th></tr></thead><tbody><tr><td><code>exec</code></td><td>执行 Shell 命令</td><td>deny_patterns 拦截危险命令；超时最大 600s；输出截断头尾各 5000 字符</td></tr><tr><td><code>read_file</code></td><td>读文件（支持图片）</td><td>仅限 allowed_dir；128K 字符上限；支持分页</td></tr><tr><td><code>write_file</code></td><td>写文件</td><td>仅限 allowed_dir；自动创建父目录</td></tr><tr><td><code>edit_file</code></td><td>替换文件中的文本</td><td>模糊匹配容忍空格差异；多处匹配需指定 replace_all</td></tr><tr><td><code>list_dir</code></td><td>列目录</td><td>自动忽略 .git/node_modules；默认最多 200 条</td></tr><tr><td><code>message</code></td><td>主动发消息/附件给用户</td><td>唯一能发文件附件的工具；依赖 send_callback 注入</td></tr><tr><td><code>web_search</code></td><td>网页搜索</td><td>支持 Brave/Tavily/DuckDuckGo；无 Key 自动降级；最多 10 条</td></tr><tr><td><code>web_fetch</code></td><td>抓取网页正文</td><td>SSRF 防护；外部内容加不可信标记；图片返回多模态块；50K 字符上限</td></tr><tr><td><code>spawn</code></td><td>派发子 Agent 后台执行</td><td>子 Agent 无 message/spawn 工具；完成通过 MessageBus 通知</td></tr><tr><td><code>cron</code></td><td>定时任务增删查</td><td>不能在 cron 中再创建任务；持久化到 jobs.json 重启不丢失</td></tr><tr><td><code>mcp_*</code></td><td>接入外部 MCP 服务器</td><td>需外部进程；工具名自动生成；超时默认 30s</td></tr></tbody></table><hr><h2 id="6-SubAgent-机制"><a href="#6-SubAgent-机制" class="headerlink" title="6. SubAgent 机制"></a>6. SubAgent 机制</h2><p><strong>核心思想</strong>：主 Agent 专注交互，将复杂耗时任务派发给子 Agent 在后台独立执行，完成后通知主 Agent。</p><p><strong>实现方式</strong>：</p><ul><li>SpawnTool 是普通 Tool，注册在 ToolRegistry，LLM 通过 Function Calling 调用，和 exec/web_search 没有本质区别</li><li>LLM 判断何时用 spawn 完全靠 SpawnTool 的 description，没有专门的 Skill：<blockquote><p>“Use this for complex or time-consuming tasks that can run independently.”</p></blockquote></li><li>用户可在 workspace 的 <code>AGENTS.md</code> 补充说明影响 LLM 判断</li><li>SubagentManager 负责子 Agent 的生命周期管理</li><li>子 Agent 通过独立的 AgentRunner 执行，与主 Agent 完全隔离</li></ul><p><strong>执行流程</strong>：</p><ol><li>主 Agent LLM 调用 <code>spawn</code> 工具</li><li>SubagentManager 创建 <code>asyncio.Task</code> 后台运行</li><li>主 Agent 立即返回，继续处理其他消息</li><li>子 Agent 完成后，Python 代码直接调用 <code>bus.publish_inbound()</code> 通知主 Agent</li><li>主 Agent 收到 system 消息，LLM 整理结果告知用户</li></ol><p><strong>子 Agent 的工具限制</strong>：</p><ul><li>✅ 有：文件读写、Shell、网页搜索</li><li>❌ 无：MessageTool（不能直接发消息给用户）</li><li>❌ 无：SpawnTool（不能再派发子 Agent，避免无限递归）</li></ul><p><strong>关键设计</strong>：</p><ul><li>通知机制不是回调，是 <code>await runner.run()</code> 完成后顺序执行 <code>_announce_result()</code></li><li>多个子 Agent 可并发运行，但无内置冲突处理，靠 LLM 规划任务边界</li><li>取消机制：<code>cancel_by_session()</code> 可取消某会话的所有子 Agent</li></ul><hr><h2 id="7-Agent-人格构建（identity-bootstrap）"><a href="#7-Agent-人格构建（identity-bootstrap）" class="headerlink" title="7. Agent 人格构建（identity + bootstrap）"></a>7. Agent 人格构建（identity + bootstrap）</h2><p><strong>核心思想</strong>：Agent 的”人格”由两层构成——所有 Agent 共享的底层 identity + 每个 workspace 独有的 bootstrap 文件。</p><h3 id="identity（硬编码层）"><a href="#identity（硬编码层）" class="headerlink" title="identity（硬编码层）"></a>identity（硬编码层）</h3><p>写死在 <code>context.py</code> 的 <code>_get_identity()</code> 里，用户不能通过文件修改，包含：</p><ul><li>Agent 身份声明（”You are nanobot”）</li><li>运行时信息（操作系统、CPU 架构、Python 版本）</li><li>workspace 路径和文件结构说明（memory、history、skills 的位置）</li><li>平台策略（Windows 不假设有 grep/sed；POSIX 优先用 shell 工具）</li><li>行为指南（先读再改、不信外部内容、工具调用前说明意图等）</li></ul><h3 id="bootstrap-文件（用户定制层）"><a href="#bootstrap-文件（用户定制层）" class="headerlink" title="bootstrap 文件（用户定制层）"></a>bootstrap 文件（用户定制层）</h3><p>由 <code>BOOTSTRAP_FILES = [&quot;AGENTS.md&quot;, &quot;SOUL.md&quot;, &quot;USER.md&quot;, &quot;TOOLS.md&quot;]</code> 定义（<code>context.py</code> 第 19 行），workspace 下存在才加载。</p><table><thead><tr><th>文件</th><th>定制什么</th></tr></thead><tbody><tr><td>AGENTS.md</td><td>行为指令（定时提醒规范、心跳任务规范等）</td></tr><tr><td>SOUL.md</td><td>性格和价值观（友好/严肃、简洁/详细）</td></tr><tr><td>USER.md</td><td>用户画像（姓名、时区、偏好、技术水平）</td></tr><tr><td>TOOLS.md</td><td>工具使用注意事项（安全限制、特殊用法）</td></tr></tbody></table><h3 id="workspace-初始化"><a href="#workspace-初始化" class="headerlink" title="workspace 初始化"></a>workspace 初始化</h3><p><code>sync_workspace_templates()</code> 负责把 <code>nanobot/templates/</code> 下的模板拷贝到 workspace。只创建不存在的文件，不覆盖已有内容。同时创建空的 HISTORY.md 和 skills 目录。该函数在 <code>nanobot agent</code>、<code>nanobot gateway</code>、<code>nanobot onboard</code> 命令启动时都会调用，确保 workspace 结构完整。</p><p>换一个 workspace 放不同的 bootstrap 文件，就是一个完全不同性格的 Agent。</p><hr><h2 id="8-记忆系统（Memory-Consolidation）"><a href="#8-记忆系统（Memory-Consolidation）" class="headerlink" title="8. 记忆系统（Memory Consolidation）"></a>8. 记忆系统（Memory Consolidation）</h2><p><strong>核心思想</strong>：对话越来越长会撑爆 LLM 上下文窗口，解决方案是让 LLM 自己当”笔记员”——定期把旧对话压缩成笔记存文件，然后只带着笔记 + 近期对话继续聊。</p><p><strong>两层记忆</strong>：</p><table><thead><tr><th>层</th><th>文件</th><th>类比</th><th>进 system prompt</th></tr></thead><tbody><tr><td>长期事实</td><td><code>MEMORY.md</code></td><td>个人档案，随时更新覆写</td><td>是</td></tr><tr><td>时间线日志</td><td><code>HISTORY.md</code></td><td>日记本，只追加不修改</td><td>否，需要时 grep 搜索</td></tr></tbody></table><p><strong>什么时候触发压缩</strong>：</p><ul><li>每次调用 LLM 前估算当前 prompt 的 token 量</li><li>超过上下文窗口的安全预算就触发</li><li>压到**预算的 50%**（留空间给后续对话，避免刚压完又触发）</li></ul><p><strong>压缩怎么做</strong>：</p><ul><li>估算 token 超出预算</li><li>→ 找切割点：从最早未压缩的消息往后扫，在 user 消息处标记（避免切断工具调用配对）</li><li>→ 切出旧消息段，连同当前 MEMORY.md 一起交给 LLM</li><li>→ LLM 输出两样东西：<ul><li>更新后的个人档案 → 覆写 MEMORY.md</li><li>带时间戳的摘要 → 追加到 HISTORY.md</li></ul></li><li>→ 指针前移，这段旧消息以后不再送给 LLM</li><li>→ 重新估算 token → 还超？再切一段（最多 5 轮）→ 不超？结束</li></ul><p><strong>为什么不需要重叠 chunk</strong>：</p><ul><li>每次压缩时 LLM 能同时看到已有 MEMORY.md 和新消息段，MEMORY.md 本身就是跨批次的共享上下文</li></ul><p><strong>降级兜底</strong>：LLM 压缩连续失败 3 次，就把原始消息直接存进 HISTORY.md，宁可粗糙也不丢数据。</p><p><strong>已知缺口</strong>：MEMORY.md 没有大小上限。正常情况下 LLM 会合并覆写旧事实，增长很慢；但长期运行 + 话题极度分散时，理论上可能膨胀到挤压对话空间。</p><hr><h2 id="9-技能系统（Skills）"><a href="#9-技能系统（Skills）" class="headerlink" title="9. 技能系统（Skills）"></a>9. 技能系统（Skills）</h2><p><strong>核心思想</strong>：Skill 是给 LLM 看的”操作手册”（markdown），教 Agent 遇到什么场景、调什么工具、传什么参数。写 markdown 就能扩展 Agent 能力。</p><p><strong>Skill vs Tool</strong>：Skill 教 LLM 怎么用 Tool，Tool 是 LLM 真正能调用的函数。Skill 本身不被执行，LLM 读完后按手册调用 Tool 完成任务。</p><p><strong>设计要点</strong>：</p><table><thead><tr><th>设计点</th><th>说明</th></tr></thead><tbody><tr><td>两类来源</td><td>workspace 自定义（高优先级，同名覆盖内置）+ nanobot 内置</td></tr><tr><td>两种加载模式</td><td>always=true 全文进 system prompt；其余只放 XML 目录，LLM 按需用 read_file 读取</td></tr><tr><td>渐进式加载</td><td>元数据始终在 → SKILL.md 触发后读 → 附带资源按需加载</td></tr><tr><td>依赖检查</td><td>frontmatter 声明 bins/env，不满足标记 available=false</td></tr><tr><td>无缓存</td><td>每次 LLM 请求前重新扫描，运行时新增 skill 立即生效</td></tr></tbody></table><p>内置了 memory、cron、weather、github 等 8 个 skill，其中 memory 是唯一 always=true 的。</p><hr><h2 id="10-LLM-提供商（Provider）"><a href="#10-LLM-提供商（Provider）" class="headerlink" title="10. LLM 提供商（Provider）"></a>10. LLM 提供商（Provider）</h2><p><strong>核心思想</strong>：通过注册表 + 自动匹配 + 统一抽象，让上层代码不关心底层用的是哪家 LLM。用户只需填模型名，系统自动找到对应服务商并抹平 API 差异。</p><p><strong>设计模式</strong>：注册表模式（<code>PROVIDERS</code> 元组定义所有服务商元数据）+ 适配器模式（4 种 backend 实现类把不同 API 转为统一 <code>LLMResponse</code>）+ 策略模式（<code>_match_provider()</code> 按优先级自动路由）。</p><p><strong>Provider 分类</strong>：</p><table><thead><tr><th>类型</th><th>特征</th><th>代表</th></tr></thead><tbody><tr><td>Gateway</td><td>聚合平台，一个 key 访问所有模型，请求格式兼容 OpenAI</td><td>OpenRouter、AiHubMix、SiliconFlow、火山引擎</td></tr><tr><td>标准</td><td>直连厂商 API，靠模型名关键词匹配</td><td>Anthropic、OpenAI、DeepSeek、Gemini、智谱、通义、Moonshot</td></tr><tr><td>OAuth</td><td>不用 API key，走浏览器授权登录</td><td>OpenAI Codex、GitHub Copilot</td></tr><tr><td>本地</td><td>本地部署，无需或简单 key</td><td>vLLM、Ollama、OpenVINO</td></tr><tr><td>自定义</td><td>用户提供完整 api_base + api_key</td><td>Custom、Azure OpenAI</td></tr></tbody></table><p><strong>Backend 实现类</strong>：</p><table><thead><tr><th>backend 值</th><th>实现类</th><th>覆盖范围</th></tr></thead><tbody><tr><td><code>openai_compat</code></td><td>OpenAICompatProvider</td><td>绝大多数（标准、Gateway、本地都走这个）</td></tr><tr><td><code>anthropic</code></td><td>AnthropicProvider</td><td>Anthropic（消息格式完全不同，需要专门转换）</td></tr><tr><td><code>azure_openai</code></td><td>AzureOpenAIProvider</td><td>Azure OpenAI（需要 api_version）</td></tr><tr><td><code>openai_codex</code></td><td>OpenAICodexProvider</td><td>OpenAI Codex（OAuth 认证）</td></tr></tbody></table><p><strong>模型名 → Provider 匹配优先级</strong>：</p><table><thead><tr><th>优先级</th><th>匹配方式</th><th>例子</th></tr></thead><tbody><tr><td>1</td><td>用户强制指定 provider</td><td>config 里写 <code>provider: deepseek</code></td></tr><tr><td>2</td><td>模型名前缀精确匹配</td><td><code>github_copilot/gpt-4o</code> → GitHub Copilot</td></tr><tr><td>3</td><td>模型名包含关键词</td><td><code>deepseek-chat</code> 含 “deepseek” → DeepSeek</td></tr><tr><td>4</td><td>本地部署兜底</td><td>无关键词匹配 → 有 api_base 的本地 provider</td></tr><tr><td>5</td><td>有 API key 的兜底</td><td>什么都没匹配 → 第一个有 key 的 provider</td></tr></tbody></table><p><strong>重试机制</strong>：瞬时错误（429/500/502/503/504/timeout）按 1s → 2s → 4s 重试最多 3 次；非瞬时错误且含图片时，去掉图片重试一次（兼容不支持多模态的模型）。</p><hr><h2 id="11-配置系统（Config）"><a href="#11-配置系统（Config）" class="headerlink" title="11. 配置系统（Config）"></a>11. 配置系统（Config）</h2><p><strong>核心思想</strong>：单一 <code>config.json</code> 管理所有运行时配置，通过 Pydantic 做结构校验，环境变量可覆盖，所有路径从配置文件位置派生。</p><p><strong>三个核心文件</strong>：</p><table><thead><tr><th>文件</th><th>职责</th></tr></thead><tbody><tr><td><code>schema.py</code></td><td>用 Pydantic 定义配置结构（Config 根对象），声明字段类型和默认值，自动校验不合法配置</td></tr><tr><td><code>loader.py</code></td><td>负责加载 config.json → 版本迁移 → Pydantic 验证 → 返回 Config 对象</td></tr><tr><td><code>paths.py</code></td><td>所有路径从 <code>config.json</code> 所在目录派生（data、cron、media、logs、workspace 等）</td></tr></tbody></table><p><strong>Config 根对象结构</strong>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">Config</span><br><span class="line">├── agents: AgentDefaults     # model、workspace、temperature、max_tokens、context_window_tokens、timezone</span><br><span class="line">├── channels: ChannelsConfig  # Telegram/Slack/Discord/飞书等渠道配置</span><br><span class="line">├── providers: ProvidersConfig # 所有 LLM 提供商的 api_key / api_base</span><br><span class="line">├── gateway: GatewayConfig    # HTTP 网关设置</span><br><span class="line">└── tools: ToolsConfig        # 工具安全限制（deny_patterns 等）</span><br></pre></td></tr></table></figure><p><strong>关键机制</strong>：</p><table><thead><tr><th>机制</th><th>说明</th></tr></thead><tbody><tr><td>Pydantic 校验</td><td>声明字段类型，写入时自动检测类型错误（如字符串填到整数字段）</td></tr><tr><td>环境变量覆盖</td><td><code>NANOBOT_</code> 前缀 + <code>__</code> 分隔嵌套层级，如 <code>NANOBOT_AGENTS__MODEL=gpt-4o</code></td></tr><tr><td>版本迁移</td><td><code>_migrate_config()</code> 处理旧版配置字段位置变更</td></tr><tr><td>多实例支持</td><td>通过 <code>--config</code> 参数指定不同配置文件，全局变量 <code>_config_path</code> 记录当前实例路径</td></tr><tr><td>Provider 自动匹配</td><td>Config 初始化时调用 <code>_match_provider()</code>，按 5 级优先级为模型名找到对应 provider</td></tr><tr><td>路径派生</td><td>所有数据目录从 <code>get_config_path().parent</code> 派生，无硬编码路径</td></tr></tbody></table><p><strong>config.json 维护方式</strong>：用户手动编辑，Agent 自身没有修改配置的工具。</p><hr><h2 id="12-会话管理（Session）⭐"><a href="#12-会话管理（Session）⭐" class="headerlink" title="12. 会话管理（Session）⭐"></a>12. 会话管理（Session）⭐</h2><p><strong>核心思想</strong>：按 <code>channel:chat_id</code> 隔离对话历史，持久化到 JSONL，append-only 保证 LLM prompt cache 命中。</p><p><strong>两个类</strong>：</p><table><thead><tr><th>类</th><th>职责</th></tr></thead><tbody><tr><td><code>Session</code></td><td>持有 messages 列表，提供裁剪、清空、合法性修正</td></tr><tr><td><code>SessionManager</code></td><td>JSONL 读写、内存 cache、生命周期管理</td></tr></tbody></table><p><strong>get_or_create 三种情况</strong>：</p><table><thead><tr><th>情况</th><th>处理</th></tr></thead><tbody><tr><td>内存 cache 命中</td><td>直接返回</td></tr><tr><td>磁盘有文件</td><td><code>_load()</code> 恢复，含游标</td></tr><tr><td>都没有</td><td>新建空 Session</td></tr></tbody></table><p><strong>隔离规则</strong>：key 格式 <code>channel:chat_id</code>，不同用户或平台各自独立。唯一共享的是 Agent 级别的 <code>MEMORY.md</code> / <code>HISTORY.md</code>。</p><hr><p><strong>last_consolidated 游标</strong></p><p><code>messages[0:last_consolidated]</code> 已压缩进 <code>MEMORY.md</code> / <code>HISTORY.md</code>，游标持久化在 JSONL 第一行，重启后恢复。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">messages: [msg_0 ... msg_49 | msg_50 ... msg_99]</span><br><span class="line">                              ↑</span><br><span class="line">                     last_consolidated = 50</span><br><span class="line">                     get_history() 只返回右侧部分</span><br><span class="line"></span><br><span class="line">msg_0~49 已压缩进 MEMORY.md（摘要），不再原文给 LLM</span><br></pre></td></tr></table></figure><hr><p><strong>LLM 每次收到的完整上下文</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">┌─ system prompt ──────────────────────────────┐</span><br><span class="line">│  identity + MEMORY.md摘要 + HISTORY.md + skills │</span><br><span class="line">└──────────────────────────────────────────────┘</span><br><span class="line">         +</span><br><span class="line">┌─ get_history() ──────────────────────────────┐</span><br><span class="line">│  messages[last_consolidated:]                 │</span><br><span class="line">│  → 最后 N 条 → 从第一条 user 开始            │</span><br><span class="line">│  → 丢弃孤儿 tool result                      │</span><br><span class="line">└──────────────────────────────────────────────┘</span><br><span class="line">         +</span><br><span class="line">  本次用户新消息</span><br></pre></td></tr></table></figure><p><strong>孤儿 tool result</strong>：截断后 window 开头是 tool result，但对应 assistant tool_calls 已被截掉，provider 会拒绝。<code>_find_legal_start()</code> 扫描找合法起始点，丢弃孤儿。</p><hr><p><strong>append-only 与 LLM Cache</strong></p><p>是一个LLM层的机制，但实际底层就是KV Cache，来避免前面重复计算：provider 对消息前缀做 hash，前缀不变则复用缓存。</p><p>因此传过去的消息，只追加不修改 → 前缀稳定 → cache 命中。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">第1次 [A,B,C]      → 缓存前缀</span><br><span class="line">第2次 [A,B,C,D]    → 前缀命中，只算 D</span><br><span class="line">第3次 [A,B,C,D,E]  → 前缀命中，只算 E</span><br><span class="line"></span><br><span class="line">截断后：</span><br><span class="line">第4次 [C,D,E,F]    → 缓存失效，全量计算，缓存新前缀</span><br><span class="line">第5次 [C,D,E,F,G]  → 新前缀命中，只算 G</span><br><span class="line"></span><br><span class="line">Memory Consolidation：</span><br><span class="line">[A,B,C] 压缩进 MEMORY.md → last_consolidated 前移</span><br><span class="line">messages 不动 → 缓存前缀不受影响</span><br></pre></td></tr></table></figure><hr><h2 id="13-事件总线（Bus）"><a href="#13-事件总线（Bus）" class="headerlink" title="13. 事件总线（Bus）"></a>13. 事件总线（Bus）</h2><p><strong>核心思想</strong>：两个 <code>asyncio.Queue</code>（inbound / outbound），所有需要触达 AgentLoop 的入口统一走 inbound queue，AgentLoop 回复走 outbound queue。各方只和 Bus 交互，互不认识。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">Channel / SubAgent / Cron</span><br><span class="line">  → publish_inbound(msg)</span><br><span class="line">      ↓</span><br><span class="line">  [inbound queue]</span><br><span class="line">      ↓</span><br><span class="line">  AgentLoop.consume_inbound() → 处理 → publish_outbound()</span><br><span class="line">      ↓</span><br><span class="line">  [outbound queue]</span><br><span class="line">      ↓</span><br><span class="line">  channel.consume_outbound() → 发回用户</span><br></pre></td></tr></table></figure><p><strong>三类 inbound 来源</strong>：</p><table><thead><tr><th>发送方</th><th>场景</th></tr></thead><tbody><tr><td>Channel</td><td>用户发来消息</td></tr><tr><td>SubAgent</td><td>子 Agent 完成后通知主 Agent</td></tr><tr><td>Cron</td><td>定时任务触发</td></tr></tbody></table><p><strong>session_key_override</strong>：<code>InboundMessage</code> 默认 session key 是 <code>channel:chat_id</code>，此字段可覆盖，用于 Slack thread 等需要在同一 chat_id 下隔离子会话的场景。</p><hr><h2 id="14-集成层-Channels"><a href="#14-集成层-Channels" class="headerlink" title="14. 集成层 Channels"></a>14. 集成层 Channels</h2><p><strong>核心思想</strong>：Channel 是平台适配层，统一把各平台消息转为 InboundMessage 塞进 Bus，互相完全隔离，只和 Bus 交互。</p><p><strong>BaseChannel 最少实现</strong>：</p><table><thead><tr><th>方法</th><th>职责</th></tr></thead><tbody><tr><td><code>start()</code></td><td>连接平台，长期监听，收到消息调 <code>_handle_message()</code></td></tr><tr><td><code>stop()</code></td><td>断开连接，清理资源</td></tr><tr><td><code>send(msg)</code></td><td>发送 OutboundMessage 给用户</td></tr></tbody></table><p>可选：<code>send_delta()</code>（流式）、<code>login()</code>（交互登录）</p><p><strong>_handle_message 流程</strong>：平台消息 → 白名单检查 → 封装 InboundMessage → bus.publish_inbound()</p><p><strong>ChannelManager</strong>：读 config 决定启用哪些 Channel，统一启动，持续消费 outbound queue 分发回对应 Channel，失败按 1s/2s/4s 重试，流式 delta 合并后再发。</p><p><strong>Registry 发现机制</strong>：内置（pkgutil 扫描目录）+ 插件（entry_points），内置优先。用户可通过第三方包注册插件，config.json 开启即用。</p><p><strong>内置 Channel</strong>：Telegram、Slack、Discord、飞书、企业微信、微信、钉钉、QQ、Mochat、Matrix、Email、WhatsApp</p><p><strong>不走 Channel 体系的入口</strong>：</p><table><thead><tr><th>入口</th><th>说明</th></tr></thead><tbody><tr><td>CLI</td><td>手动构建 Bus + AgentLoop，直接 publish_inbound</td></tr><tr><td>SubAgent</td><td>完成后直接 publish_inbound 通知主 Agent</td></tr></tbody></table><hr><h2 id="15-定时计划（Cron-Heartbeat）"><a href="#15-定时计划（Cron-Heartbeat）" class="headerlink" title="15. 定时计划（Cron / Heartbeat）"></a>15. 定时计划（Cron / Heartbeat）</h2><p><strong>Cron — 定时任务</strong></p><p>LLM 通过 CronTool 增删查任务，持久化到 jobs.json，重启不丢失。三种调度方式：</p><table><thead><tr><th>kind</th><th>说明</th></tr></thead><tbody><tr><td><code>at</code></td><td>指定时间点执行一次</td></tr><tr><td><code>every</code></td><td>固定间隔循环</td></tr><tr><td><code>cron</code></td><td>标准 cron 表达式，支持时区</td></tr></tbody></table><p>执行时调 <code>on_job</code> 回调（AgentLoop 注入），到期后重新计算下次时间。</p><p><strong>Heartbeat — 周期性唤醒</strong></p><p>用户用自然语言在 <code>HEARTBEAT.md</code> 写任务条件（如”每天9点提醒喝水”），LLM 定期读取判断当前是否该执行。用 tool call 强制返回结构化 <code>&#123;action: skip/run&#125;</code>，避免解析自由文本。有任务则跑完整 agent loop，结果值得通知时才发送。</p><hr><h2 id="16-CLI-入口与运行模式"><a href="#16-CLI-入口与运行模式" class="headerlink" title="16. CLI 入口与运行模式"></a>16. CLI 入口与运行模式</h2><p><strong>两种启动模式</strong>：</p><table><thead><tr><th></th><th>终端模式（<code>nanobot agent</code>）</th><th>服务模式（<code>nanobot gateway</code>）</th></tr></thead><tbody><tr><td>交互入口</td><td>键盘终端输入</td><td>Telegram/Slack 等 Channel</td></tr><tr><td>面向用户</td><td>你自己</td><td>部署给多人使用</td></tr><tr><td>典型部署</td><td>本地 PC</td><td>服务器常驻</td></tr><tr><td>Channel</td><td>无</td><td>启动 ChannelManager</td></tr><tr><td>Heartbeat</td><td>无</td><td>启动 HeartbeatService</td></tr></tbody></table><blockquote><p>注：代码里命令名叫 <code>gateway</code>，与 Provider 层的”聚合平台 Gateway”（OpenRouter 等）是两个不同概念，此处称”服务模式”以作区分。</p></blockquote><p><strong>终端模式启动流程</strong>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">_load_runtime_config()      → 加载 config.json</span><br><span class="line">sync_workspace_templates()  → 初始化 workspace 目录结构</span><br><span class="line">MessageBus()                → 创建事件总线</span><br><span class="line">_make_provider()            → 构建 LLM provider</span><br><span class="line">CronService()               → 初始化定时任务</span><br><span class="line">AgentLoop()                 → 注入所有依赖，构建主运行时</span><br><span class="line">键盘输入循环               → 直接 publish_inbound，读取回复打印</span><br></pre></td></tr></table></figure><p><strong>服务模式启动流程</strong>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">_load_runtime_config()      → 加载 config.json</span><br><span class="line">sync_workspace_templates()  → 初始化 workspace 目录结构</span><br><span class="line">MessageBus()                → 创建事件总线</span><br><span class="line">_make_provider()            → 构建 LLM provider</span><br><span class="line">SessionManager()            → 初始化会话管理</span><br><span class="line">CronService()               → 初始化定时任务</span><br><span class="line">AgentLoop()                 → 注入所有依赖，构建主运行时</span><br><span class="line">ChannelManager()            → 启动所有 Channel</span><br><span class="line">HeartbeatService()          → 启动心跳，常驻运行</span><br></pre></td></tr></table></figure><p><strong>多用户限制</strong>：单进程 asyncio，消息进 inbound queue 排队串行处理。小规模（几十人）够用，规模扩大时：</p><table><thead><tr><th>问题</th><th>原因</th></tr></thead><tbody><tr><td>串行处理</td><td>用户A等LLM时，用户B的消息在排队</td></tr><tr><td>LLM 调用慢</td><td>每次几秒到几十秒，排队体验差</td></tr><tr><td>单点故障</td><td>进程崩了所有用户断连</td></tr><tr><td>无水平扩展</td><td>不能加机器分摊压力</td></tr></tbody></table><p>项目本身不内置高并发方案，大规模需在外层自行添加负载均衡和多进程部署。</p><hr><h2 id="17-沙盒机制"><a href="#17-沙盒机制" class="headerlink" title="17. 沙盒机制"></a>17. 沙盒机制</h2><p><strong>结论：这不是真正的沙盒。</strong> 没有独立沙盒模块，没有 Docker / seccomp / chroot 等操作系统级隔离，只是代码层面的路径检查和命令过滤。</p><table><thead><tr><th>机制</th><th>位置</th><th>作用</th></tr></thead><tbody><tr><td><code>restrict_to_workspace</code></td><td>config.json + ExecTool / FilesystemTool</td><td>文件读写和命令执行只能在 workspace 目录内</td></tr><tr><td><code>deny_patterns</code></td><td>ExecTool</td><td>正则黑名单，阻止危险 shell 命令（如 <code>rm -rf /</code>）</td></tr></tbody></table><p>生产部署时官方推荐开启 <code>restrictToWorkspace: true</code>，但本质仍是应用层防护，绕过方式理论上存在。</p><hr>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;nanobot-技术要点&quot;&gt;&lt;a href=&quot;#nanobot-技术要点&quot; class=&quot;headerlink&quot; title=&quot;nanobot 技术要点&quot;&gt;&lt;/a&gt;nanobot 技术要点&lt;/h1&gt;&lt;h2 id=&quot;1-适配器模式实现多-LLM-统一响应&quot;&gt;&lt;a h</summary>
      
    
    
    
    <category term="AI" scheme="https://codingcodingk.github.io/categories/AI/"/>
    
    <category term="Claw" scheme="https://codingcodingk.github.io/categories/AI/Claw/"/>
    
    
    <category term="AI" scheme="https://codingcodingk.github.io/tags/AI/"/>
    
    <category term="AI Agent" scheme="https://codingcodingk.github.io/tags/AI-Agent/"/>
    
  </entry>
  
  <entry>
    <title>Hello Agents - 上下文工程</title>
    <link href="https://codingcodingk.github.io/Tech/AI/hello_agents/cp4/"/>
    <id>https://codingcodingk.github.io/Tech/AI/hello_agents/cp4/</id>
    <published>2026-03-10T09:16:09.565Z</published>
    <updated>2026-03-10T10:51:27.462Z</updated>
    
    <content type="html"><![CDATA[<h1 id="上下文工程是什么？"><a href="#上下文工程是什么？" class="headerlink" title="上下文工程是什么？"></a>上下文工程是什么？</h1><p>所谓“上下文”，是指在对大语言模型（LLM）进行采样时所包含的那组 tokens。手头的工程问题，是在 LLM 的固有约束之下，<strong>优化这些 tokens 的效用</strong>，以便稳定地得到预期结果。</p><p><strong>上下文工程 vs. 提示工程</strong></p><p>提示工程关注如何编写与组织 LLM 的指令以获得更优结果（例如系统提示的写法与结构化策略）；而上下文工程则是<strong>在推理阶段，如何策划与维护“最优的信息集合（tokens）”</strong>，其中不仅包含提示本身，还包含其他会进入上下文窗口的一切信息。</p><p><strong>上下文腐蚀（context rot）</strong></p><p>是指随着上下文窗口中的 tokens 增加，模型从上下文中准确回忆信息的能力反而下降。因此，上下文必须被视作一种有限资源，且具有<strong>边际收益递减</strong>。</p><h1 id="上下文工程的目标与实现"><a href="#上下文工程的目标与实现" class="headerlink" title="上下文工程的目标与实现"></a>上下文工程的目标与实现</h1><p>用尽可能少、但高信号密度的 tokens，最大化获得期望结果的概率。</p><ul><li><strong>系统提示（System Prompt）</strong>：语言清晰、直白，信息层级把握在“刚刚好”的高度。常见两极误区：<ul><li>过度硬编码：在提示中写入复杂、脆弱的 if-else 逻辑，长期维护成本高、易碎。</li><li>过于空泛：只给出宏观目标与泛化指引，缺少对期望输出的<strong>具体信号</strong>或假定了错误的“共享上下文”。</li></ul></li><li><strong>工具（Tools）</strong>：工具定义了智能体与信息/行动空间的契约，必须促进效率：既要返回<strong>token 友好</strong>的信息，又要鼓励高效的智能体行为。工具应当：<ul><li>职责单一、相互低重叠，接口语义清晰；</li><li>对错误鲁棒；</li><li>入参描述明确、无歧义，充分发挥模型擅长的表达与推理能力。</li></ul></li><li><strong>示例（Few-shot）</strong></li></ul><h1 id="长时程任务的上下文工程"><a href="#长时程任务的上下文工程" class="headerlink" title="长时程任务的上下文工程"></a>长时程任务的上下文工程</h1><p>无限增大上下文窗口并不能根治<strong>“上下文污染”与相关性退化</strong>的问题，因此需要直接面向这些约束的工程手段：<strong>压缩整合（Compaction）</strong>、<strong>结构化笔记（Structured note-taking）</strong>与<strong>子代理架构（Sub-agent architectures）</strong>。</p><p><strong>压缩整合（Compaction）</strong></p><ul><li>定义：当对话接近上下文上限时，对其进行高保真总结，并用该摘要重启一个新的上下文窗口，以维持长程连贯性。</li><li>实践：让模型压缩并保留架构性决策、未解决缺陷、实现细节，丢弃重复的工具输出与噪声；新窗口携带压缩摘要 + 最近少量高相关工件（如“最近访问的若干文件”）。</li></ul><p><strong>结构化笔记（Structured note-taking）</strong></p><ul><li>定义：也称“智能体记忆”。智能体以固定频率将关键信息写入<strong>上下文外的持久化存储</strong>，在后续阶段按需拉回。</li><li>实践：上下文保存外部文件的索引+概括，而不是整个文件，需要用了再去读。</li><li>价值：以极低的上下文开销维持持久状态与依赖关系。</li></ul><p><strong>子代理架构（Sub-agent architectures）</strong></p><ul><li>定义：由主代理负责高层规划与综合，多个专长子代理在“干净的上下文窗口”中各自深挖、调用工具并探索，最后仅回传<strong>凝练摘要</strong>（常见 1,000–2,000 tokens）。</li></ul><p><strong>三者评价：</strong></p><ul><li><strong>压缩整合</strong>：适合需要长对话连续性的任务，强调上下文的“接力”。</li><li><strong>结构化笔记</strong>：适合有里程碑/阶段性成果的迭代式开发与研究。</li><li><strong>子代理架构</strong>：适合复杂研究与分析，能从并行探索中获益。</li></ul><h1 id="上下文管理器设计"><a href="#上下文管理器设计" class="headerlink" title="上下文管理器设计"></a>上下文管理器设计</h1><h2 id="候选信息包"><a href="#候选信息包" class="headerlink" title="候选信息包"></a>候选信息包</h2><p><code>ContextPacket</code> 是系统中信息的<strong>基本单元</strong>。每个候选信息都会被封装为一个 ContextPacket，包含内容、时间戳、token 数量和相关性分数等核心属性。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">Attributes:</span><br><span class="line">       content: 信息内容</span><br><span class="line">       timestamp: 时间戳</span><br><span class="line">       token_count: Token 数量</span><br><span class="line">       relevance_score: 相关性分数(<span class="number">0.0</span>-<span class="number">1.0</span>)</span><br><span class="line">       metadata: 可选的元数据</span><br></pre></td></tr></table></figure><h2 id="上下文生成流水线"><a href="#上下文生成流水线" class="headerlink" title="上下文生成流水线"></a>上下文生成流水线</h2><p>这边叫GSSC(Gather-Select-Structure-Compress)流水线，它将上下文构建过程分解为四个清晰的阶段。</p><h3 id="1-Gather：多源信息汇集"><a href="#1-Gather：多源信息汇集" class="headerlink" title="1.Gather：多源信息汇集"></a>1.Gather：多源信息汇集</h3><p>其实就是做出一份<strong>候选上下文列表</strong>，进行评级、裁剪、融合。核心机制是 <strong>优先级+数量限制</strong>。</p><h4 id="流程："><a href="#流程：" class="headerlink" title="流程："></a>流程：</h4><ol><li><strong>系统指令</strong>：有就先放进去（最高优先级），直接给满分相关性 <code>1.0</code>，确保一定保留。</li><li><strong>记忆检索</strong>：调用 <code>memory_tool</code> 按 <code>user_query</code> 搜索（limit 10），把结果解析成 <code>ContextPacket</code> 加入；失败就告警但不中断。</li><li><strong>RAG 检索</strong>：调用 <code>rag_tool</code> 搜索知识（limit 5），解析后加入；失败同样只告警。</li><li><strong>对话历史</strong>：只取最近 5 条，每条包装成 <code>ContextPacket</code>，给一个基础相关性 <code>0.6</code>。</li><li><strong>自定义包</strong>：外部传进来的 <code>custom_packets</code> 直接追加。</li></ol><h3 id="2-Select：智能信息选择"><a href="#2-Select：智能信息选择" class="headerlink" title="2.Select：智能信息选择"></a>2.Select：智能信息选择</h3><p><code>_select()</code> 负责把 <code>_gather()</code> 收集到的候选信息 <strong>打分、排序、过滤</strong>，再在 token 预算内 <strong>挑出最值得放进最终上下文的一组</strong>（系统指令优先保留）。</p><h4 id="流程：-1"><a href="#流程：-1" class="headerlink" title="流程："></a>流程：</h4><pre><code>1. **系统指令单独处理**：先把 `type=system_instruction` 的包分离出来，永远保留且不参与竞争。2. **算 token 预算**：系统指令先占坑，剩余 token 不够就直接只返回系统指令。3. **给其他包算综合分**：- 相关性：若还是默认值 `0.5`，就用 `_calculate_relevance()` 重新算- 新近性：用 `_calculate_recency()` 算时间越近分越高- 综合分：`relevance_weight * relevance + recency_weight * recency`4. **过滤低质量**：相关性低于 `min_relevance` 的直接丢掉。5. **排序 + 贪心选入**：按综合分从高到低依次加入，直到 token 用满为止。</code></pre><h4 id="总结："><a href="#总结：" class="headerlink" title="总结："></a>总结：</h4><ul><li><strong>评分机制</strong>：相关性 + 新近性加权（可配置权重）</li><li><strong>贪心选择</strong>：预算有限时，优先塞“单位价值最高”的内容</li><li><strong>过滤机制</strong>：用 <code>min_relevance</code> 把低相关噪声挡掉</li></ul><h3 id="3-Structure：结构化输出"><a href="#3-Structure：结构化输出" class="headerlink" title="3.Structure：结构化输出"></a>3.Structure：结构化输出</h3><p><code>_structure()</code> 把选中的 <code>ContextPacket</code> <strong>按类型分组</strong>，再拼成一个带分区标题的 <strong>结构化上下文模板字符串</strong>，用于最终喂给模型。</p><h4 id="流程：-2"><a href="#流程：-2" class="headerlink" title="流程："></a>流程：</h4><ol><li><strong>分类分组</strong><ul><li><code>system_instruction</code> → 放到 <code>system_instructions</code></li><li><code>rag_result / knowledge</code> → 当作证据放到 <code>evidence</code></li><li>其他 → 放到一般 <code>context</code></li></ul></li><li><strong>按固定模板输出 5 个区块</strong><ul><li><code>[Role &amp; Policies]</code>：系统指令（如果有）</li><li><code>[Task]</code>：用户问题 <code>user_query</code>（始终有）</li><li><code>[Evidence]</code>：检索到的知识/证据（用 <code>---</code> 分隔）</li><li><code>[Context]</code>：对话历史/自定义信息等背景</li><li><code>[Output]</code>：输出要求（提示“基于以上信息回答”）</li></ul></li></ol><p>最后用空行把各区块连接成一个完整 prompt。</p><h3 id="4-Compress：兜底压缩"><a href="#4-Compress：兜底压缩" class="headerlink" title="4.Compress：兜底压缩"></a>4.Compress：兜底压缩</h3><p><code>_compress()</code> 是上下文超出 token 上限时的<strong>兜底压缩</strong>：尽量按分区保留结构，只在必要时截断最后一部分内容，让最终上下文不超过 <code>max_tokens</code>。</p><h4 id="流程：-3"><a href="#流程：-3" class="headerlink" title="流程："></a>流程：</h4><ol><li><strong>先估算 token</strong>：<code>_count_tokens(context)</code>，没超限就直接返回。</li><li><strong>按分区处理</strong>：用空行 <code>\n\n</code> 把上下文切成多个 section（对应你前面的结构化分区）。</li><li><strong>贪心保留</strong>：从前到后逐段加入，只要不超 token 就“整段保留”。</li><li><strong>需要超限时</strong>：<ul><li>计算剩余 token（<code>remaining_tokens</code>）</li><li>如果剩余还够 50 tokens，就对该 section 做<strong>截断</strong>（<code>_truncate_text</code>），并追加提示 <code>&quot;[... 内容已压缩 ...]&quot;</code></li><li>然后停止（后面的分区不再保留）</li></ul></li><li><strong>输出压缩结果</strong>：拼回文本，并打印压缩前后 token 数。</li></ol><h4 id="结论："><a href="#结论：" class="headerlink" title="结论："></a>结论：</h4><p>基本就是先分区压缩，还不够就直接从5个区块从上往下放，token一不够，直接截断，抛弃后面内容。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;上下文工程是什么？&quot;&gt;&lt;a href=&quot;#上下文工程是什么？&quot; class=&quot;headerlink&quot; title=&quot;上下文工程是什么？&quot;&gt;&lt;/a&gt;上下文工程是什么？&lt;/h1&gt;&lt;p&gt;所谓“上下文”，是指在对大语言模型（LLM）进行采样时所包含的那组 tokens。手</summary>
      
    
    
    
    <category term="AI" scheme="https://codingcodingk.github.io/categories/AI/"/>
    
    <category term="Hello Agents" scheme="https://codingcodingk.github.io/categories/AI/Hello-Agents/"/>
    
    
    <category term="AI" scheme="https://codingcodingk.github.io/tags/AI/"/>
    
    <category term="AI Agent" scheme="https://codingcodingk.github.io/tags/AI-Agent/"/>
    
  </entry>
  
  <entry>
    <title>Hello Agents - 记忆与检索&amp;RAG</title>
    <link href="https://codingcodingk.github.io/Tech/AI/hello_agents/cp3/"/>
    <id>https://codingcodingk.github.io/Tech/AI/hello_agents/cp3/</id>
    <published>2026-03-08T06:15:27.020Z</published>
    <updated>2026-03-08T13:50:45.548Z</updated>
    
    <content type="html"><![CDATA[<h1 id="记忆与检索"><a href="#记忆与检索" class="headerlink" title="记忆与检索"></a>记忆与检索</h1><h2 id="需求原因"><a href="#需求原因" class="headerlink" title="需求原因"></a>需求原因</h2><p><strong>局限一：无状态导致的对话遗忘</strong></p><p>当前的大语言模型虽然强大，但设计上是<strong>无状态的</strong>。这意味着，每一次用户请求（或API调用）都是一次独立的、无关联的计算。模型本身不会自动“记住”上一次对话的内容。这带来了几个问题：</p><ol><li><strong>上下文丢失</strong>：在长对话中，早期的重要信息可能会因为上下文窗口限制而丢失</li><li><strong>个性化缺失</strong>：Agent无法记住用户的偏好、习惯或特定需求</li><li><strong>学习能力受限</strong>：无法从过往的成功或失败经验中学习改进</li><li><strong>一致性问题</strong>：在多轮对话中可能出现前后矛盾的回答</li></ol><p>要解决这个问题，我们的框架需要引入<strong>记忆系统</strong>。</p><p><strong>局限二：模型内置知识的局限性</strong></p><p>除了遗忘对话历史，LLM 的另一个核心局限在于其知识是<strong>静态的、有限的</strong>。这些知识完全来自于它的训练数据，并因此带来一系列问题：</p><ol><li><strong>知识时效性</strong>：大模型的训练数据有时间截止点，无法获取最新信息</li><li><strong>专业领域知识</strong>：通用模型在特定领域的深度知识可能不足</li><li><strong>事实准确性</strong>：通过检索验证，减少模型的幻觉问题</li><li><strong>可解释性</strong>：提供信息来源，增强回答的可信度</li></ol><p>为了克服这一局限，<strong>RAG技术</strong>应运而生。它的核心思想是在模型生成回答之前，先从一个外部知识库（如文档、数据库、API）中检索出最相关的信息，并将这些信息作为上下文一同提供给模型。</p><h2 id="记忆模块"><a href="#记忆模块" class="headerlink" title="记忆模块"></a>记忆模块</h2><h3 id="设计"><a href="#设计" class="headerlink" title="设计"></a>设计</h3><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20260308181733.png"></p><ol><li> **工作记忆 (Working Memory)**：充当短期记忆的角色，存储上下文，快速访问，限制容量（比如默认50条）。</li><li>**情景记忆 (Episodic Memory)**：长期存储具体的交互事件和智能体的学习经历。支持按时间序列或主题进行回顾式检索。</li><li>**语义记忆 (Semantic Memory)**：它存储的是更为抽象的知识、概念和规则。比如用户偏好、需要长期遵守的指令或者领域知识点。</li><li>**感知记忆 (Perceptual Memory)**：专门处理图像音频，支持跨模态检索。</li></ol><h3 id="工作记忆"><a href="#工作记忆" class="headerlink" title="工作记忆"></a>工作记忆</h3><ol><li>对memory操作的主要的方法是：<code>add</code>、<code>search</code>、<code>forget</code>、<code>consolidate</code>（整合，短期记忆提升为长期记忆）</li><li>工作记忆采用了纯内存存储方案，配合TTL（Time To Live）机制进行<strong>自动清理</strong>。</li><li>工作记忆的检索采用了<strong>混合检索策略</strong>，首先尝试使用TF-IDF向量化进行<strong>语义检索</strong>，如果失败则回退到<strong>关键词匹配</strong>。</li></ol><h3 id="情景记忆"><a href="#情景记忆" class="headerlink" title="情景记忆"></a>情景记忆</h3><p>情景记忆负责存储具体的事件和经历，它的设计重点在于保持事件的完整性和时间序列关系。采用了SQLite+Qdrant（向量数据库）的混合存储方案。</p><h4 id="1-存记忆（add）"><a href="#1-存记忆（add）" class="headerlink" title="1) 存记忆（add）"></a>1) 存记忆（<code>add</code>）</h4><ul><li>把一条记忆（内容 + 时间 + 会话ID + 其他信息）打包成一个 <code>Episode</code></li><li>记录这条记忆属于哪个会话（<code>session_id</code>）</li><li>然后保存到两种地方：<ul><li><strong>SQLite</strong>：像“表格数据库”，方便按时间、用户、会话等条件筛选</li><li><strong>Qdrant</strong>：像“向量库”，把文本变成向量，方便按“语义相似”搜索</li></ul></li></ul><h4 id="2-找记忆（retrieve）"><a href="#2-找记忆（retrieve）" class="headerlink" title="2) 找记忆（retrieve）"></a>2) 找记忆（<code>retrieve</code>）</h4><p>找的时候走三步：</p><ol><li><strong>先按条件过滤</strong>（结构化过滤）<br>比如：只看某个时间范围、某个用户、某个会话的记忆（用 SQLite 这种结构化方式更快更准）</li><li><strong>再做语义搜索</strong>（向量检索）<br>用 Qdrant 找“意思最像”的记忆（不是关键词匹配，而是语义相似）</li><li><strong>最后打分排序</strong><br>每条候选记忆会算一个总分，分数越高越靠前。</li></ol><h4 id="打分怎么来的（-calculate-episode-score）"><a href="#打分怎么来的（-calculate-episode-score）" class="headerlink" title="打分怎么来的（_calculate_episode_score）"></a>打分怎么来的（<code>_calculate_episode_score</code>）</h4><p>它把三件事合起来：</p><ul><li><strong>语义相似度</strong>（最重要，占 80%）</li><li><strong>时间越近越加分</strong>（占 20%）</li><li><strong>重要性权重</strong>（importance 越高，整体再乘一个更大的系数）</li></ul><h3 id="语义记忆"><a href="#语义记忆" class="headerlink" title="语义记忆"></a>语义记忆</h3><p>它负责存储抽象的概念、规则和知识。语义记忆采用了<strong>Neo4j图数据库</strong>和<strong>Qdrant向量数据库</strong>的混合架构。简单来说，在这边做了2套数据库来供检索，1是向量数据库，2是实体/关系的知识图谱数据库。</p><h4 id="向量-amp-知识图谱-数据库，怎么用的？"><a href="#向量-amp-知识图谱-数据库，怎么用的？" class="headerlink" title="向量&amp;知识图谱 数据库，怎么用的？"></a>向量&amp;知识图谱 数据库，怎么用的？</h4><ul><li><p>你把一段文本/知识喂给它：<code>add(memory_item)</code><br>它会自动：</p><ol><li><p>把文本转成<strong>向量</strong></p></li><li><p>从文本里抽取<strong>实体、关系</strong></p></li><li><p>写入图数据库 + 向量数据库</p></li></ol></li><li><p>你问一个问题：<code>retrieve(query, limit=5)</code><br>它会：</p><ol><li><p>用<strong>向量</strong>库找语义相似的记忆</p></li><li><p>用图数据库找相关<strong>实体/关系</strong>的记忆</p></li><li><p>把两边结果合并排序，取前 <code>limit</code> 条返回</p></li></ol></li></ul><h4 id="具体存储-amp-检索代码呢？"><a href="#具体存储-amp-检索代码呢？" class="headerlink" title="具体存储&amp;检索代码呢？"></a>具体存储&amp;检索代码呢？</h4><p>向量部分：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">function add:</span><br><span class="line">    // 把记忆文本编码成向量：</span><br><span class="line">embedding = self.embedding_model.encode(memory_item.content)</span><br><span class="line">    // 然后把向量丢进 Qdrant：</span><br><span class="line">    self.vector_store.add_vectors(vectors=[...], metadata=[...], ids=[memory_item.<span class="built_in">id</span>])</span><br><span class="line">    </span><br><span class="line">function retrieve:</span><br><span class="line">    // 查向量数据库</span><br><span class="line">    vector_results = self._vector_search(query, limit * <span class="number">2</span>, user_id)</span><br><span class="line">    // 这块没代码，流程通常是：</span><br><span class="line">// 先把 query 也 encode 成向量</span><br><span class="line">// 在 Qdrant 里找“距离最近”的若干条向量（Top-K）</span><br><span class="line">// 返回结果里带一个 score（相似度分）</span><br><span class="line"></span><br><span class="line">// Qdrant存储的结构一般是：</span><br><span class="line">&#123;<span class="built_in">id</span>, // 记忆ID（用来回到原文/数据库）</span><br><span class="line">vector, // 向量</span><br><span class="line">metadata, //附加信息（比如包含哪些实体、重要性、用户ID、时间等）</span><br><span class="line">    &#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>知识图谱部分：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line">function add:</span><br><span class="line">    // 抽实体&amp;关系， 例如：蓝牙耳机 -故障部位-&gt; 左耳</span><br><span class="line">entities = self._extract_entities(memory_item.content)</span><br><span class="line">    relations = self._extract_relations(memory_item.content, entities)</span><br><span class="line">    // 写入 Neo4j：</span><br><span class="line">    <span class="keyword">for</span> entity <span class="keyword">in</span> entities:</span><br><span class="line">        self._add_entity_to_graph(entity, memory_item)</span><br><span class="line">    <span class="keyword">for</span> relation <span class="keyword">in</span> relations:</span><br><span class="line">        self._add_relation_to_graph(relation, memory_item)</span><br><span class="line">    </span><br><span class="line">function retrieve:</span><br><span class="line">    // 查图谱数据库</span><br><span class="line">    graph_results = self._graph_search(query, limit * <span class="number">2</span>, user_id)</span><br><span class="line">    // 这块没代码，流程通常是：</span><br><span class="line">// <span class="number">1.</span>先从 query 里抽实体/关键词（比如“订单<span class="number">984233</span>”、“耳机”）</span><br><span class="line">// <span class="number">2.</span>去 Neo4j 里查：</span><br><span class="line">// 直接命中的实体节点</span><br><span class="line">// 与之相连的一跳/两跳关系（邻居节点）</span><br><span class="line">// <span class="number">3.</span>把这些实体/关系关联到的 memory_id 找出来</span><br><span class="line">// <span class="number">4.</span>给每条记忆一个 graph_score（关系匹配程度/路径相关程度）</span><br><span class="line"></span><br><span class="line">// Neo4j存储的结构一般是：</span><br><span class="line">&#123;Node, // 节点，实际是Entity（带类型、名称、ID）</span><br><span class="line">Edge, // 边，实际是Relation（带关系类型、强度、来源记忆ID等）</span><br><span class="line">memory_id, // 把“这条关系/实体来自哪条记忆(memory_id)”记下来，方便回溯原文</span><br><span class="line">    &#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h4 id="为什么要混合用？"><a href="#为什么要混合用？" class="headerlink" title="为什么要混合用？"></a>为什么要混合用？</h4><ul><li>只用向量：语义相近很强，但对“实体关系链”的查询不一定稳（尤其是编号、关系推理）</li><li>只用图谱：对实体关系很强，但对“同义改写/自然语言泛化”弱</li></ul><p>所以它混合：</p><ul><li>向量给“语义相似度”（0.7）</li><li>图谱给“关系相似度/推理补充”（0.3）</li><li>再用重要性做轻微加权</li></ul><h4 id="知识图谱是什么？为什么要用？"><a href="#知识图谱是什么？为什么要用？" class="headerlink" title="知识图谱是什么？为什么要用？"></a>知识图谱是什么？为什么要用？</h4><p>知识图谱把信息变成：</p><ul><li><strong>实体（Entity）</strong>：人、产品、公司、地点、订单、功能名……</li><li>关系（Relation）：实体之间的连接，例如：<ul><li>“用户A <strong>购买</strong> 订单984233”</li><li>“订单984233 <strong>包含</strong> 商品：蓝牙耳机”</li><li>“蓝牙耳机 <strong>属于</strong> 品牌X”</li><li>“故障 <strong>发生在</strong> 左耳”</li></ul></li></ul><p>图谱强在：<strong>不仅能找“像不像”，还能找“有没有关系”</strong>。</p><h3 id="感知记忆"><a href="#感知记忆" class="headerlink" title="感知记忆"></a>感知记忆</h3><p>感知记忆支持文本、图像、音频等多种模态的数据存储和检索，为不同模态的数据创建独立的向量集合。</p><h4 id="是什么"><a href="#是什么" class="headerlink" title="是什么"></a>是什么</h4><p>这是一个“<strong>多模态记忆库</strong>”：可以把 <strong>文本 / 图片 / 音频</strong> 等内容存起来，并在需要时按“相似”把相关内容找出来。它的关键设计是：<strong>不同模态分开存</strong>（每种模态一个独立向量库/collection），这样不会因为向量维度不同而混乱，检索也更准。</p><h4 id="用法（怎么用）"><a href="#用法（怎么用）" class="headerlink" title="用法（怎么用）"></a>用法（怎么用）</h4><ul><li><p><strong>存入</strong>：把一条记忆（内容 + 模态 + 时间 + 重要性等）交给它保存（代码里没贴 add，但逻辑就是：先编码成向量，再写入对应模态的向量库）。</p></li><li><p><strong>检索</strong>：调用</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">retrieve(query, limit=5, query_modality=..., target_modality=..., user_id=...)</span><br></pre></td></tr></table></figure><ul><li><code>query_modality</code>：你输入的 query 是什么模态（默认 text）</li><li><code>target_modality</code>：你想搜哪种模态的记忆（不填就搜同模态）</li><li><code>user_id</code>：只查某个用户的记忆（可选）</li></ul></li></ul><p>同模态例子：用文本去搜文本记忆。<br>跨模态例子：用文本描述去搜相关图片/音频（依赖“对齐到同一语义空间”的编码器）。</p><h4 id="怎么做的（实现步骤）"><a href="#怎么做的（实现步骤）" class="headerlink" title="怎么做的（实现步骤）"></a>怎么做的（实现步骤）</h4><ol><li>为不同模态准备<strong>不同编码器</strong></li></ol><ul><li>文本：<code>text_embedder</code></li><li>图片：<code>CLIP</code> 模型（把图片或文本映射到和图片相关的向量空间）</li><li>音频：<code>CLAP</code> 模型（把音频或文本映射到和音频相关的向量空间）</li></ul><ol start="2"><li> 为每个模态建一个独立的向量集合（Qdrant collections）</li></ol><ul><li><code>perceptual_text</code>（文本向量，维度 = text 的 dim）</li><li><code>perceptual_image</code>（图片向量，维度 = image 的 dim）</li><li><code>perceptual_audio</code>（音频向量，维度 = audio 的 dim）</li></ul><p>这样做的目的：<strong>不同模态向量维度不同，必须分开存</strong>。</p><ol start="3"><li><p>检索流程（retrieve）</p><ol><li><p>先把 query 用对应模态的<strong>编码器</strong>转成向量：<code>_encode_data(query, query_modality)</code></p></li><li><p>选择要搜的向量库（同模态或指定 target 模态）</p></li><li><p>在 Qdrant 里做相似度搜索（并加一些过滤条件：user_id、modality 等）</p></li><li><p>对结果重新打分排序：</p><ul><li><p>语义相似度（占 80%）</p></li><li><p>时间越近越加分（占 20%，用指数衰减模拟遗忘）</p></li><li><p>重要性再乘一个权重（让重要内容略靠前）</p></li></ul></li><li><p>返回前 <code>limit</code> 条</p></li></ol></li></ol><h1 id="RAG系统"><a href="#RAG系统" class="headerlink" title="RAG系统"></a>RAG系统</h1><h2 id="什么是RAG"><a href="#什么是RAG" class="headerlink" title="什么是RAG"></a>什么是RAG</h2><p><strong>检索增强生成（Retrieval-Augmented Generation，RAG）</strong>是一种结合了信息检索和文本生成的技术。它的核心思想是：在生成回答之前，先从外部知识库中检索相关信息，然后将检索到的信息作为上下文提供给大语言模型，从而生成更准确、更可靠的回答。</p><p>一个完整的RAG应用流程主要分为两大核心环节。</p><ol><li>在<strong>数据准备阶段</strong>，系统通过<strong>数据提取</strong>、<strong>文本分割</strong>和<strong>向量化</strong>，将外部知识构建成一个可检索的数据库。</li><li>随后在<strong>应用阶段</strong>，系统会响应用户的<strong>提问</strong>，从数据库中<strong>检索</strong>相关信息，将其<strong>注入Prompt</strong>，并最终驱动大语言模型<strong>生成答案</strong>。</li></ol><h2 id="工作模式"><a href="#工作模式" class="headerlink" title="工作模式"></a>工作模式</h2><ol><li><strong>数据处理流程</strong>：处理和存储知识文档，在这里我们采取工具<code>Markitdown</code>，设计思路是将传入的一切外部知识源统一转化为Markdown格式进行处理。</li><li><strong>查询与生成流程</strong>：根据查询检索相关信息并生成回答。</li></ol><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/8-5.png"></p><h2 id="RAG-存储pipeline-☆"><a href="#RAG-存储pipeline-☆" class="headerlink" title="RAG 存储pipeline ☆"></a>RAG 存储pipeline ☆</h2><ol><li><strong>转换格式</strong>：把所有不同类型的文件（JPG,MP3…） 全部通过<code>MarkItDown</code>转换形成markdown格式文件。</li><li><strong>智能分块</strong>：<ol><li>使用markdown的标题（#、##）、和段落，进行<strong>语义分割</strong></li><li>根据Token的数量，把这些分割好的段落切成<strong>块(chunk)</strong></li><li>下一个块的开头，会有上一个块结尾的内容，这叫做**重叠()**，目的是为了保持块之间的上下文连接</li></ol></li><li><strong>统一嵌入与向量存储</strong>：把文本块转换为向量，存储在向量数据库中。这边嵌入（embedding）说的就是转成高维度的向量模型。</li></ol><h2 id="高级查询策略"><a href="#高级查询策略" class="headerlink" title="高级查询策略"></a>高级查询策略</h2><p><strong>（1）多查询扩展（MQE）</strong></p><blockquote><p>例如，”如何学习Python”可以扩展为”Python入门教程”、”Python学习方法”、”Python编程指南”等多个查询。    </p></blockquote><p>​    多查询扩展（Multi-Query Expansion）是一种通过<strong>生成语义等价的多样化查询</strong>来提高检索召回率的技术。这种方法的核心洞察是：同一个问题可以有多种不同的表述方式，而不同的表述可能匹配到不同的相关文档。</p><p>​    MQE的优势在于它能够自动理解用户查询的多种可能含义，特别是对于模糊查询或专业术语查询效果显著。</p><p>​    系统使用LLM生成扩展查询，确保扩展的多样性和语义相关性。</p><p><strong>（2）假设文档嵌入（HyDE）</strong></p><p>​    假设文档嵌入（Hypothetical Document Embeddings，HyDE）是一种创新的检索技术，它的核心思想是”用答案找答案”。</p><p>​    HyDE通过让LLM先生成一个假设性的答案段落，然后用这个答案段落去检索真实文档，从而缩小了查询和文档之间的语义鸿沟。因为答案与答案比起问题与答案来说，拥有更加接近的语义空间。</p><p><strong>（3）扩展检索框架</strong></p><p>​    将MQE和HyDE两种策略整合到统一的扩展检索框架中。扩展检索的核心机制是**”扩展-检索-合并”**三步流程。先用多种搜索方法（比如上面的MQE+HyDE），检索到的文档进池，然后去重+分数排序，获得最后的Top-K检索结果。</p><p>​    <strong>优化点</strong>：对于需要高召回率的场景可以同时启用两种策略，对于性能敏感的场景可以只使用基础检索。</p><h2 id="使用案例"><a href="#使用案例" class="headerlink" title="使用案例"></a>使用案例</h2><ol><li>把一些pdf等资料，预先调用RAGTool的方法，来触发完整处理流程（MarkItDown转换、增强处理、智能分块、向量化存储）。</li><li>把“这个文档加载成功”加到情景记忆模块中。</li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;记忆与检索&quot;&gt;&lt;a href=&quot;#记忆与检索&quot; class=&quot;headerlink&quot; title=&quot;记忆与检索&quot;&gt;&lt;/a&gt;记忆与检索&lt;/h1&gt;&lt;h2 id=&quot;需求原因&quot;&gt;&lt;a href=&quot;#需求原因&quot; class=&quot;headerlink&quot; title=&quot;需求原因&quot;</summary>
      
    
    
    
    <category term="AI" scheme="https://codingcodingk.github.io/categories/AI/"/>
    
    <category term="Hello Agents" scheme="https://codingcodingk.github.io/categories/AI/Hello-Agents/"/>
    
    
    <category term="AI" scheme="https://codingcodingk.github.io/tags/AI/"/>
    
    <category term="AI Agent" scheme="https://codingcodingk.github.io/tags/AI-Agent/"/>
    
  </entry>
  
  <entry>
    <title>Hello Agents - 构建Agent</title>
    <link href="https://codingcodingk.github.io/Tech/AI/hello_agents/cp2/"/>
    <id>https://codingcodingk.github.io/Tech/AI/hello_agents/cp2/</id>
    <published>2026-03-05T07:49:16.059Z</published>
    <updated>2026-03-07T10:16:39.652Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Agent-经典范式"><a href="#Agent-经典范式" class="headerlink" title="Agent 经典范式"></a>Agent 经典范式</h1><p>为了更好地组织智能体的“思考”与“行动”过程，业界涌现出了多种经典的架构范式。在本章中，我们将聚焦于其中最具代表性的三种，并一步步从零实现它们：</p><ul><li><strong>ReAct (Reasoning and Acting)：</strong> 一种将“思考”和“行动”紧密结合的范式，让智能体边想边做，动态调整。</li><li><strong>Plan-and-Solve：</strong> 一种“三思而后行”的范式，智能体首先生成一个完整的行动计划，然后严格执行。</li><li><strong>Reflection：</strong> 一种赋予智能体“反思”能力的范式，通过自我批判和修正来优化结果。</li></ul><h2 id="ReAct"><a href="#ReAct" class="headerlink" title="ReAct"></a>ReAct</h2><h3 id="基本模式"><a href="#基本模式" class="headerlink" title="基本模式"></a>基本模式</h3><p>思考与行动是相辅相成的，思考指导行动，而行动的结果又反过来修正思考。</p><p>具体执行分成3步：</p><ul><li><strong>Thought (思考)：</strong> 这是智能体的“内心独白”。它会分析当前情况、分解任务、制定下一步计划，或者反思上一步的结果。</li><li><strong>Action (行动)：</strong> 这是智能体决定采取的具体动作，通常是调用一个外部工具，例如 <code>Search[&#39;华为最新款手机&#39;]</code>。</li><li><strong>Observation (观察)：</strong> 这是执行<code>Action</code>后从外部工具返回的结果，例如搜索结果的摘要或API的返回值。</li></ul><p>智能体将不断重复这个 <strong>Thought -&gt; Action -&gt; Observation</strong> 的循环，将新的观察结果追加到历史记录中，形成一个不断增长的上下文，直到它在<code>Thought</code>中认为已经找到了最终答案，然后输出结果。这个过程形成了一个强大的协同效应：<strong>推理使得行动更具目的性，而行动则为推理提供了事实依据。</strong></p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20260305203319.png"></p><h3 id="代码架构"><a href="#代码架构" class="headerlink" title="代码架构"></a>代码架构</h3><p>核心是<code>[工具，工具描述]</code>。</p><ul><li><strong>角色定义</strong>： “你是一个有能力调用外部工具的智能助手”，设定了LLM的角色。</li><li>**工具清单 (<code>&#123;tools&#125;</code>)**： 告知LLM它有哪些可用的“手脚”。</li><li>**格式规约 (<code>Thought</code>/<code>Action</code>)**： 这是最重要的部分，它强制LLM的输出具有结构性，使我们能通过代码精确解析其意图。</li><li>**动态上下文 (<code>&#123;question&#125;</code>/<code>&#123;history&#125;</code>)**： 将用户的原始问题和不断累积的交互历史注入，让LLM基于完整的上下文进行决策。每次的observation也会扔到上下文里。</li></ul><h3 id="优缺点、优化技巧☆"><a href="#优缺点、优化技巧☆" class="headerlink" title="优缺点、优化技巧☆"></a>优缺点、优化技巧☆</h3><p>优点：</p><ol><li>高可解释性：步骤透明。通过 <code>Thought</code> 链，能让我们清楚看到每一步AI的理解、操作。</li><li>动态规划和纠错能力：每一次都会把observation放到上下文，里面包含了外界新信息，因此可以动态调整&amp;修正搜索词。</li><li>工具协同能力：结构清晰，工具非常好写。</li></ol><p>缺点：</p><ol><li>非常依赖LLM能力：因为决策由LLM的<code>Thought</code>环节来决定，导致错误会产生错误的规划和错误的 Tool Function 的输入值。</li><li>执行效率：循序渐进，串行思考，导致每次都会多次调用LLM，消耗比较大。</li><li>脆弱性：提示词模板（工具的解释？）的改动会非常影响输出结果，导致变化会大。</li><li>陷入局部最优：步进式的决策模式，容易因为眼前的<code>observation</code>来决策，导致缺乏长远规划。</li></ol><p>优化技巧：</p><ol><li>检查完整提示词</li><li>分析原始输出</li><li>验证工具输入输出</li><li>调整提示词的示例</li><li>尝试不同模型或者参数</li></ol><h2 id="Plan-and-solve"><a href="#Plan-and-solve" class="headerlink" title="Plan and solve"></a>Plan and solve</h2><ol><li><strong>规划阶段 (Planning Phase)<strong>： 首先，智能体会接收用户的完整问题。它的第一个任务不是直接去解决问题或调用工具，而是</strong>将问题分解，并制定出一个清晰、分步骤的行动计划</strong>。这个计划本身就是一次大语言模型的调用产物。</li><li><strong>执行阶段 (Solving Phase)<strong>： 在获得完整的计划后，智能体进入执行阶段。它会</strong>严格按照计划中的步骤，逐一执行</strong>。每一步的执行都可能是一次独立的 LLM 调用，或者是对上一步结果的加工处理，直到计划中的所有步骤都完成，最终得出答案。</li></ol><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20260306180202.png"></p><h3 id="规划器"><a href="#规划器" class="headerlink" title="规划器"></a>规划器</h3><p>使用<code>prompt</code>来控制LLM输出一个plan，里面包含了所有执行步骤。</p><h3 id="执行器-amp-状态管理"><a href="#执行器-amp-状态管理" class="headerlink" title="执行器&amp;状态管理"></a>执行器&amp;状态管理</h3><p>执行器的目标不是分解问题，而是<strong>在已有上下文的基础上，专注解决当前这一个步骤</strong>。因此，<code>prompt</code> 需要包含以下关键信息：</p><ul><li><strong>原始问题</strong>： 确保模型始终了解最终目标。</li><li><strong>完整计划</strong>： 让模型了解当前步骤在整个任务中的位置。</li><li><strong>历史步骤与结果</strong>： 提供至今为止已经完成的工作，作为当前步骤的直接输入。</li><li><strong>当前步骤</strong>： 明确指示模型现在需要解决哪一个具体任务。</li></ul><p><strong>状态管理</strong>：指的是维护一个历史记录（状态），也就是上下文，慢慢添加。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 更新历史记录，为下一步做准备，在for循环里</span></span><br><span class="line">   history += <span class="string">f&quot;步骤 <span class="subst">&#123;i+<span class="number">1</span>&#125;</span>: <span class="subst">&#123;step&#125;</span>\n结果: <span class="subst">&#123;response_text&#125;</span>\n\n&quot;</span></span><br></pre></td></tr></table></figure><h2 id="Reflection"><a href="#Reflection" class="headerlink" title="Reflection"></a>Reflection</h2><p>为智能体引入一种 <strong>事后（post-hoc）的自我校正循环</strong>，使其能够审视自己的工作，发现不足，并进行<strong>自我迭代优化</strong>。核心工作流程：<strong>执行 -&gt; 反思 -&gt; 优化</strong> 。</p><ol><li>**执行 (Execution)**：首先，智能体使用我们熟悉的方法（如 ReAct 或 Plan-and-Solve）尝试完成任务，生成一个初步的解决方案或行动轨迹。这可以看作是“初稿”。</li><li><strong>反思 (Reflection)<strong>：接着，智能体进入反思阶段。它会调用一个独立的、或者带有特殊提示词的大语言模型实例，来扮演一个“</strong>评审员</strong>”的角色。这个“评审员”会审视第一步生成的“初稿”，并从多个维度进行评估，例如：<ul><li><strong>事实性错误</strong>：是否存在与常识或已知事实相悖的内容？</li><li><strong>逻辑漏洞</strong>：推理过程是否存在不连贯或矛盾之处？</li><li><strong>效率问题</strong>：是否有更直接、更简洁的路径来完成任务？</li><li><strong>遗漏信息</strong>：是否忽略了问题的某些关键约束或方面？ 根据评估，它会生成一段结构化的**反馈 (Feedback)**，指出具体的问题所在和改进建议。</li></ul></li><li>**优化 (Refinement)**：最后，智能体将“初稿”和“反馈”作为新的上下文，再次调用大语言模型，要求它根据反馈内容对初稿进行修正，生成一个更完善的“修订稿”。</li></ol><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20260306190540.png"></p><h3 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h3><ol><li><strong>初始执行提示词 (Execution Prompt)</strong> ：这是智能体首次尝试解决问题的提示词，内容相对直接，只要求模型完成指定任务。</li><li><strong>反思提示词 (Reflection Prompt)</strong> ：这个提示词是 Reflection 机制的灵魂。它指示模型扮演“代码评审员”的角色，对上一轮生成的代码进行批判性分析，并提供具体的、可操作的反馈。</li><li><strong>优化提示词 (Refinement Prompt)</strong> ：当收到反馈后，这个提示词将引导模型根据反馈内容，对原有代码进行修正和优化。</li></ol><h3 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h3><p><strong>优点：</strong></p><ol><li>解决方案质量提升：迭代，提升结果质量</li><li>鲁棒性和可靠性增强：因为有内部自我纠错循环</li></ol><p><strong>缺点：</strong></p><ol><li>开销增大：模型多次迭代，意味着需要花费更多的 token调用</li><li>任务延迟提高：因为reflection是串行运行的，多迭代多花时间</li><li>提示工程复杂度上升：需要对 “执行、反思、优化” 有不同的高效提示词</li></ol><h2 id="三者比较"><a href="#三者比较" class="headerlink" title="三者比较"></a>三者比较</h2><ol><li>ReAct: 通过“思考-行动-观察”的动态循环，它成功地利用搜索引擎回答了自身知识库无法覆盖的实时性问题。其核心优势在于<strong>环境适应性</strong>和<strong>动态纠错能力</strong>。</li><li>Plan-and-Solve: 它将复杂的任务分解为清晰的步骤，然后逐一执行。其核心优势在于<strong>结构性</strong>和<strong>稳定性</strong>，特别适合处理逻辑路径确定、内部推理密集的任务。</li><li>Reflection (自我反思与迭代): 其核心价值在于能<strong>显著提升解决方案的质量</strong>，适用于对结果的准确性和可靠性有极高要求的场景。</li></ol><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20260306202900.png"></p><h1 id="Agent框架"><a href="#Agent框架" class="headerlink" title="Agent框架"></a>Agent框架</h1><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/0b87602d5a69dc229032a81628c36754.png"></p><h2 id="使用场景总结"><a href="#使用场景总结" class="headerlink" title="使用场景总结"></a>使用场景总结</h2><ul><li><strong>深度协作和创造性思维：</strong>CAMEL 对于创作特别有用处，代码少提示多，重设计。</li><li><strong>严格流程控制</strong>：对于需要精确步骤控制的任务，LangGraph 的图结构更合适。</li><li><strong>大规模并发</strong>：AgentScope 的消息驱动架构在高并发场景下更有优势。</li><li><strong>复杂决策树</strong>：AutoGen 的群聊模式在多方决策场景下更加灵活。</li></ul><h2 id="AutoGen"><a href="#AutoGen" class="headerlink" title="AutoGen"></a>AutoGen</h2><h3 id="思路-amp-架构"><a href="#思路-amp-架构" class="headerlink" title="思路&amp;架构"></a>思路&amp;架构</h3><p>多个Agent扮演专家，自我对话式完成任务。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20260307124621.png"></p><h4 id="有不同角色"><a href="#有不同角色" class="headerlink" title="有不同角色"></a>有不同角色</h4><ul><li><strong>AssistantAgent (助理智能体)：</strong> 这是任务的主要解决者，其核心是封装了一个大型语言模型（LLM）。它的职责是根据对话历史生成富有逻辑和知识的回复，例如提出计划、撰写文章或编写代码。通过不同的系统消息（System Message），我们可以为其赋予不同的“专家”角色。</li><li><strong>UserProxyAgent (用户代理智能体)：</strong> 这是 AutoGen 中功能独特的组件。它扮演着双重角色：既是人类用户的“代言人”，负责发起任务和传达意图；又是一个可靠的“执行器”，可以配置为执行代码或调用工具，并将结果反馈给其他智能体。这种设计清晰地区分了“思考”（由 <code>AssistantAgent</code> 完成）与“行动”。</li></ul><h4 id="执行流程"><a href="#执行流程" class="headerlink" title="执行流程"></a>执行流程</h4><ul><li><strong>轮询群聊 (RoundRobinGroupChat)：</strong> 这是一种明确的、顺序化的对话协调机制。它会让参与的智能体按照预定义的顺序依次发言。这种模式非常适用于流程固定的任务，例如一个典型的软件开发流程：产品经理先提出需求，然后工程师编写代码，最后由代码审查员进行检查。</li><li><strong>工作流：</strong><ol><li>首先，创建一个 <code>RoundRobinGroupChat</code> 实例，并将所有参与协作的智能体（如产品经理、工程师等）加入其中。</li><li>当一个任务开始时，群聊会按照预设的顺序，依次激活相应的智能体。</li><li>被选中的智能体根据当前的对话上下文进行响应。</li><li>群聊将新的回复加入对话历史，并激活下一个智能体。</li><li>这个过程会持续进行，直到达到最大对话轮次或满足预设的终止条件。</li></ol></li></ul><h3 id="优缺点-1"><a href="#优缺点-1" class="headerlink" title="优缺点"></a>优缺点</h3><ol><li><p>易于设计，职责匹配的方式贴近人类社会</p></li><li><p>对于流程化任务，有清晰的执行步骤</p></li><li><p>LLM不确定性，可能陷入错误分支或者陷入循环。</p></li><li><p>整体串行，调试困难</p></li></ol><h2 id="AgentScope"><a href="#AgentScope" class="headerlink" title="AgentScope"></a>AgentScope</h2><h3 id="思路-amp-架构-1"><a href="#思路-amp-架构-1" class="headerlink" title="思路&amp;架构"></a>思路&amp;架构</h3><p> AgentScope 就是一个完整的”智能体操作系统”，为开发者提供了从开发、测试到部署的全生命周期支持。与许多框架采用的继承式设计不同，AgentScope 选择了<strong>组合式架构</strong>和<strong>消息驱动模式</strong>。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20260307141128.png"></p><h3 id="消息驱动"><a href="#消息驱动" class="headerlink" title="消息驱动"></a>消息驱动</h3><p>AgentScope 的核心创新在于其<strong>消息驱动架构</strong>。在这个架构中，所有的智能体交互都被抽象为<strong>消息</strong>的发送和接收，而不是传统的函数调用。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> agentscope.message <span class="keyword">import</span> Msg</span><br><span class="line"></span><br><span class="line"><span class="comment"># 消息的标准结构</span></span><br><span class="line">message = Msg(</span><br><span class="line">    name=<span class="string">&quot;Alice&quot;</span>,           <span class="comment"># 发送者名称</span></span><br><span class="line">    content=<span class="string">&quot;Hello, Bob!&quot;</span>,  <span class="comment"># 消息内容</span></span><br><span class="line">    role=<span class="string">&quot;user&quot;</span>,           <span class="comment"># 角色类型</span></span><br><span class="line">    metadata=&#123;             <span class="comment"># 元数据信息</span></span><br><span class="line">        <span class="string">&quot;timestamp&quot;</span>: <span class="string">&quot;2024-01-15T10:30:00Z&quot;</span>,</span><br><span class="line">        <span class="string">&quot;message_type&quot;</span>: <span class="string">&quot;text&quot;</span>,</span><br><span class="line">        <span class="string">&quot;priority&quot;</span>: <span class="string">&quot;normal&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>好处：</p><ul><li><strong>异步解耦</strong>: 消息的发送方和接收方在时间上解耦，无需相互等待，天然支持高并发场景。</li><li><strong>位置透明</strong>: 智能体无需关心另一个智能体是在本地进程还是在远程服务器上，消息系统会自动处理路由。</li><li><strong>可观测性</strong>: 每一条消息都可以被记录、追踪和分析，极大地简化了复杂系统的调试与监控。</li><li><strong>可靠性</strong>: 消息可以被持久化存储和重试，即使系统出现故障，也能保证交互的最终一致性，提升了系统的容错能力。</li></ul><h3 id="优缺点-2"><a href="#优缺点-2" class="headerlink" title="优缺点"></a>优缺点</h3><ol><li><p>消息机制，天然支持<strong>并行</strong>发布任务给所有Agent</p></li><li><p>消息机制，可视化好，便于留<strong>记录</strong></p></li><li><p>通信开销，分布式的消息传递机制导致会有网络延迟</p></li><li><p>消耗多，每次对话都有详细提示词、多次通信，token消耗快</p></li></ol><h2 id="CAMEL"><a href="#CAMEL" class="headerlink" title="CAMEL"></a>CAMEL</h2><p>与 AutoGen 和 AgentScope 这样功能全面的框架不同，CAMEL最初的核心目标是探索如何在最少的人类干预下，让两个智能体通过“角色扮演”自主协作解决复杂任务。</p><p>CAMEL 实现自主协作的基石是两大核心概念：<strong>角色扮演 (Role-Playing)</strong> 和 **引导性提示 (Inception Prompting)**。</p><p> <strong>引导性提示：</strong></p><ul><li>明确自身角色</li><li>告知协作者角色</li><li>定义共同目标</li><li><strong>设定行为约束和沟通协议</strong>：这是最关键的一环。例如，指令会要求 AI 用户“一次只提出一个清晰、具体的步骤”，并要求 AI 助理“在完成上一步之前不要追问更多细节”，同时规定双方需在回复的末尾使用特定标志（如 <code>&lt;SOLUTION&gt;</code>）来标识任务的完成。</li></ul><h3 id="优缺点-3"><a href="#优缺点-3" class="headerlink" title="优缺点"></a>优缺点</h3><ol><li><p>轻架构、重提示（代码设计少）</p></li><li><p>多模态工具支持</p></li><li><p>高度依赖提示工程</p></li><li><p>不善于搭建大规模多智能体工程</p></li></ol><h2 id="LangGraph"><a href="#LangGraph" class="headerlink" title="LangGraph"></a>LangGraph</h2><h3 id="思路-amp-架构-2"><a href="#思路-amp-架构-2" class="headerlink" title="思路&amp;架构"></a>思路&amp;架构</h3><p>LangGraph 将智能体的执行流程建模为一种<strong>状态机（State Machine）</strong>，并将其表示为<strong>有向图（Directed Graph）</strong>。在这种范式中，图的<strong>节点（Nodes）</strong>代表一个具体的计算步骤（如调用 LLM、执行工具），而 <strong>边（Edges）</strong>则定义了从一个节点到另一个节点的跳转逻辑。</p><p>状态节点：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 定义全局状态的数据结构</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AgentState</span>(<span class="params">TypedDict</span>):</span></span><br><span class="line">    messages: <span class="type">List</span>[<span class="built_in">str</span>]      <span class="comment"># 对话历史</span></span><br><span class="line">    current_task: <span class="built_in">str</span>        <span class="comment"># 当前任务</span></span><br><span class="line">    final_answer: <span class="built_in">str</span>        <span class="comment"># 最终答案</span></span><br><span class="line">    <span class="comment"># ... 任何其他需要追踪的状态</span></span><br></pre></td></tr></table></figure><p>有无条件边，这边举例一个<strong>有条件边</strong>：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 添加条件边，实现动态路由</span></span><br><span class="line">workflow.add_conditional_edges(</span><br><span class="line">    <span class="comment"># 起始节点</span></span><br><span class="line">    <span class="string">&quot;executor&quot;</span>,</span><br><span class="line">    <span class="comment"># 判断函数</span></span><br><span class="line">    should_continue,</span><br><span class="line">    <span class="comment"># 路由映射：将判断函数的返回值映射到目标节点</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="string">&quot;continue_to_planner&quot;</span>: <span class="string">&quot;planner&quot;</span>, <span class="comment"># 如果返回&quot;continue_to_planner&quot;，则跳回planner节点</span></span><br><span class="line">        <span class="string">&quot;end_workflow&quot;</span>: END               <span class="comment"># 如果返回&quot;end_workflow&quot;，则结束流程</span></span><br><span class="line">    &#125;</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 判断函数</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">should_continue</span>(<span class="params">state: AgentState</span>) -&gt; <span class="built_in">str</span>:</span></span><br><span class="line">    <span class="string">&quot;&quot;&quot;条件函数：根据状态决定下一步路由。&quot;&quot;&quot;</span></span><br><span class="line">    <span class="comment"># 假设如果消息少于3条，则需要继续规划</span></span><br><span class="line">    <span class="keyword">if</span> <span class="built_in">len</span>(state[<span class="string">&quot;messages&quot;</span>]) &lt; <span class="number">3</span>:</span><br><span class="line">        <span class="comment"># 返回的字符串需要与添加条件边时定义的键匹配</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;continue_to_planner&quot;</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        state[<span class="string">&quot;final_answer&quot;</span>] = state[<span class="string">&quot;messages&quot;</span>][-<span class="number">1</span>]</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;end_workflow&quot;</span></span><br></pre></td></tr></table></figure><h3 id="优缺点-4"><a href="#优缺点-4" class="headerlink" title="优缺点"></a>优缺点</h3><ol><li><p>代码架构，高度可控。</p></li><li><p>使用状态机图，可预测、可设置路径，对于需要严格按照串行实现的工作（比如审计）会非常适合。</p></li><li><p>对于**循环(Cycles)**有原生支持，比如可以通过条件边来构建“反思-修正”循环。</p></li><li><p>需要事先写好结点，可控但缺少涌现式的交互，就是不太柔软。</p></li><li><p>某个结点数据出错误时会造成全部出错。</p></li></ol><h1 id="Hello-Agent框架"><a href="#Hello-Agent框架" class="headerlink" title="Hello Agent框架"></a>Hello Agent框架</h1><h2 id="架构"><a href="#架构" class="headerlink" title="架构"></a>架构</h2><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line">hello-agents/</span><br><span class="line">├── hello_agents/</span><br><span class="line">│   │</span><br><span class="line">│   ├── core/                     <span class="comment"># 核心框架层</span></span><br><span class="line">│   │   ├── agent.py              <span class="comment"># Agent基类</span></span><br><span class="line">│   │   ├── llm.py                <span class="comment"># HelloAgentsLLM统一接口</span></span><br><span class="line">│   │   ├── message.py            <span class="comment"># 消息系统</span></span><br><span class="line">│   │   ├── config.py             <span class="comment"># 配置管理</span></span><br><span class="line">│   │   └── exceptions.py         <span class="comment"># 异常体系</span></span><br><span class="line">│   │</span><br><span class="line">│   ├── agents/                   <span class="comment"># Agent实现层</span></span><br><span class="line">│   │   ├── simple_agent.py       <span class="comment"># SimpleAgent实现</span></span><br><span class="line">│   │   ├── react_agent.py        <span class="comment"># ReActAgent实现</span></span><br><span class="line">│   │   ├── reflection_agent.py   <span class="comment"># ReflectionAgent实现</span></span><br><span class="line">│   │   └── plan_solve_agent.py   <span class="comment"># PlanAndSolveAgent实现</span></span><br><span class="line">│   │</span><br><span class="line">│   ├── tools/                    <span class="comment"># 工具系统层</span></span><br><span class="line">│   │   ├── base.py               <span class="comment"># 工具基类</span></span><br><span class="line">│   │   ├── registry.py           <span class="comment"># 工具注册机制</span></span><br><span class="line">│   │   ├── chain.py              <span class="comment"># 工具链管理系统</span></span><br><span class="line">│   │   ├── async_executor.py     <span class="comment"># 异步工具执行器</span></span><br><span class="line">│   │   └── builtin/              <span class="comment"># 内置工具集</span></span><br><span class="line">│   │       ├── calculator.py     <span class="comment"># 计算工具</span></span><br><span class="line">│   │       └── search.py         <span class="comment"># 搜索工具</span></span><br><span class="line">└──</span><br></pre></td></tr></table></figure><h2 id="自动检测供应商"><a href="#自动检测供应商" class="headerlink" title="自动检测供应商"></a>自动检测供应商</h2><ol><li>最优先：框架会依次检查 <code>MODELSCOPE_API_KEY</code>, <code>OPENAI_API_KEY</code>, <code>ZHIPU_API_KEY</code> 等环境变量是否存在。一旦发现任何一个，就会立即确定对应的服务商。</li><li>次优先：如果用户没有设置特定服务商的密钥，但设置了通用的 <code>LLM_BASE_URL</code>，框架会转而解析这个 URL。</li><li>辅助判断：如果上述两种方式都无法确定，框架会尝试分析通用环境变量 <code>LLM_API_KEY</code> 的格式。</li></ol><h2 id="框架接口"><a href="#框架接口" class="headerlink" title="框架接口"></a>框架接口</h2><h3 id="Message类"><a href="#Message类" class="headerlink" title="Message类"></a>Message类</h3><p>类似于阿里的<code>AgentScope</code>框架，也做了Msg来进行通讯，其实就是封装下<code>role</code>和<code>content</code>，然后加了个<code>stamp</code>时间戳。</p><p>核心还是要理解OpenAI的API标准是：<code>&#123; role:str, content:str &#125;</code></p><h3 id="Config类"><a href="#Config类" class="headerlink" title="Config类"></a>Config类</h3><p>把全局静态变量（比如环境变量）那些全部扔到里面统一管理。</p><h3 id="Agent抽象基类"><a href="#Agent抽象基类" class="headerlink" title="Agent抽象基类"></a>Agent抽象基类</h3><ol><li><strong>构建函数</strong>里把核心都带进来：名称、LLM 实例、系统提示词和配置</li><li>强制要求实现<strong>run方法</strong></li><li>历史记录<code>history</code>的初始化，以及一些基础方法：clear、append、copy</li></ol><h3 id="自定义的Agent类"><a href="#自定义的Agent类" class="headerlink" title="自定义的Agent类"></a>自定义的Agent类</h3><p>继承抽象Agent基类后，在run方法里实现</p><ol><li>历史消息添加</li><li>while循环，带着工具列表和上下文，多次调用LLM<ol><li>在循环里，工具调用之后的结果也会加入上下文内容</li><li>如果已经没有工具调用了，或者 超过最大迭代次数，就代表回答结束。</li></ol></li><li>保存上面的长串message到历史记录</li></ol><h3 id="工具管理类"><a href="#工具管理类" class="headerlink" title="工具管理类"></a>工具管理类</h3><p>有注册Tool功能等，本质就是个字典 + 一堆方法。</p><h2 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h2><p>可以直接用这个基础框架，优化一下prompt、实现ReAct等不同范式。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;Agent-经典范式&quot;&gt;&lt;a href=&quot;#Agent-经典范式&quot; class=&quot;headerlink&quot; title=&quot;Agent 经典范式&quot;&gt;&lt;/a&gt;Agent 经典范式&lt;/h1&gt;&lt;p&gt;为了更好地组织智能体的“思考”与“行动”过程，业界涌现出了多种经典的架构范式</summary>
      
    
    
    
    <category term="AI" scheme="https://codingcodingk.github.io/categories/AI/"/>
    
    <category term="Hello Agents" scheme="https://codingcodingk.github.io/categories/AI/Hello-Agents/"/>
    
    
    <category term="AI" scheme="https://codingcodingk.github.io/tags/AI/"/>
    
    <category term="AI Agent" scheme="https://codingcodingk.github.io/tags/AI-Agent/"/>
    
  </entry>
  
  <entry>
    <title>AB包基础</title>
    <link href="https://codingcodingk.github.io/Tech/Game/tools/assetbundleInit/"/>
    <id>https://codingcodingk.github.io/Tech/Game/tools/assetbundleInit/</id>
    <published>2026-03-05T04:37:56.980Z</published>
    <updated>2026-03-05T04:37:56.980Z</updated>
    
    <content type="html"><![CDATA[<p>2023年6月更新：已完成学习并实现huatuo+yooasset+uniTask的demo，下一步是接入自己的ui框架并迭代，地址：<a href="https://github.com/CodingCodingK/CodingK_SutureFramework">https://github.com/CodingCodingK/CodingK_SutureFramework</a></p><p>=================================================================================</p><p>学习计划：（我感觉用脑图好像比md更适合这种架构的学习和复习）</p><p>什么是热更 <a href="https://www.bilibili.com/video/BV1Np4y1e7DX/">https://www.bilibili.com/video/BV1Np4y1e7DX/</a></p><p>AB入门 <a href="https://www.bilibili.com/video/BV1LD4y1m7kF">https://www.bilibili.com/video/BV1LD4y1m7kF</a></p><p>AB实战 <a href="https://www.bilibili.com/video/BV1VP411G7w2/">https://www.bilibili.com/video/BV1VP411G7w2/</a></p><p>AA包 <a href="https://blog.csdn.net/u011366226/article/details/104506802">https://blog.csdn.net/u011366226/article/details/104506802</a></p><p>UniTask <a href="https://www.lfzxb.top/unitask_reademe_cn/">https://www.lfzxb.top/unitask_reademe_cn/</a></p><p>做出自己的学习版demo，服务器上跑一下，测试后上传github。</p><p>后面开始学习yooasset+huatuo方案，学习整合成自己能用的插件。如果资源管理不完整或者太难理解可以再参考gf。</p><p>等做完对资源有划分理解后，我们再开始做战斗，学习结点吧。</p><p>如果实现完还有时间，就用xLua重写c#热更项目。</p><h1 id="AB包入门"><a href="#AB包入门" class="headerlink" title="AB包入门"></a>AB包入门</h1><p>什么是AB包：资产压缩包，资产包括：模型、贴图、预设体、音效、材质球等。</p><p>AB包和Resources区别：Resources打包时定死，只读，无法修改。所以AB包的优势就是可压缩资源、可热更新。</p><p>热更基本规则：双端都带有一个资源对比文件，客户端检查热更就是检查资源对比文件。</p><p>可生成AB包资源文件的资源管理系统：</p><p>1.可以自定义打包工具，AB包的话gf和yooasset都是不错的开源插件。</p><p>2.官方在过去提供的AB包管理系统插件，叫Asset Bundle Browser，但在如今已经弃用了。</p><p>3.官方现在推崇的是Addressables了，它可以打AA包也可以打AB包。</p><p>AB包和AA包的区别：</p><ul><li>Addressable Asset Bundle可以自动加载相关依赖包，而AssetBundle是需要编程实现的；</li><li>Addressable Asset Bundle加载到内存中的bundle有引用计数，而AssetBundle加载到内存中的bundle需要自己进行管理</li></ul><h1 id="yooAsset"><a href="#yooAsset" class="headerlink" title="yooAsset"></a>yooAsset</h1><p>手册：<a href="https://www.yooasset.com/docs">https://www.yooasset.com/docs</a></p><p>教程：<a href="https://www.bilibili.com/video/BV1do4y1v7Pb/">https://www.bilibili.com/video/BV1do4y1v7Pb/</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;2023年6月更新：已完成学习并实现huatuo+yooasset+uniTask的demo，下一步是接入自己的ui框架并迭代，地址：&lt;a href=&quot;https://github.com/CodingCodingK/CodingK_SutureFramework&quot;&gt;htt</summary>
      
    
    
    
    <category term="Game" scheme="https://codingcodingk.github.io/categories/Game/"/>
    
    
    <category term="工具组" scheme="https://codingcodingk.github.io/tags/%E5%B7%A5%E5%85%B7%E7%BB%84/"/>
    
    <category term="网络库" scheme="https://codingcodingk.github.io/tags/%E7%BD%91%E7%BB%9C%E5%BA%93/"/>
    
  </entry>
  
  <entry>
    <title>UGUI学习 - 合批规则</title>
    <link href="https://codingcodingk.github.io/Tech/Game/UI/ugui_batch/"/>
    <id>https://codingcodingk.github.io/Tech/Game/UI/ugui_batch/</id>
    <published>2026-03-05T04:37:56.978Z</published>
    <updated>2026-03-05T04:37:56.978Z</updated>
    
    <content type="html"><![CDATA[<p><strong>1.抛开UGUI的概念，什么是drawcall？什么是批处理？他俩有啥关系？</strong></p><p>CPU告诉GPU“可以渲染这个模型”的过程或者说这个命令叫做Draw Call（我们在Stats面板上看到的Batches其实就是Draw Call的调用次数）。在NGUI里甚至有UIDrawCall这个具体的类去抽象一次drawcall。</p><p>批处理（UGUI里类似的行为叫合批）就是把渲染时使用相同材质(Shader)、相同贴图的3D模型的网格合并在一起，成为一个大网格，然后再调用一次Draw Call，直接渲染这一个大网格。</p><p>关系就是，批处理是降低CPU发起drawcall次数的手段。</p><p><strong>2.Unity中我怎么直观的去检查它？</strong></p><p>frame debugger可以查看一帧整体的绘制流程，从而观察到UGUI层调了多少次Draw Mesh、每次是怎么样的。</p><p>profiler可以更直接的看到一个Canvas下的batch 0、batch 1…</p><p><strong>3.基础的UGUI合批规则</strong></p><p>无论是UGUI还是NGUI，都以一个基础的合批规则：当前渲染的UI组件与上一个UI组件的material+shader（也就是材质球）、texture（贴图）都需要一致。满足了这个最基本要求才能合批。</p><p><strong>4.进阶的UGUI合批规则</strong></p><p>首先我们要明确UGUI中Canvas下可以嵌套子Canvas，但是合批是以Canvas(不包含子Canvas)为单位的(子Canvas会是另外一个批次了)。除此之外，合批的操作是在子线程完成的。这段的理解要结合图，<a href="https://blog.csdn.net/sinat_25415095/article/details/112388638">详见</a>。<br>① 既然合批是以Canvas为单位，第一步自然就是把所有Canvas给找出来，然后剔除掉不必渲染的Canvas(透明度为0，长宽为0，在RectMask2D控件下，且在RectMask2D的区域外)<br>② 然后计算Canvas下各UI控件的深度值Depth(需要注意的是Image的属性里面也有个depth，两者不是同一个东西)</p><p>③Depth的计算规则如下：</p><p>按照Hierarchy中从上往下的顺序依次遍历Canvas下所有UI元素<br>对于当前的UI元素CurrentUI<br>i.如果CurrentUI不渲染，则Depth = -1<br>ii.如果CurrentUI要渲染，但CurrentUI下面没有其他UI元素与其相交，则Depth = 0<br>iii.如果CurrentUI要渲染，下面只有一个UI元素(LowerUI)与其相交，且CurrentUI与LowerUI可以合批(材质和贴图完全相同)，则CurrentUI.Depth = LowerUI.Depth；如果两者不能合批，CurrentUI.Depth= LowerUI.Depth + 1<br>iv.如果CurrentUI要渲染，下面有n个元素与其相交，则按照步骤iii，分别计算出n个Depth(Depth_1、Depth_2、Depth_3…)，然后CurrentUI.Depth取其最大值，即CurrentUI.Depth = max(Depth_1, Depth_2, Depth_3，…)<br>上面步骤中的“下面”和“相交”要明确下意思，这两个概念很重要。<br>CurrentUI下面的UI，指Hierarchy面板中，在CurrentUI之上的元素。</p><p>在计算相交时，由于要遍历所有UI元素和已计算的底层UI元素（平方复杂度），源码中使用<strong>分组计算包围盒矩形的方法加快计算，即16个UI元素为一组计算Group 网格Rect</strong>，检查是否与底层UI元素相交时，先计算是否与底层Group相交，如果相交再与Group中的元素做判定。<br>④各个UI的Depth计算完毕后，依次按照Depth、material ID、texture ID、RendererOrder（即UI层级队列顺序，即Hierarchy面板上的顺序）排序（条件的优先级依次递减，且均为从小到大排序）。然后剔除Depth = -1的UI元素，得到Batch前的UI 元素队列，这个队列被称之为VisiableList。<br>⑤得到VisiableList之后，判断VisiableList中相邻的元素是否能够合批(相同的材质和贴图)。需要注意这里不再考虑Depth是否相同，只要两个元素相邻然后材质和贴图相同，即使两个元素的Depth不相同，这两个元素也能合批。然后一个批次一个批次的合并网格，提交GPU进行渲染。</p><p>除此之外，需要注意的是，合批是将同一Canvas下多个UI的网格合并在一起，如果其中任何一个元素的材质、网格顶点、位置(Transform)甚至颜色或者在该Canvas下动态创建或删除UI元素都将导致该Canvas重新计算合批(需要注意的是仅仅会影响这一个Canvas，子Canvas或父Canvas以及其他Canvas不会重新计算)，重新生成新的网格，这个重新计算生成网格的过程被称为rebuild。所以，这也是为什么做UI提倡动静分离(动态部分和静态部分分别用不同的Canvas)，层级尽量减少(层级多了，重新计算更耗时)的原因。</p><p><strong>5.总结合批规则</strong></p><p>合批是自己Canvas内部的规则，排序规则：计算得到的Depth &gt; material ID &gt; texture ID &gt; RendererOrder。计算Depth时会16个UI元素为一组最为包围盒检测，计算Depth和Mesh（UGUI本质）是否相交也有关。会剔除透明度为0、长宽为0、在RectMask2D下在Mask外的UI组件。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;&lt;strong&gt;1.抛开UGUI的概念，什么是drawcall？什么是批处理？他俩有啥关系？&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;CPU告诉GPU“可以渲染这个模型”的过程或者说这个命令叫做Draw Call（我们在Stats面板上看到的Batches其实就是Draw Call</summary>
      
    
    
    
    <category term="Game" scheme="https://codingcodingk.github.io/categories/Game/"/>
    
    <category term="UI" scheme="https://codingcodingk.github.io/categories/Game/UI/"/>
    
    
    <category term="Unity" scheme="https://codingcodingk.github.io/tags/Unity/"/>
    
  </entry>
  
  <entry>
    <title>书籍笔记 - 程序是怎样跑起来的（硬件软件交互理解）</title>
    <link href="https://codingcodingk.github.io/Tech/BookNote/bookNote_howProgramRun/"/>
    <id>https://codingcodingk.github.io/Tech/BookNote/bookNote_howProgramRun/</id>
    <published>2026-03-05T04:37:56.976Z</published>
    <updated>2026-03-05T04:37:56.976Z</updated>
    
    <content type="html"><![CDATA[<h1 id="CPU"><a href="#CPU" class="headerlink" title="CPU"></a>CPU</h1><h2 id="CPU可直接执行的“机器码”，到底能处理什么？"><a href="#CPU可直接执行的“机器码”，到底能处理什么？" class="headerlink" title="CPU可直接执行的“机器码”，到底能处理什么？"></a>CPU可直接执行的“机器码”，到底能处理什么？</h2><p>简单来说分为4种，</p><p>数据传送指令：用于寄存器、内存、外围设备之间进行数据读写的操作。也就是<strong>读写操作</strong>。</p><p>运算指令：用累加寄存器执行<strong>算术运算、逻辑运算、比较运算和位移运算</strong>。</p><p>跳转指令：实现条件分支、循环、强制跳转等。其实就是个<strong>goto</strong>。</p><p>call/return指令：函数的调用 / 返回调用前的地址。和c#的函数<strong>栈桢</strong>展开是类似的。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20221026103246.png"></p><h1 id="二进制数"><a href="#二进制数" class="headerlink" title="二进制数"></a>二进制数</h1><h2 id="如何在硬件层面理解二进制、bit、byte？"><a href="#如何在硬件层面理解二进制、bit、byte？" class="headerlink" title="如何在硬件层面理解二进制、bit、byte？"></a>如何在硬件层面理解二进制、bit、byte？</h2><p>像计算机的CPU和内存都是IC的一种，IC就是有很多引脚的集成电路，而每一个引脚的状态只有直流电压0V和5V，这两种。因此计算机的信息只能转化成二进制。</p><p>而**位(bit)**这个数据结构，就刚好对应了这个规律。</p><p>**字节(byte)**则是8个bit，因为内存和磁盘都是用字节为最小单位来存储、读写数据的，所以用位bit不能直接读写数据，所以都会在高位补0实现bit转byte。</p><p>而一个CPU有32位，就意味着它有32个引脚来处理信息的输入和输出，也就是说一次可以处理32位的二进制数信息。</p><h2 id="为什么计算机内用补码来存数？（比如-1是11111111而不是10000001）"><a href="#为什么计算机内用补码来存数？（比如-1是11111111而不是10000001）" class="headerlink" title="为什么计算机内用补码来存数？（比如-1是11111111而不是10000001）"></a>为什么计算机内用补码来存数？（比如-1是11111111而不是10000001）</h2><p>为了实现通过相加来实现减法。</p><p>补码是取反+1的结果，同样的这个结果和无符号位的原码相加结果为0。</p><h1 id="小数"><a href="#小数" class="headerlink" title="小数"></a>小数</h1><h2 id="什么是浮点数？它是怎么表示小数的？"><a href="#什么是浮点数？它是怎么表示小数的？" class="headerlink" title="什么是浮点数？它是怎么表示小数的？"></a>什么是浮点数？它是怎么表示小数的？</h2><p><strong>什么是浮点数？</strong>是使用<strong>符号、尾数、基数、指数</strong>这四个部分来表示小数的，形式为<font color= "red"><code>± m * n的e次方</code></font >。符号就是±，基数n因为计算机二进制所以是2。而至于尾数m和指数n，就是根据具体数字来的，但也并不是毫无参照。</p><p><strong>它是怎么表示小数的？</strong></p><p>首先，分为单、双精度浮点数，它们表示范围不同，根据最为普遍的IEEE标准，单精浮点（32位）有1位符号部分、8位指数部分、23位尾数部分；双精浮点（64位）有1位符号部分、11位指数部分、52位尾数部分。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20221018103720.png"></p><p>其次，对于尾数m，就像科学计数法一样，浮点数也有一种表示规则来限制诸如<code>0.75 * 10^0</code>、<code>7.5 * 10^-1</code>这种同一个数字，表示方式却不同的情况。在二进制数中，用的是<strong>“将小数点前面的值固定为1的正则表达式”</strong>。</p><p>比如<code>1011.0011</code> =&gt; <code>0001.0110011</code> =&gt;第一位固定1所以可以去掉，多表示1位 <code>0110011000...(省略0，补齐23/52位)</code></p><p>最后，对于指数e，用的是<strong>EXCESS系统</strong>来表示它的正负。比如单精指数部分只有8位的情况下，最大值<code>11111111</code>=255，用它的一半<code>01111111</code>=127来记作0，比它大的是正数，比它小的是负数。指数e的表示范围从0<del>255变为-127</del>128。</p><h2 id="浮点数会不准确"><a href="#浮点数会不准确" class="headerlink" title="浮点数会不准确"></a>浮点数会不准确</h2><p><strong>为什么浮点数会不准确？</strong>计算机用浮点数无法处理0.1这种转换为二进制会无尽的小数。所以会导致0.1打印输出会变成0.10000002之类的。</p><p><strong>如何解决？</strong>一般可以不解决，因为科学近似精度已经足够高了。但如果是一些严格要求准确的领域，</p><p>方法1.可以把小数倍乘成整数来计算，计算完后再除回去。</p><p>方法2.可以使用BCD(Binary Coded Decimal)表示法，就是用4位来表示0~9的1位数字。</p><h2 id="浮点数c-实践"><a href="#浮点数c-实践" class="headerlink" title="浮点数c#实践"></a>浮点数c#实践</h2><p>在c#中尝试把一个float用二进制输出出来，比如<code>0.75f</code>，输出的值确实是按照1-8-23划分的：<code>0-01111110-10000000000000000000000</code>。但是结果是反过来的，不清楚为什么，书中用的c++是正序的。另外就是，用<code>BitConverter.GetBytes</code>接口转的是byte，也就是8位2进制转成10进制读得的数值，所以还需要再转回8位2进制数。</p><h1 id="内存"><a href="#内存" class="headerlink" title="内存"></a>内存</h1><h2 id="如何理解内存硬件"><a href="#如何理解内存硬件" class="headerlink" title="如何理解内存硬件"></a>如何理解内存硬件</h2><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20221020085834.png"></p><p>比如一个内存，其实就是一堆引脚。VCC、GND引脚是接电源的，DR、WR是控制信号用来操控读、写。</p><p>A0-A9的引脚用来表示地址，所以可以表示1024个地址；D0-D7表示数据，8位2进制，也就是1个字节B。所以这个内存的容量为1KB。</p><p>一个内存内部有大量空间去存储这些8位数据，通过地址引脚指定的地址来读写这些数据。</p><h2 id="内存地址"><a href="#内存地址" class="headerlink" title="内存地址"></a>内存地址</h2><p>1个地址指向1个字节（内存的最小单位）；</p><p>32位系统一次可以读4个字节，等于其最高可表示2^32个地址数。</p><p>所以32位系统，内存上限为4GB。</p><h1 id="内存与磁盘"><a href="#内存与磁盘" class="headerlink" title="内存与磁盘"></a>内存与磁盘</h1><h2 id="CPU、内存、磁盘"><a href="#CPU、内存、磁盘" class="headerlink" title="CPU、内存、磁盘"></a>CPU、内存、磁盘</h2><p>1.程序保存在存储设备中，通过有序地被读出来实现运行。</p><p>2.CPU只能执行已经加载到内存中的程序。</p><h2 id="如何理解磁盘硬件"><a href="#如何理解磁盘硬件" class="headerlink" title="如何理解磁盘硬件"></a>如何理解磁盘硬件</h2><p>磁盘是用扇区作为读写最小物理单位的，一个扇区为512字节。但是Windows在逻辑方面对磁盘进行读写的单位是<strong>簇</strong>，1簇可以设置为扇区整数倍，比如可以1簇=1扇区（512字节）、也可以1簇=2扇区（1KB）等等。</p><p>无论是多么小的文件，都会占用至少1簇。</p><h2 id="磁盘缓存"><a href="#磁盘缓存" class="headerlink" title="磁盘缓存"></a>磁盘缓存</h2><p>磁盘访问速度 &lt; 内存访问速度，所以会用内存映射一段磁盘数据作为缓存，如果下一次还用到的磁盘数据刚好被内存加载过，那就直接去速度更快的内存里拿数据。这叫<strong>缓存</strong>。到win98时代，由于磁盘访问速度越来越快，这种技术的提升就不是很显著了，但是这个思想也用于Web把远端服务器加载下来的数据存入磁盘中。</p><h2 id="虚拟内存"><a href="#虚拟内存" class="headerlink" title="虚拟内存"></a>虚拟内存</h2><p>正好与磁盘缓存相对的概念，就是说划分一块磁盘区域作为假想的内存来使用。但CPU只能执行已经加载到内存中的程序，所以实际上是在把内存和磁盘中的内容不停进行置换，保持当前正在运行的程序部分有在物理内存中。而置换就涉及一个最小单位，Windows是用<strong>页</strong>为单位（一般4KB）。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20221020142308.png"></p><h1 id="压缩"><a href="#压缩" class="headerlink" title="压缩"></a>压缩</h1><p>就只列举一些常见的压缩算法。</p><p>文本文件：RLE压缩（文本 x 次数 的形式）、哈夫曼（权重画树、编码）压缩</p><p>图像文件：可逆压缩、不可逆压缩。</p><h1 id="虚拟机"><a href="#虚拟机" class="headerlink" title="虚拟机"></a>虚拟机</h1><h2 id="虚拟机是什么"><a href="#虚拟机是什么" class="headerlink" title="虚拟机是什么"></a>虚拟机是什么</h2><p>因为应用是针对操作系统OS的，所以Windows应用只能在Windows系统上运行。而虚拟机就是在其他OS上，模拟Windows运行环境，来实现跨平台。</p><h2 id="其他跨平台方式"><a href="#其他跨平台方式" class="headerlink" title="其他跨平台方式"></a>其他跨平台方式</h2><p>除了虚拟机之外，还有两个方式。一个是<strong>Ports机制</strong>，是移植的意思。该机制能够结合当前运行的硬件环境来编译应用的源代码。</p><p>另一个是Java虚拟机，它其实是一种运行环境。Java在编译之后生成的不是机器码而是<strong>字节代码</strong>，然后字节代码在不同平台实现的Java虚拟机上运行，转换成本机代码。和c#一样。</p><h1 id="编译"><a href="#编译" class="headerlink" title="编译"></a>编译</h1><h2 id="从程序到机器码-编译器"><a href="#从程序到机器码-编译器" class="headerlink" title="从程序到机器码 - 编译器"></a>从程序到机器码 - 编译器</h2><p><strong>编译器</strong>把源代码转换成本地代码（本机代码），它内部仿佛有一个 源代码-本机码 的对应表，但实际上读入的源代码还需要经过<strong>语法解析、句法解析、语义解析</strong>等，才能生成本地代码。</p><p>编译器本身也是一个程序，所以也需要<strong>运行环境</strong>（比如Windows）。根据<strong>输出的目标CPU类型、输入的高级语言</strong>的不同，编译器也不同（比如Windows可用的x86系CPU的C编译器）。</p><h2 id="生成EXE-链接器"><a href="#生成EXE-链接器" class="headerlink" title="生成EXE - 链接器"></a>生成EXE - 链接器</h2><p>编译后的机器代码是一个obj文件（里面都是2进制数），并不能直接运行。还需要把代码中的引用其他文件的部分、和obj文件结合，生成出一个EXE文件。这就是<strong>链接</strong>。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20221025144538.png"></p><h2 id="EXE启动-分配内存"><a href="#EXE启动-分配内存" class="headerlink" title="EXE启动 - 分配内存"></a>EXE启动 - 分配内存</h2><p>一个程序经过编译+链接后，生成了可执行文件EXE，但是程序源码里对函数、变量的调用都是指向内存地址的，但一个EXE文件并没有给它分配内存地址，怎么办呢？</p><p>其实，EXE文件中给变量、函数分配了虚拟的内存地址，当EXE运行时会把虚拟的内存地址转换成实际的内存地址。链接器在EXE文件的开头，追加转换内存地址所需的必要信息，这个叫做<strong>再配置信息</strong>。</p><p>当EXE运行时，还会分配额外的2块连续内存，一个是栈、一个是堆。栈用来存储函数内部临时变量，堆用来存储运行时数据以及对象。栈的内存管理代码是编译器自动生成的，而堆的内存管理是需要人为编写的，c++中用new申请、delete来释放。这就是GC。</p><h2 id="常见编译问题"><a href="#常见编译问题" class="headerlink" title="常见编译问题"></a>常见编译问题</h2><h3 id="什么是编译器、什么是解释器？"><a href="#什么是编译器、什么是解释器？" class="headerlink" title="什么是编译器、什么是解释器？"></a>什么是编译器、什么是解释器？</h3><p>两者都是把高级语言转化为机器语言的程序。编译器再运行前把源码转换成二进制可执行文件，而解释器逐条读取源码转换成机器码。</p><h3 id="Build是什么意思？"><a href="#Build是什么意思？" class="headerlink" title="Build是什么意思？"></a>Build是什么意思？</h3><p>编译+链接</p><h3 id="DLL可以不用链接就调用到吗？"><a href="#DLL可以不用链接就调用到吗？" class="headerlink" title="DLL可以不用链接就调用到吗？"></a>DLL可以不用链接就调用到吗？</h3><p>可以，通过<code>LoadLibrary()</code>、<code>GetProcAddress()</code>这些API，即使不链接导入库，也可以在程序运行时调用DLL文件中的函数。</p><h1 id="通过汇编理解程序"><a href="#通过汇编理解程序" class="headerlink" title="通过汇编理解程序"></a>通过汇编理解程序</h1><h2 id="什么是汇编语言？"><a href="#什么是汇编语言？" class="headerlink" title="什么是汇编语言？"></a>什么是汇编语言？</h2><p>汇编语言和本地代码是一一对应的。汇编语言是本地代码（二进制）的助记符。</p><h2 id="汇编语言怎么看？"><a href="#汇编语言怎么看？" class="headerlink" title="汇编语言怎么看？"></a>汇编语言怎么看？</h2><p><strong>伪指令</strong>：负责把程序的构造以及汇编的方法指示给汇编器。伪指令<code>proc</code>和<code>endp</code>围起来的部分，表示的是函数的范围，里面的是函数执行过程。</p><p><strong>语法结构</strong>：操作码+操作数，比如<code>mov</code>+内存地址</p><p>以下是x86的部分操作码、主要寄存器：</p><table><thead><tr><th>操作码</th><th>操作数</th><th>功能</th></tr></thead><tbody><tr><td>mov</td><td>A、B</td><td>把B的值赋给A</td></tr><tr><td>and</td><td>A、B</td><td>把A同B的值相加，并将结果赋给A</td></tr><tr><td>push</td><td>A</td><td>把A的值存储在栈中</td></tr><tr><td>pop</td><td>A</td><td>从栈中读取出值，并将其赋给A</td></tr><tr><td>call</td><td>A</td><td>调用函数A，<font color="red">调用时自动获取栈上对应数量的入参</font></td></tr><tr><td>ret</td><td>无</td><td>将处理返回到函数的调用源</td></tr></tbody></table><p><strong>调用函数流程</strong>：</p><p>1.先把ebp寄存器（数据领域基址寄存器）的值入栈，再把函数的入参push入栈。</p><p>//注意，栈的高位地址在下面，低位地址在上面。</p><p>2.使用<code>call</code>指令 + 函数名（函数指针），调用时自动获取栈上对应数量的入参。</p><p>3.清理栈，把入参销毁掉。具体是把esp的寄存器值（栈顶指针寄存器）add上 4 * 入参数。</p><p>//至于为什么是4字节，因为堆栈进行数值的输入输出时，数值的单位是4字节。</p><p>4.<code>pop</code>读出此时栈顶的数值，也就是1中存储的ebp寄存器值，存入ebp寄存器。执行<code>ret</code>返回。</p><p>//为什么把edp寄存器的值原地倒腾了一遍？因为这是c语言的规定，确保函数调用前后edp不变。</p><p><strong>此外.函数的参数是通过栈来传递，返回值是通过寄存器返回的。</strong></p><h2 id="汇编实现功能"><a href="#汇编实现功能" class="headerlink" title="汇编实现功能"></a>汇编实现功能</h2><h3 id="循环"><a href="#循环" class="headerlink" title="循环"></a>循环</h3><p>使用标记 + goto标记 的 思路去做，具体助记符就不记了（先cmp再jmp）。</p><h3 id="条件分支"><a href="#条件分支" class="headerlink" title="条件分支"></a>条件分支</h3><p>一样是标记 + go标记去实现的。</p><h3 id="多线程问题"><a href="#多线程问题" class="headerlink" title="多线程问题"></a>多线程问题</h3><p>写2个把全局变量x2的相同的函数，再多线程分别调用1次，可能遇到结果为2倍而不是2x2倍的情况。原因如图。解决方案是用c语言中的锁定方法。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20221027090224.png"></p><h1 id="硬件控制"><a href="#硬件控制" class="headerlink" title="硬件控制"></a>硬件控制</h1><h2 id="外围设备硬件和软件是如何实现通信的？"><a href="#外围设备硬件和软件是如何实现通信的？" class="headerlink" title="外围设备硬件和软件是如何实现通信的？"></a>外围设备硬件和软件是如何实现通信的？</h2><p><strong>首先，应用与硬件无关：</strong>Windows系统下的硬件是由系统全权负责的，应用层只能通过API来通过系统调用。</p><p><strong>其次，硬件们是通过I/O控制器来实现和操作系统通信的：</strong>计算机有着USB等和外围设备交互的连接器，因为外围设备的电压不同、用的也是模拟信号，所以无法和硬件直接连接，需要用I/O控制器。</p><p><strong>什么是端口：</strong>I/O控制器中由用于临时保存输入输出数据的内存，这个内存叫端口。端口是内存，也有自己的地址，叫<strong>端口号</strong>或者<strong>I/O地址</strong>。</p><p><strong>汇编语言的IN/OUT指令，是通过端口和寄存器之间数据互换实现的：</strong>汇编中的IN/OUT助记符是实现和硬件交互的，IN指令把端口数据存储在CPU寄存器中，OUT把CPU寄存器数据存储在端口中。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20221027100225.png"></p><h2 id="外围设备硬件的中断操作IRQ"><a href="#外围设备硬件的中断操作IRQ" class="headerlink" title="外围设备硬件的中断操作IRQ"></a>外围设备硬件的中断操作IRQ</h2><p>IRQ（Interrupt Request）是<strong>中断请求</strong>的意思，对应的值是0x06。</p><p>它负责把当前正在运行的主程序中断（同时把CPU所有寄存器的数值保存到栈中），然后跳转到外围设备的输入输出运行，等运行完后返回中断程序（同时恢复所有寄存器的值），继续执行。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20221027103320.png"></p><p>遇到诸如打印机这种操作比CPU慢很多的设备，就可以等中断请求发生时再输出数据即可；而对于实时性比较高的比如鼠标等，就可以不停轮询。</p><h2 id="外围设备直接和内存通信DMA"><a href="#外围设备直接和内存通信DMA" class="headerlink" title="外围设备直接和内存通信DMA"></a>外围设备直接和内存通信DMA</h2><p>像IRQ的中断，本质上还是要和CPU交互的。但是DMA（Direct Memory Access）可以不通过CPU，外围设备直接和主内存进行数据传送。这样更快。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20221027104221.png"></p><h2 id="计算机图形的内存VRAM"><a href="#计算机图形的内存VRAM" class="headerlink" title="计算机图形的内存VRAM"></a>计算机图形的内存VRAM</h2><p>显示器中显示的数据，一直存储在内存总，这个内存叫<strong>VRAM</strong>（Video RAM）。在程序中，只要往VRAM中写入数据，该数据就会在显示其中显示出来，此功能由操作系统或BIOS提供。</p><p>在DOS系统时代，VRAM是主内存的一部分，图形颜色只能用16种是因为这块内存太小了。而如今，显卡配有独立的VRAM内存。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20221027104612.png"></p><h1 id="计算机的随机数"><a href="#计算机的随机数" class="headerlink" title="计算机的随机数"></a>计算机的随机数</h1><h2 id="线性同余法实现伪随机"><a href="#线性同余法实现伪随机" class="headerlink" title="线性同余法实现伪随机"></a>线性同余法实现伪随机</h2><p>很多伪随机其实就是用线性同余法的递推公式<img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20221027110424.png"></p><p>实现的。</p><p>指定了a、b、c之后，就会生成0 ~ c（不包括c）的随机数。其中Ri、a、b、c就是随机数的种子。</p><p>那这样为什么叫伪随机呢？因为指定好a、b、c之后的数，不停递推生成的结果是有<strong>周期性</strong>的。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20221027110819.png"></p><h1 id="C入门"><a href="#C入门" class="headerlink" title="C入门"></a>C入门</h1><h2 id="数据类型"><a href="#数据类型" class="headerlink" title="数据类型"></a>数据类型</h2><table><thead><tr><th>名称</th><th>长度（位长）</th><th>精度</th></tr></thead><tbody><tr><td>char</td><td>8</td><td>-128，+127</td></tr><tr><td>short</td><td>16</td><td>-32768，32767</td></tr><tr><td>int（或者long）</td><td>32</td><td>-2147483648，2147483647</td></tr><tr><td>float</td><td>32</td><td>略</td></tr><tr><td>double</td><td>64</td><td>略</td></tr></tbody></table><h2 id="常用关键字"><a href="#常用关键字" class="headerlink" title="常用关键字"></a>常用关键字</h2><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20221028110814.png"></p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;CPU&quot;&gt;&lt;a href=&quot;#CPU&quot; class=&quot;headerlink&quot; title=&quot;CPU&quot;&gt;&lt;/a&gt;CPU&lt;/h1&gt;&lt;h2 id=&quot;CPU可直接执行的“机器码”，到底能处理什么？&quot;&gt;&lt;a href=&quot;#CPU可直接执行的“机器码”，到底能处理什么？&quot; c</summary>
      
    
    
    
    <category term="读书笔记" scheme="https://codingcodingk.github.io/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/"/>
    
    <category term="技术类" scheme="https://codingcodingk.github.io/categories/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/%E6%8A%80%E6%9C%AF%E7%B1%BB/"/>
    
    
    <category term="硬件" scheme="https://codingcodingk.github.io/tags/%E7%A1%AC%E4%BB%B6/"/>
    
    <category term="软件" scheme="https://codingcodingk.github.io/tags/%E8%BD%AF%E4%BB%B6/"/>
    
  </entry>
  
  <entry>
    <title>2D游戏中，角色预制体Image和Sprite怎么选择？</title>
    <link href="https://codingcodingk.github.io/Project/Unity/RPG/himegami/spriteOrImgFor2D/"/>
    <id>https://codingcodingk.github.io/Project/Unity/RPG/himegami/spriteOrImgFor2D/</id>
    <published>2026-03-05T04:37:56.976Z</published>
    <updated>2026-03-05T04:37:56.976Z</updated>
    
    <content type="html"><![CDATA[<p>选型的时候搜了一下，Sprite更优，本来决定用Image的这样方便UI血条什么跟随不需要自己单独写代码了，但是发现Image对于帧动画支持并不好，需要手动SetNativeSize，感觉不如Sprite。以下是抄来的：</p><p>1.首先分析下两者的异同。</p><p>使用上，两者区别不大，都是使用一个Sprite源进行渲染，而Image需要位于某个Canvas下才能显示出来。场景中的Sprite可以像普通的3D游戏物体一样对待，通过Transform组件进行移动等操作，而Image则使用RectTransform进行布局，以便通过Canvas统一管理。由于RectTransform可以设置大小、对齐方式等，Image可以说更加方便一点，这也是很多人选择使用Image的原因。</p><p>渲染上，Sprite使用SpriteRenderer组件渲染，而Image则由CanvasRenderer组件渲染。两者在视觉上没有任何区别（都使用默认材质时）。它们默认的渲染也都是在Transparent Geometry队列中。</p><p>而在引擎的处理上，两者则有很大的不同。将Wireframe选项打开然后在场景中观察，就可以清楚地发现，Image会老老实实地为一个矩形的Sprite生成两个三角形拼成的矩形几何体，而Sprite则会根据显示内容，裁剪掉元素中的大部分透明区域，最终生成的几何体可能会有比较复杂的顶点结构。</p><p>image</p><p>那么这种不同会造成什么结果呢？在继续之前，我们先回顾一下游戏中每帧的渲染过程。对任何物体的渲染，我们需要先准备好相关数据（顶点、UV、贴图数据和shader参数等等），然后调用GPU的渲染接口进行绘制，这个过程称作Draw Call。GPU接收到DrawCall指令后，通过一系列流程生成最终要显示的内容并进行渲染，其中大致的步骤包括：</p><p>CPU发送Draw Call指令给GPU；</p><p>GPU读取必要的数据到自己的显存；</p><p>GPU通过顶点着色器（vertex shader）等步骤将输入的几何体信息转化为像素点数据；</p><p>每个像素都通过片段着色器（fragment shader）处理后写入帧缓存；</p><p>当全部计算完成后，GPU将帧缓存内容显示在屏幕上。</p><p>通过上面的认知，我们可以推断：</p><p>Sprite由于顶点数据更加复杂，在第1/2步时会比Image效率更低；</p><p>Sprite会比Image执行较多的顶点着色器运算；</p><p>Image会比Sprite执行更多的片段着色器运算；</p><p>看起来似乎Image比Sprite有更大的好处，然而事实上，由于片段着色器是针对每个像素运算，Sprite通过增加顶点而裁剪掉的部分减少了相当多的运算次数，在绝大多数情况下，反而比Image拥有更好的效率 —— 尤其是场景中有大量的2D精灵时。</p><p><strong>总结一下，SpriteRenderer会创建额外的几何体来裁剪掉多余的透明像素区域，从而减少了大量的片段着色器运算，并降低了overdraw；而Image则会创建简单的矩形几何体。随着2D元素数量的增加，这种差别会慢慢明显起来。</strong></p><p>可以看出，SpriteRenderer确实是经过优化以显示更多的元素的。所以在2D游戏开发中，游戏场景中的元素，应该尽量使用它去渲染。而Image应该仅用于UI显示（实际上即使不考虑性能原因，由于屏幕分辨率的变化，Image可能会被Canvas改变显示位置和实际大小，如果用于游戏内元素的显示，可能会造成跟预期设计不一致的显示结果，也应该避免使用）。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;选型的时候搜了一下，Sprite更优，本来决定用Image的这样方便UI血条什么跟随不需要自己单独写代码了，但是发现Image对于帧动画支持并不好，需要手动SetNativeSize，感觉不如Sprite。以下是抄来的：&lt;/p&gt;
&lt;p&gt;1.首先分析下两者的异同。&lt;/p&gt;
&lt;</summary>
      
    
    
    
    <category term="Unity" scheme="https://codingcodingk.github.io/categories/Unity/"/>
    
    
    <category term="Unity" scheme="https://codingcodingk.github.io/tags/Unity/"/>
    
    <category term="工程" scheme="https://codingcodingk.github.io/tags/%E5%B7%A5%E7%A8%8B/"/>
    
    <category term="ひめがみ絵巻" scheme="https://codingcodingk.github.io/tags/%E3%81%B2%E3%82%81%E3%81%8C%E3%81%BF%E7%B5%B5%E5%B7%BB/"/>
    
  </entry>
  
  <entry>
    <title>Unity中如何把RGB图和Alpha图分离的图片还原</title>
    <link href="https://codingcodingk.github.io/Project/Unity/RPG/himegami/himegami_before/"/>
    <id>https://codingcodingk.github.io/Project/Unity/RPG/himegami/himegami_before/</id>
    <published>2026-03-05T04:37:56.975Z</published>
    <updated>2026-03-05T04:37:56.975Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Unity中如何把RGB图和Alpha图分离的图片还原"><a href="#Unity中如何把RGB图和Alpha图分离的图片还原" class="headerlink" title="Unity中如何把RGB图和Alpha图分离的图片还原"></a>Unity中如何把RGB图和Alpha图分离的图片还原</h1><h2 id="一-RGB图、Alpha图简述"><a href="#一-RGB图、Alpha图简述" class="headerlink" title="一.RGB图、Alpha图简述"></a>一.RGB图、Alpha图简述</h2><h3 id="RGB图"><a href="#RGB图" class="headerlink" title="RGB图"></a>RGB图</h3><p>RGB图是指由红、绿、蓝三个基本色光按不同比例合成的图像，是最常见的彩色图像。RGB图像的每个像素点由三个通道的颜色值组成，通常用三元组（R,G,B）表示，其中R表示红色通道，G表示绿色通道，B表示蓝色通道，三个通道的值均为0-255之间的整数。</p><h3 id="Alpha图"><a href="#Alpha图" class="headerlink" title="Alpha图"></a>Alpha图</h3><p>Alpha图是指一种用于描述图像透明度的图像格式，通常用于PNG格式的图像中。Alpha图像的每个像素点也由一个通道的值表示，通常用A表示，取值范围为0-255，0表示完全透明，255表示完全不透明。</p><h2 id="二-为什么需要拆成2种图打包？"><a href="#二-为什么需要拆成2种图打包？" class="headerlink" title="二.为什么需要拆成2种图打包？"></a>二.为什么需要拆成2种图打包？</h2><h3 id="1-ETC不支持透明通道"><a href="#1-ETC不支持透明通道" class="headerlink" title="1.ETC不支持透明通道"></a>1.ETC不支持透明通道</h3><p>ETC是一种Unity支持的贴图压缩格式，用于安卓设备，不过目前不支持ETC2的已经少到可以忽略了。</p><ul><li>ETC：不支持透明通道，被所有 android 设备支持</li><li>ETC2：支持透明通道，Android 设备的 GPU 必须支持 OpenGL es 3.0 才可以使用，对于不支持的设备，会以未压缩的形式存在内存中，占用更多内存</li><li>PVRTC：所有苹果设备都可以使用，要求压缩纹理长宽相等，且是 2 的幂次（POT，Power of 2）</li><li>ASTC：高质量低内存占用，未来可能普遍使用的压缩格式，现在有一部分机型不支持</li></ul><h3 id="2-对ios机上内存占用与素材清晰度之间的平衡问题的一种优解"><a href="#2-对ios机上内存占用与素材清晰度之间的平衡问题的一种优解" class="headerlink" title="2.对ios机上内存占用与素材清晰度之间的平衡问题的一种优解"></a>2.对ios机上内存占用与素材清晰度之间的平衡问题的一种优解</h3><p>一般来说，目前 Unity 的手机游戏 android 上非透明贴图会使用 RGB Compressed ETC 4bits，透明贴图可以使用 RGBA Compressed ETC2 8bit，iOS 非透明贴图使用 RGB Compressed PVRTC 4bits，透明贴图使用 RGBA Compressed PVRTC 4bits。</p><p>这里的 bits 概念的意思为：每个像素占用的比特数，举个例子，RGB Compressed PVRTC 4bits 格式的 1024x1024 的贴图，其在内存中占用的大小 = 1024x1024x4 (比特) = 4M (比特) = 0.5M (字节)。</p><p>我们可以看到，在 iOS 上，非透明贴图和透明贴图都是 4bpp（4bits per pixel）的，多了透明通道还是一样的大小，自然 4bpp 的透明贴图压缩出来效果就会变差，而实机上看确实也是惨不忍睹。这是第一个问题的答案。</p><p>要解决上面图片模糊的问题，可以有这些做法：</p><ul><li>透明贴图不压缩，内存占用 32bpp</li><li>分离 alpha 通道，内存占用 4bpp+4bpp（或 4bpp+8bpp）</li></ul><p>显然第一种是不靠谱的， 32bpp 的内存消耗对于手机来说过大了。所以就需要拆分。</p><h2 id="三-如何还原"><a href="#三-如何还原" class="headerlink" title="三.如何还原"></a>三.如何还原</h2><p>我现在通过解包拿到的就是2张图，一张rpg图，一张alpha通道的图，带梯度（并不是01非黑即白图）。还原有2种方式，1是可以<a href="https://blog.csdn.net/qq_39738178/article/details/120023891">ps之类的将图片遮罩还原</a>，2是可以通过写shader，采样rgb图的rgb数值和aplha图的a数值，作为最终输出。这里只介绍第二种，我用的shader graph。</p><p>1.导入2张png，转成sprite，alpha图的 Alpha Source 设置为 From Gray Scale 。</p><p>2.写shader，并将shader绑到material上，注意 Surface Type 要选择 Transparent 模式，不然会被当做01图用。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20230409145504.png"></p><p>3.新建一个sprite2D，给配上材质就ok了，效果还是不错的</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20230409151528.png"></p><h2 id="四-如何拆分打包"><a href="#四-如何拆分打包" class="headerlink" title="四.如何拆分打包"></a>四.如何拆分打包</h2><p>如何拆分打包：<a href="https://www.gameres.com/872788.html">https://www.gameres.com/872788.html</a></p><h2 id="五-如何通过PS还原"><a href="#五-如何通过PS还原" class="headerlink" title="五.如何通过PS还原"></a>五.如何通过PS还原</h2><p>又回来补PS版本的了…因为解包下来游戏内容上千张图集，很多都甚至需要手动拆分成散图，实在是不想自己干，还是用PS还原png透明图交给别人吧。</p><p>1.把RGB图扔到ps里，点击“添加蒙版”。</p><p>2.在图层中选中“蒙版”，保持选中的状态下，点击“通道”，可以看到有“图层 蒙版”这个图层，把前面的眼睛点开。</p><p>3.去复制alpha图，粘贴到这个蒙版图层里。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;Unity中如何把RGB图和Alpha图分离的图片还原&quot;&gt;&lt;a href=&quot;#Unity中如何把RGB图和Alpha图分离的图片还原&quot; class=&quot;headerlink&quot; title=&quot;Unity中如何把RGB图和Alpha图分离的图片还原&quot;&gt;&lt;/a&gt;Unity</summary>
      
    
    
    
    <category term="Unity" scheme="https://codingcodingk.github.io/categories/Unity/"/>
    
    
    <category term="Unity" scheme="https://codingcodingk.github.io/tags/Unity/"/>
    
    <category term="工程" scheme="https://codingcodingk.github.io/tags/%E5%B7%A5%E7%A8%8B/"/>
    
    <category term="ひめがみ絵巻" scheme="https://codingcodingk.github.io/tags/%E3%81%B2%E3%82%81%E3%81%8C%E3%81%BF%E7%B5%B5%E5%B7%BB/"/>
    
  </entry>
  
  <entry>
    <title>我用向日葵打开unity编辑器怎么白屏了？</title>
    <link href="https://codingcodingk.github.io/Project/Unity/RPG/himegami/remoteUnityBug/"/>
    <id>https://codingcodingk.github.io/Project/Unity/RPG/himegami/remoteUnityBug/</id>
    <published>2026-03-05T04:37:56.975Z</published>
    <updated>2026-03-05T04:37:56.975Z</updated>
    
    <content type="html"><![CDATA[<h1 id="原因"><a href="#原因" class="headerlink" title="原因"></a>原因</h1><p>根据网上搜索的结果，大致有两个原因：</p><p>台式机没有外接显示器，或显示器处于关闭状态。<br>笔记本合上了盖子。</p><p>根据以上原因，个人猜测主要是因为没有检测到显示器，电脑显卡自动切换到某种低负载模式，这将导致一些使用显卡硬件加速的软件出现无法显示的问题。</p><p>没错，所以unity编辑器肯定是用了硬件加速或者根据硬件情况切换负载模式这种策略。</p><h1 id="解决方法"><a href="#解决方法" class="headerlink" title="解决方法"></a>解决方法</h1><p>解决思路：既然软件无法正常显示，是由于显卡模式的切换，导致依赖显卡硬件加速的应用无法显示，那么解决方法主要有两种：</p><p>方法一： 让显卡能检测到显示器。可以通过外接显示器并通电、让笔记本打开盖子、外接虚拟HDMI之类的负载（让显卡误以为我们外接了显示器）。<br>方法二： 若某个软件时因为依赖显卡硬件加速而导致无法显示，那么禁用该软件的显卡硬件加速功能即可。禁用显卡硬件加速方法又有两种思路：<br>禁用手段一：百度，不同软件有不同的禁用方法。（可能有的软件没提供禁用选项）<br>禁用手段二：在NVIDIA控制面板里指定禁用某个软件（个人猜测，没试过）</p><h1 id="其他解决方法"><a href="#其他解决方法" class="headerlink" title="其他解决方法"></a>其他解决方法</h1><p>搜索的时候还遇到过别人有其他情况，一一列举，</p><p>1.清除cache</p><p>2.重装对应版本unity（前提是就个别版本unity白屏）</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;原因&quot;&gt;&lt;a href=&quot;#原因&quot; class=&quot;headerlink&quot; title=&quot;原因&quot;&gt;&lt;/a&gt;原因&lt;/h1&gt;&lt;p&gt;根据网上搜索的结果，大致有两个原因：&lt;/p&gt;
&lt;p&gt;台式机没有外接显示器，或显示器处于关闭状态。&lt;br&gt;笔记本合上了盖子。&lt;/p&gt;
&lt;p&gt;根</summary>
      
    
    
    
    <category term="Unity" scheme="https://codingcodingk.github.io/categories/Unity/"/>
    
    
    <category term="Unity" scheme="https://codingcodingk.github.io/tags/Unity/"/>
    
    <category term="工程" scheme="https://codingcodingk.github.io/tags/%E5%B7%A5%E7%A8%8B/"/>
    
    <category term="ひめがみ絵巻" scheme="https://codingcodingk.github.io/tags/%E3%81%B2%E3%82%81%E3%81%8C%E3%81%BF%E7%B5%B5%E5%B7%BB/"/>
    
  </entry>
  
  <entry>
    <title>Spine安装和简单了解、导入unity</title>
    <link href="https://codingcodingk.github.io/Project/Unity/RPG/himegami/spineInit/"/>
    <id>https://codingcodingk.github.io/Project/Unity/RPG/himegami/spineInit/</id>
    <published>2026-03-05T04:37:56.975Z</published>
    <updated>2026-03-05T04:37:56.976Z</updated>
    
    <content type="html"><![CDATA[<p>1.首先简单了解下spine导出的东西是什么。spine导出的文件会有3个：png图集、atlas图集切分文件、记录骨骼、动画的一个二进制skel文件 或者 一个json文件。</p><p>2.要把spine导出的文件导入进unity，只要事先在<a href="https://zh.esotericsoftware.com/spine-unity-download">spine-unity页面</a>下载好对应的 spine-runtime unitypackage就可以。注意这个版本卡的特别死，unity版本和spine导出版本是必须严格遵照要求的，具体看页面。<strong>安装好对应的package之后，需要先把atlas后缀加上.txt，再给二进制文件skel加上.bytes，json就不用改了，然后直接复制粘贴文件到unity里就会自动转换。</strong></p><p>3.<strong>在这里记录一个大坑</strong>，众所周知 Spine3.8.75 这个版本是网上能找到的破解版本。所以Spine直接在自己的 3.8版本unity runtime里，特别在2个地方写了一行：</p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="string">&quot;3.8.75&quot;</span> == skeletonData.version)</span><br><span class="line">       <span class="keyword">throw</span> <span class="keyword">new</span> Exception(<span class="string">&quot;Unsupported skeleton data, please export with a newer version of Spine.&quot;</span>);</span><br></pre></td></tr></table></figure><p>这会直接导致你的转换文件生成失败。我一开始以为是自己资源的版本导出有问题，研究了很久，最后感觉哪都跑通了就这个离奇，就直接查代码了，结果才发现这行特殊的代码。</p><p>实在是666。</p><p>解决方法是直接全局搜索到<code>&quot;3.8.75&quot;</code>，然后注释掉就完事了。</p><p>4.一个简单的优化，就是spine的文件选择skel二进制而不是json文件。json文件的读取，那可真的是慢到爆炸了…其他的话应该还可以在战斗开始前就把所有可能用到的人物bytes全部加载好一起进，这个具体怎么操作还没看估计得小改一下spine的加载部分。</p><p>5.途径的有用链接</p><p>Spine-Unity 官网页面：<a href="http://zh.esotericsoftware.com/spine-unity-download/">http://zh.esotericsoftware.com/spine-unity-download/</a></p><p>一个日本老哥做的 skel转json：<a href="https://naganeko.pages.dev/chibi-gif/">https://naganeko.pages.dev/chibi-gif/</a></p><p>6.总得来说还是第3点最离谱，网上看到很多有同样问题的却只有人回答说要把json文件里的版本号改成3.8这种方法去解决。说回来如果Spine真的想玩一手盗版用户，为什么还要抛出而不是直接在这里return掉呢，那样我估计可以自我怀疑到天荒地老了。总之不会做商用仅作学习，搞了一晚上总算可以开始用spine了。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;1.首先简单了解下spine导出的东西是什么。spine导出的文件会有3个：png图集、atlas图集切分文件、记录骨骼、动画的一个二进制skel文件 或者 一个json文件。&lt;/p&gt;
&lt;p&gt;2.要把spine导出的文件导入进unity，只要事先在&lt;a href=&quot;http</summary>
      
    
    
    
    <category term="Unity" scheme="https://codingcodingk.github.io/categories/Unity/"/>
    
    
    <category term="Unity" scheme="https://codingcodingk.github.io/tags/Unity/"/>
    
    <category term="工程" scheme="https://codingcodingk.github.io/tags/%E5%B7%A5%E7%A8%8B/"/>
    
    <category term="ひめがみ絵巻" scheme="https://codingcodingk.github.io/tags/%E3%81%B2%E3%82%81%E3%81%8C%E3%81%BF%E7%B5%B5%E5%B7%BB/"/>
    
  </entry>
  
  <entry>
    <title>战斗同步思考（未完）</title>
    <link href="https://codingcodingk.github.io/Project/Unity/RPG/himegami/battle/"/>
    <id>https://codingcodingk.github.io/Project/Unity/RPG/himegami/battle/</id>
    <published>2026-03-05T04:37:56.973Z</published>
    <updated>2026-03-05T04:37:56.975Z</updated>
    
    <content type="html"><![CDATA[<h1 id="需求拆解"><a href="#需求拆解" class="headerlink" title="需求拆解"></a>需求拆解</h1><h2 id="战斗同步"><a href="#战斗同步" class="headerlink" title="战斗同步"></a>战斗同步</h2><p>1.战斗同步有2种模式，一种是离线验证模式，在本地记录操作帧和随机种子，发送到服务端，再重新演算战斗逻辑；另一种是同步模式，客户端只转发操作帧，由服务端下发真实操作帧。<strong>服务器成本原因，优先实现第一种。</strong></p><p>2.结果验证</p><p>3.掉落验证</p><h2 id="战斗流程"><a href="#战斗流程" class="headerlink" title="战斗流程"></a>战斗流程</h2><p>1.实现战前匹配（可以是玩家也可以是Boss）、匹配成功后在服务端对所有玩家建立房间，下发开始。房间用状态机管理。进入房间后，启动倒计时，超过设定的最长战斗时长（10分钟）就判定失败，战斗视为失败，服务器回收资源。</p><p>2.从第一回合到最终回合，每一个回合的内部，进行回合触发时机的执行、行动优先级的判定、技能逻辑的执行、存活死亡的结算、战斗胜负的判定这几个程序步骤。</p><h1 id="技术实现"><a href="#技术实现" class="headerlink" title="技术实现"></a>技术实现</h1><h2 id="伪随机"><a href="#伪随机" class="headerlink" title="伪随机"></a>伪随机</h2><p>1.PRD算法</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20230413152135.png"></p><p>这是伪随机在游戏中最常见的用法，因此直接就被玩家用Pseudo Random Distribution的缩写PRD来指代了。</p><p>在WAR3中，一个暴击率20%的英雄，并不是每一刀都20%暴击率的。而是以5.57%作为初始暴率，如果第一刀不暴，则第二刀的暴率增加到初始值的2倍：11.14%；如果还是不暴，就继续增加到初始值的3倍：16.71%，以此类推。</p><p>而如果在这个过程中任何一次攻击打出了暴击，就会把暴击率重置到5.57%。</p><p>因此可以把所有游戏中可能出现的暴击概率（比如0-100的整数%） 和 公式计算出的初始值 做成一张映射表，就不需要随机了。</p><p>2.洗牌算法</p><p>类似于音乐播放器，在一开始就把所有可能的结果列出并洗成乱序，然后从第一个开始取。那么具体怎么洗的话，就是比如30个按顺序排列的不重复数字，先从下标0-29里random一个，放到29，再从0-28里random一个，放到28…每次放到数组最后的就是洗好的，这样可以避免每次random的时候随到重复下标。</p><p>3.组合随机</p><p>其实就是保底机制，用于抽卡。品质值不随机（固定SR、SSR），只随机具体物品值。</p><h1 id="业务实现"><a href="#业务实现" class="headerlink" title="业务实现"></a>业务实现</h1><h2 id="掉落"><a href="#掉落" class="headerlink" title="掉落"></a>掉落</h2><p><a href="https://www.jianshu.com/p/f9c37c46c03c">https://www.jianshu.com/p/f9c37c46c03c</a></p><h2 id="战斗序列"><a href="#战斗序列" class="headerlink" title="战斗序列"></a>战斗序列</h2><p>使用行动队列的方式，通过将每一个行动定义为一个行动节点（操作帧），按照队列中节点的顺序依次执行。不管是回合初就确定了行动顺序，还是回合中每完成一个行动都需要重新计算行动优先级的方案，都适用上述的方式，区别只在于对技能逻辑中衍生的行动节点，是添加到队列的末端还是插入到队列的头部。</p><p>另外为了打破技能循环（比如暴击后再攻击一次），还需要定义出行动节点的类型，由战斗回合产生的行动节点称为原始行动节点，由技能触发的行动节点则称为衍生行动节点，所有的反制行动、追加行动都应该归属于衍生行动节点。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;需求拆解&quot;&gt;&lt;a href=&quot;#需求拆解&quot; class=&quot;headerlink&quot; title=&quot;需求拆解&quot;&gt;&lt;/a&gt;需求拆解&lt;/h1&gt;&lt;h2 id=&quot;战斗同步&quot;&gt;&lt;a href=&quot;#战斗同步&quot; class=&quot;headerlink&quot; title=&quot;战斗同步&quot;&gt;&lt;/a</summary>
      
    
    
    
    <category term="Unity" scheme="https://codingcodingk.github.io/categories/Unity/"/>
    
    
    <category term="Unity" scheme="https://codingcodingk.github.io/tags/Unity/"/>
    
    <category term="工程" scheme="https://codingcodingk.github.io/tags/%E5%B7%A5%E7%A8%8B/"/>
    
    <category term="ひめがみ絵巻" scheme="https://codingcodingk.github.io/tags/%E3%81%B2%E3%82%81%E3%81%8C%E3%81%BF%E7%B5%B5%E5%B7%BB/"/>
    
  </entry>
  
  <entry>
    <title>Hello Agents - 基础概念</title>
    <link href="https://codingcodingk.github.io/Tech/AI/hello_agents/cp1/"/>
    <id>https://codingcodingk.github.io/Tech/AI/hello_agents/cp1/</id>
    <published>2026-03-02T09:10:37.476Z</published>
    <updated>2026-03-05T07:48:56.447Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Agent构成-amp-运行原理"><a href="#Agent构成-amp-运行原理" class="headerlink" title="Agent构成&amp;运行原理"></a>Agent构成&amp;运行原理</h1><h2 id="PEAS"><a href="#PEAS" class="headerlink" title="PEAS"></a>PEAS</h2><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20260303133650.png"></p><h2 id="运行机制"><a href="#运行机制" class="headerlink" title="运行机制"></a>运行机制</h2><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20260302200931.png"></p><h1 id="AI模型进化路线"><a href="#AI模型进化路线" class="headerlink" title="AI模型进化路线"></a>AI模型进化路线</h1><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20260303141752.png"></p><h2 id="符号主义范式"><a href="#符号主义范式" class="headerlink" title="符号主义范式"></a>符号主义范式</h2><p>知识库（规则）+ 推理机。不智能，需要人工输入规则比如 IF THEN，且比较死板导致健壮性差，诞生了“专家系统”这类产物。</p><h2 id="联结主义范式"><a href="#联结主义范式" class="headerlink" title="联结主义范式"></a>联结主义范式</h2><p>与符号主义自上而下、依赖明确逻辑规则的设计哲学不同，联结主义是一种自下而上的方法，其灵感来源于对生物大脑神经网络结构的模仿。做出<strong>神经元</strong>（小的功能），把他们<strong>分布式</strong>的连接起来，并通过算法学习对系统设置神经元的<strong>连接权重</strong>。这一范式能成功源于计算机已经能对图片声音文本等信息直接进行理解，反映了心智社会。</p><h2 id="强化学习"><a href="#强化学习" class="headerlink" title="强化学习"></a>强化学习</h2><p>通过给行为设置奖励权重，来让智能体在大量“试错”中有目的性的学习。通过“感知-行动-学习”的闭环中持续迭代自己，让其更有远见。</p><h2 id="预训练"><a href="#预训练" class="headerlink" title="预训练"></a>预训练</h2><p>在进行强化学习等特化目标的训练前，先用<strong>自然语言处理+监督自学习</strong>，把大量的网络文本进行输入训练出一个基础的通用模型LLM，它的目的是有一些基础常识，比如“预测下一个词”。</p><p>然后再对预训练好的模型，根据具体任务要求进行微调（小样本数据标注学习），来实现目的。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20260303161331.png"></p><p>通过大量预训练的LLM，涌现出了预料外的能力：</p><ul><li><strong>上下文学习（In-context Learning）</strong>：无需调整模型权重，仅在输入中提供<strong>几个示例（Few-shot）</strong>甚至<strong>零个示例（Zero-shot）</strong>，模型就能理解并完成新的任务。</li><li><strong>思维链（Chain-of-Thought）推理</strong>：通过引导模型在回答复杂问题前，先输出一步步的推理过程，可以显著提升其在逻辑、算术和常识推理任务上的准确性。</li></ul><h2 id="大模型"><a href="#大模型" class="headerlink" title="大模型"></a>大模型</h2><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20260303162026.png"></p><h1 id="大语言模型"><a href="#大语言模型" class="headerlink" title="大语言模型"></a>大语言模型</h1><h2 id="RNN-循环神经网络"><a href="#RNN-循环神经网络" class="headerlink" title="RNN 循环神经网络"></a>RNN 循环神经网络</h2><p>引入多维向量概念，来表示词库，结点在空间中离得越近就代表两个词意思越接近。串行计算，<code>单词[N]</code>需要前面<code>单词[0] ~ 单词[N-1]</code>总计算下来的结果作为参数。</p><p>RNN 的设计引入了一个<strong>隐藏状态 (hidden state)</strong> 向量，我们可以将其理解为网络的短期记忆。在处理序列的每一步，网络都会读取当前的输入词，并结合它上一刻的记忆（即上一个时间步的隐藏状态），然后生成一个新的记忆（即当前时间步的隐藏状态）传递给下一刻。</p><p>因此“隐藏状态”这个记忆机制，导致计算必须串行进行，且上下文可能会很大。</p><h2 id="Transformer"><a href="#Transformer" class="headerlink" title="Transformer"></a>Transformer</h2><h3 id="优点☆"><a href="#优点☆" class="headerlink" title="优点☆"></a>优点☆</h3><ol><li>比起RNN，它用Attention概念，更擅长远距离的情况</li><li>比起RNN，它可以并行处理计算</li></ol><h3 id="注意力计算（单头）"><a href="#注意力计算（单头）" class="headerlink" title="注意力计算（单头）"></a>注意力计算（单头）</h3><p>Transformer在2017 年由谷歌团队提出。它完全抛弃了循环结构，转而完全依赖一种名为<strong>注意力 (Attention)</strong> 的机制来捕捉序列内的依赖关系，从而实现了真正意义上的并行计算。</p><ul><li>**查询 (Query, Q)**：代表当前词元，它正在主动地“查询”其他词元以获取信息。</li><li>**键 (Key, K)**：代表句子中可被查询的词元“标签”或“索引”。</li><li>**值 (Value, V)**：代表词元本身所携带的“内容”或“信息”</li></ul><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20260303181641.png"></p><p><strong>简单理解：</strong></p><p>每个词都有 QKV三个值，Q=我要问什么，K=我的标签，V=我的信息。对每个词，都带Q去匹配其他词的K，来得出需要对其他词分配多少的Attention权重。最后再用权重乘一下每个词的V，来求得这个词的问题结果。</p><p>因此，每个词都是独立和整个句子进行匹配的，完全可以并行计算；而注意力机制本身并没有涉及到顺序概念，不像RNN那样串行直接带了顺序，因此又引入<strong>位置编码</strong>概念来解决这个问题。</p><h3 id="多头"><a href="#多头" class="headerlink" title="多头"></a>多头</h3><p>它将原始的 Q, K, V 向量在维度上切分成 h 份（h 就是“头”数），每一份都独立地进行一次单头注意力的计算。这就好比让 h 个不同的“专家”从不同的角度去审视句子，每个专家都能捕捉到一种不同的特征关系。最后，将这 h 个专家的“意见”（即输出向量）拼接起来，再通过一个线性变换进行整合，就得到了最终的输出。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20260303182352.png"></p><h2 id="Decoder-Only"><a href="#Decoder-Only" class="headerlink" title="Decoder-Only"></a>Decoder-Only</h2><p>放弃了编码器只保留解码器，只关心“预测下一个词”这件事。</p><p>优势：训练目标统一、结构简单易于扩展、天然适合生成任务。</p><h2 id="模型采样"><a href="#模型采样" class="headerlink" title="模型采样"></a>模型采样</h2><ul><li>Tempurature = 胆大程度，为0时候极度保守只选top1，0-1时比较保守，1以上越大越发散</li><li>Top-k = token从高到低排，取前k个</li><li>Top-p = token从高到低排，算token的概率，累加概率，大于等于p结束。</li></ul><h2 id="BPE分割词"><a href="#BPE分割词" class="headerlink" title="BPE分割词"></a>BPE分割词</h2><ol><li>对每个词都所有在语料库中出现过的基本字符。</li><li><strong>迭代合并</strong>：在语料库上，统计所有相邻词元对的出现频率，找到频率最高的一对，将它们合并成一个新的词元，并加入词表。</li><li><strong>重复</strong>：重复第 2 步，直到词表大小达到预设的阈值。</li></ol><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20260303205954.png"></p><h2 id="模型幻觉"><a href="#模型幻觉" class="headerlink" title="模型幻觉"></a>模型幻觉</h2><p><strong>模型幻觉（Hallucination）</strong>通常指的是大语言模型生成的内容与客观事实、用户输入或上下文信息相矛盾，或者生成了不存在的事实、实体或事件。幻觉的本质是模型在生成过程中，过度自信地“编造”了信息，而非准确地检索或推理。</p><p><strong>解决方案</strong>：</p><ol><li><p><strong>数据层面</strong>： 通过高质量数据清洗、引入事实性知识以及强化学习与人类反馈 (RLHF) 等方式，从源头减少幻觉。</p></li><li><p><strong>模型层面</strong>： 探索新的模型架构，或让模型能够表达其对生成内容的不确定性。</p></li><li><p><strong>推理与生成层面</strong>：</p><ol><li><strong>检索增强生成 (Retrieval-Augmented Generation, RAG)</strong> ： 这是目前缓解幻觉的有效方法之一。RAG 系统通过在生成之前从外部知识库（如文档数据库、网页）中检索相关信息，然后将检索到的信息作为上下文，引导模型生成基于事实的回答。</li><li><strong>多步推理与验证</strong>： 引导模型进行多步推理，并在每一步进行自我检查或外部验证。</li><li><strong>引入外部工具</strong>： 允许模型调用外部工具（如搜索引擎、计算器、代码解释器）来获取实时信息或进行精确计算。</li></ol></li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;Agent构成-amp-运行原理&quot;&gt;&lt;a href=&quot;#Agent构成-amp-运行原理&quot; class=&quot;headerlink&quot; title=&quot;Agent构成&amp;amp;运行原理&quot;&gt;&lt;/a&gt;Agent构成&amp;amp;运行原理&lt;/h1&gt;&lt;h2 id=&quot;PEAS&quot;&gt;&lt;a </summary>
      
    
    
    
    <category term="AI" scheme="https://codingcodingk.github.io/categories/AI/"/>
    
    <category term="Hello Agents" scheme="https://codingcodingk.github.io/categories/AI/Hello-Agents/"/>
    
    
    <category term="AI" scheme="https://codingcodingk.github.io/tags/AI/"/>
    
    <category term="AI Agent" scheme="https://codingcodingk.github.io/tags/AI-Agent/"/>
    
  </entry>
  
  <entry>
    <title>TCP、UDP的区别</title>
    <link href="https://codingcodingk.github.io/Tech/Net/tcp_udp/"/>
    <id>https://codingcodingk.github.io/Tech/Net/tcp_udp/</id>
    <published>2022-08-10T14:45:04.205Z</published>
    <updated>2022-08-11T01:12:49.835Z</updated>
    
    <content type="html"><![CDATA[<h1 id="什么是TCP、UDP"><a href="#什么是TCP、UDP" class="headerlink" title="什么是TCP、UDP"></a>什么是TCP、UDP</h1><p>协议，他们都只工作在传输层。</p><h1 id="什么是TCP的挥手、握手"><a href="#什么是TCP的挥手、握手" class="headerlink" title="什么是TCP的挥手、握手"></a>什么是TCP的挥手、握手</h1><p>三次握手、传输确认、四次挥手。</p><p><strong>三次握手：</strong>建立连接用的。客户端发起请求给服务端，服务端返回消息给客户端发番号，客户端再发送请求给服务端，完成连接的建立。</p><p>为什么需要第三步？为了<strong>解决网络信道不可靠问题</strong>。第一个请求包阻塞了，所以客户端重新再做了第一步成功建立了连接，此时之前的那个阻塞包又恢复了，此时如果没有第三步服务端就会成功建立第二个连接。</p><p><strong>传输确认：</strong>一包数据可能拆成多包发送，如何处理丢包问题？这些包的到达顺序不一定，如何处理乱序问题？答案是通过回复确认包。</p><p>这个根据下图模型理解。首先TCP会在建立连接之后，会创建如下的发送缓冲区。每次发送报文时，除了数据内容，还会在TCP协议头中附带本次数据内容的序列号（起始下标）+长度。接收端收到这个包后，会进行回复确认，内容为序列号+长度，也就是下一包的其实序列号。这样做是为了能够<strong>把整个包根据序列号和长度重组</strong>，中间遇到丢包的情况申请重发即可。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220811083837.png"></p><p><strong>四次挥手：</strong>结束连接用的。</p><p>一、二挥手：客户端发送结束包给服务端；服务端返回一个包表示自己进入终止等待状态。这前两次挥手完之后，服务端还可以给客户端发送之前未发完的数据，此时客户端还可以接收。也就是说这前两次是为了发送之前未发完数据。</p><p>三、四挥手：服务端发送完未完数据后，给客户端发送结束包，服务端进入最后确认状态；客户端收到后回复服务端一个确认包，客户端进入超时等待状态，超时后自动关闭连接。这最后一段等待是为了第四次挥手的确认包万一服务端没收到，服务端要求重发时客户端这还能响应。</p><h1 id="TCP和UDP区别"><a href="#TCP和UDP区别" class="headerlink" title="TCP和UDP区别"></a>TCP和UDP区别</h1><p>基于连接 与 基于非连接。</p><p>UDP不像TCP握手、确认、挥手，它是直接把数据包发过去的，所以占用资源少、速度快。</p><p>因此TCP稳定可靠，UDP速度快实时性强。</p><p>隧道网络：VPN等。// TODO</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;什么是TCP、UDP&quot;&gt;&lt;a href=&quot;#什么是TCP、UDP&quot; class=&quot;headerlink&quot; title=&quot;什么是TCP、UDP&quot;&gt;&lt;/a&gt;什么是TCP、UDP&lt;/h1&gt;&lt;p&gt;协议，他们都只工作在传输层。&lt;/p&gt;
&lt;h1 id=&quot;什么是TCP的挥手、握</summary>
      
    
    
    
    <category term="网络" scheme="https://codingcodingk.github.io/categories/%E7%BD%91%E7%BB%9C/"/>
    
    
    <category term="技术" scheme="https://codingcodingk.github.io/tags/%E6%8A%80%E6%9C%AF/"/>
    
    <category term="网络" scheme="https://codingcodingk.github.io/tags/%E7%BD%91%E7%BB%9C/"/>
    
  </entry>
  
  <entry>
    <title>工具篇：代码热更新</title>
    <link href="https://codingcodingk.github.io/Tech/Game/tools/wolong_study/"/>
    <id>https://codingcodingk.github.io/Tech/Game/tools/wolong_study/</id>
    <published>2022-08-01T14:54:12.963Z</published>
    <updated>2022-08-01T14:54:12.963Z</updated>
    
    <content type="html"><![CDATA[<p>非常感谢 HybridCLR(代号wolong) 的作者walon大佬。</p><h1 id="初识HybridCLR"><a href="#初识HybridCLR" class="headerlink" title="初识HybridCLR"></a>初识HybridCLR</h1><h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>能够不写lua，简直就是神。</p><p>HybridCLR ： <a href="https://github.com/focus-creative-games/hybridclr">github</a></p><p>HybridCLR li2cpp plus ： <a href="https://github.com/focus-creative-games/il2cpp_plus">github</a></p><p>HybridCLR示例项目 ： <a href="https://github.com/focus-creative-games/hybridclr_trial">github</a></p><p>手册：<a href="https://focus-creative-games.github.io/hybridclr">github.io</a></p><blockquote><p>HybridCLR 由两部分构成 <a href="https://github.com/focus-creative-games/il2cpp_plus">il2cpp_plus 仓库 (opens new window)</a>和 <a href="https://github.com/focus-creative-games/hybridclr">HybridCLR 仓库 (opens new window)</a>。il2cpp_plus 仓库基于 unity 原始 il2cpp 作了少量修改（几百行），使得它可以支持动态注册元数据，进行可以动态加载 dll。HybridCLR 仓库是 HybridCLR 的核心源代码。</p><p>HybridCLR的安装基于这两部分，安装有一定的<strong>版本规则</strong>。具体见<a href="https://focus-creative-games.github.io/hybridclr/support_versions/">支持的 Unity 版本</a>文档。</p></blockquote><h1 id="配置第一个Unity项目"><a href="#配置第一个Unity项目" class="headerlink" title="配置第一个Unity项目"></a>配置第一个Unity项目</h1><h2 id="搭建环境"><a href="#搭建环境" class="headerlink" title="搭建环境"></a>搭建环境</h2><p>Unity版本2020.3.7</p><ol><li>  拷贝<a href="https://github.com/focus-creative-games/il2cpp_plus">HybridCLR li2cpp plus</a>项目中的libil2cpp文件夹 到对应版本Editor的<code>Editor\Data\il2cpp</code>文件夹下，替换原先的libil2cpp文件夹。</li><li>  拷贝<a href="https://github.com/focus-creative-games/hybridclr">HybridCLR</a>中的huatuo文件夹 到对应版本Editor的<code>Editor\Data\il2cpp\libil2cpp</code>文件夹下。</li></ol><p>上面是老版本的安装方法，现在上面2步在示例中已经可以通过修改li2cpp的path来实现了，按照说明<strong>修改.bat、运行.bat</strong>即可自动从远端仓库拉取huatuo。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;非常感谢 HybridCLR(代号wolong) 的作者walon大佬。&lt;/p&gt;
&lt;h1 id=&quot;初识HybridCLR&quot;&gt;&lt;a href=&quot;#初识HybridCLR&quot; class=&quot;headerlink&quot; title=&quot;初识HybridCLR&quot;&gt;&lt;/a&gt;初识HybridC</summary>
      
    
    
    
    <category term="Game" scheme="https://codingcodingk.github.io/categories/Game/"/>
    
    <category term="工具组" scheme="https://codingcodingk.github.io/categories/Game/%E5%B7%A5%E5%85%B7%E7%BB%84/"/>
    
    
    <category term="工具组" scheme="https://codingcodingk.github.io/tags/%E5%B7%A5%E5%85%B7%E7%BB%84/"/>
    
    <category term="热更新" scheme="https://codingcodingk.github.io/tags/%E7%83%AD%E6%9B%B4%E6%96%B0/"/>
    
  </entry>
  
  <entry>
    <title>Games101 - 渲染管线、Shader、Texture</title>
    <link href="https://codingcodingk.github.io/Tech/Graphics/games101-7/"/>
    <id>https://codingcodingk.github.io/Tech/Graphics/games101-7/</id>
    <published>2022-06-29T12:50:17.270Z</published>
    <updated>2022-06-29T12:50:17.270Z</updated>
    
    <content type="html"><![CDATA[<h1 id="图形渲染管线"><a href="#图形渲染管线" class="headerlink" title="图形渲染管线"></a>图形渲染管线</h1><p>图形渲染管线 Graphics (Real-time Rendering) Pipeline</p><h2 id="Graphics-Pipeline"><a href="#Graphics-Pipeline" class="headerlink" title="Graphics Pipeline"></a>Graphics Pipeline</h2><p>前面学的流程串起来，就是下图。里面还涉及到了 <strong>Shader 和 Texture Mapping</strong> 的时机。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220623160112.png"></p><h1 id="Shader"><a href="#Shader" class="headerlink" title="Shader"></a>Shader</h1><p>一般指 Shader Programs ，是一个程序，会<strong>对一个像素点/片段进行处理、着色</strong>。可以用OpenGL的语言写，也就是<strong>GLSL</strong>。</p><p>在线shader测试：<a href="http://shadertoy.com/view/ld3Gz2">http://shadertoy.com/view/ld3Gz2</a></p><p>shader展示：<a href="https://youtu.be/XuSnLbB1j6E">https://youtu.be/XuSnLbB1j6E</a></p><h1 id="纹理映射-Texture-Mapping"><a href="#纹理映射-Texture-Mapping" class="headerlink" title="纹理映射 Texture Mapping"></a>纹理映射 Texture Mapping</h1><p>前面已经知道，每个像素点的漫反射系数kd乘以光照强度，会得到该像素点最终的颜色。显然，每个像素点的漫反射系数是由物体本身的<strong>颜色属性</strong>来决定的，而这个像素点的颜色属性我们会通过一张<strong>材质图</strong>来确定。这就是贴图。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220623175805.png"></p><h2 id="uv"><a href="#uv" class="headerlink" title="uv"></a>uv</h2><p>uv坐标是指纹理展开后，纹理上的坐标。u和v的范围，都定义在0-1之内。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220623182239.png"></p><h1 id="纹理"><a href="#纹理" class="headerlink" title="纹理"></a>纹理</h1><p>前面已经学了，三角面内进行插值着色，是用重心坐标来计算出任一个点对应的uv贴图坐标的（也就是决定漫反射系数k）。那么会出现什么问题？</p><h2 id="纹理拉伸-纹理图过小"><a href="#纹理拉伸-纹理图过小" class="headerlink" title="纹理拉伸(纹理图过小)"></a>纹理拉伸(纹理图过小)</h2><p>当纹理贴图太小了，多个像素点会映射到一个纹理点上去，看上去就像是拉伸了。纹理拉伸现象，可以通过**双线性插值 ** 来优化处理。</p><p><strong>双线性插值 Bilinear Interpolation</strong>：当一个像素点映射到uv贴图上后，取出相邻的3个uv贴图点，进行2次水平、1次竖直方向上的插值，求出这个点在4个uv贴图点中的插值平滑结果（漫反射系数）。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220626224605.png"></p><h2 id="纹理拉伸-纹理图过大"><a href="#纹理拉伸-纹理图过大" class="headerlink" title="纹理拉伸(纹理图过大)"></a>纹理拉伸(纹理图过大)</h2><p>纹理图过大时，一个像素点的映射区域会覆盖很大的范围，但是一个像素点只能映射一个颜色。最终会走样，产生大量摩尔纹和锯齿。</p><p>如果不考虑性能，可以考虑超采样来获取平均值。但是考虑到性能，我们可以用<strong>MipMap</strong>。</p><p><strong>MipMaps</strong>：只能做近似的、正方形的 范围查询。</p><p>先将原图进行一半分辨率的裁剪（比如32x32=&gt;16x16），直到1x1。所有Mipmap大小之和为的原图1/3。</p><p><strong>1.如何计算需要的数量</strong>：映射出像素点对应uv的范围是几x几。</p><p><strong>2.如何查询某个中间值</strong>（比如1.8x1.8）：三线性插值。比如1.8x1.8，就是把2x2和1x1的2张mipmap结果值，再进行一次线性插值，求出1.8的值作为最终结果。</p><p>通过上面2步完成一个正确的Mipmap处理，最终可以得到一个像素点映射连续变化的uv贴图（也就是色彩会平滑、渐变过度）。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220627113118.png"></p><p>上面说了只能做正方形的范围查询，那如果面对不同长宽比的情况（矩形）该怎么做来避免Overblur？</p><p><strong>各向异性过滤 Ripmaps</strong>：水平和竖直方向上的表现各不相同，不是1：1时，生成长宽拉伸后的图以提供矩形查询，处理长宽比不同的情况。最终额外的贴图大小是原图的3倍。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220627120903.png"></p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;图形渲染管线&quot;&gt;&lt;a href=&quot;#图形渲染管线&quot; class=&quot;headerlink&quot; title=&quot;图形渲染管线&quot;&gt;&lt;/a&gt;图形渲染管线&lt;/h1&gt;&lt;p&gt;图形渲染管线 Graphics (Real-time Rendering) Pipeline&lt;/p&gt;
&lt;h2</summary>
      
    
    
    
    <category term="Game" scheme="https://codingcodingk.github.io/categories/Game/"/>
    
    <category term="Graphics" scheme="https://codingcodingk.github.io/categories/Game/Graphics/"/>
    
    
    <category term="图形学" scheme="https://codingcodingk.github.io/tags/%E5%9B%BE%E5%BD%A2%E5%AD%A6/"/>
    
    <category term="Graphics" scheme="https://codingcodingk.github.io/tags/Graphics/"/>
    
    <category term="games101" scheme="https://codingcodingk.github.io/tags/games101/"/>
    
  </entry>
  
  <entry>
    <title>Games101 - 着色</title>
    <link href="https://codingcodingk.github.io/Tech/Graphics/games101-6/"/>
    <id>https://codingcodingk.github.io/Tech/Graphics/games101-6/</id>
    <published>2022-06-29T12:50:17.269Z</published>
    <updated>2022-06-29T12:50:17.269Z</updated>
    
    <content type="html"><![CDATA[<p><strong>目前为止我们学会了什么？</strong></p><p>相机和物体 -&gt; 变换坐标到原点 -&gt; 拉伸后映射成2D图像 -&gt; 对2D图像进行光栅化：滤波、采样、后处理，最终变成屏幕上的像素点</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220622162501.png"></p><p>现在我们引入<strong>着色</strong>。</p><h1 id="着色-Shading"><a href="#着色-Shading" class="headerlink" title="着色 Shading"></a>着色 Shading</h1><h2 id="什么是着色？"><a href="#什么是着色？" class="headerlink" title="什么是着色？"></a>什么是着色？</h2><p>着色在普世意义上，是对明暗、颜色进行绘制。</p><p>而在图形学中，<strong>着色是一个对物体应用材质的过程</strong>，正是材质的不同才导致颜色不同。</p><h2 id="理解一个简单的着色模型"><a href="#理解一个简单的着色模型" class="headerlink" title="理解一个简单的着色模型"></a>理解一个简单的着色模型</h2><p><strong>Blinn-Phong Reflectance Model</strong> 是一个常用参考的光照反射模型，它分为以下：</p><p><strong>高光 Specular highlights</strong>：一根光线打到光滑平面（比如镜面）上，会往镜面反射附近去反射。</p><p><strong>漫反射 Diffuse reflections</strong>：一根光线打到粗糙平面（比如墙面）上，被反射到各个地方的情况。图中茶杯从浅黄到深黄的变化。</p><p><strong>环境光照 Ambient lighting</strong>：是由间接光源组成的光源统称。图中光线并没有直接打到箭头处，而是打到桌面上被反射，反射光再打到了箭头处。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220622163914.png"></p><h2 id="开始前先定义一个“着色点”-shading-point"><a href="#开始前先定义一个“着色点”-shading-point" class="headerlink" title="开始前先定义一个“着色点” shading point"></a>开始前先定义一个“着色点” shading point</h2><p>虽然反射到的面有曲面、有直面，但我们只观察一个最小的反射点，那么曲面的极小也可以当作<strong>直面</strong>。这个被观察的点我们叫做 <strong>shading point</strong> ，同时再定义几个其他的属性。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220622170625.png"></p><p>单位向量 v：和相机的连线。</p><p>单位向量 n：反射面的法线。</p><p>单位向量 l：光照方向。</p><p>表面参数：颜色 color、反射强度 shininess、…</p><p><strong>着色的局部性 shading is local：我们着色只关注光线照过来这个点该怎么着色。</strong>比如因为遮挡产生的阴影，着色器是不考虑的。</p><p>接下来我们根据定义好的观测点 shading point，来分析上面提到的3种反射情况。</p><h2 id="漫反射-Diffuse-reflections"><a href="#漫反射-Diffuse-reflections" class="headerlink" title="漫反射 Diffuse reflections"></a>漫反射 Diffuse reflections</h2><p>一根光线打到一个点上时，会被均匀反射到四周。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220622173813.png"></p><p>通过观察就可以知道，着色点的法线n和光照打来的方向l的夹角θ，会和这个着色点接收到的光照能量成反比。而这个反比有一个推导，叫 <strong>朗伯余弦定理 Lambert’s cosine law</strong>。</p><p>朗伯余弦定理的推导中，将点光源发出的光的轨迹视作一个个圆球体，而根据能量守恒，只要在真空中传播没有发生损耗，这些圆球体上某点的能量和圆球体表面积（也就是半径平方r）成正比。</p><p>最终公式如下：<img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220622182248.png"></p><p>从上面也能得知一件事，就是<strong>漫反射与观测方向v无关</strong> =&gt; 通俗点说就是某点被漫反射了，即使你在不同的位置看它，它的着色情况也是不变的。</p><p>公式里还有个系数kd，这是颜色系数，乘以漫反射光的强度（右边的值），得到最终显示的颜色。</p><h2 id="高光-Specular-Term"><a href="#高光-Specular-Term" class="headerlink" title="高光 Specular Term"></a>高光 Specular Term</h2><p><strong>高光与v有关。</strong>当你的观测角度v，和反射光的方向R一致或者接近的时候，你才能看到高光。</p><p>而观测角度v和反射光R的夹角有多大，又可以转化为更简单的计算：求l和v的中间向量，然后和法线n求夹角。 这一个计算转化算是优化了，计算R和v夹角的方式叫做Phong模型，而我们这种优化的方式叫做<strong>Blinn-Phong模型</strong>，推导过程和最终公式如下：</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220623001727.png"></p><p>公式里还有一个问题，就是为什么要有一个<strong>P次方</strong>去处理cosα？原因是cos函数的容忍度太大了，导致非常大的夹角仍然能保持高光，这是不合理的，所以需要人为控制一下，一般P会采用256甚至以上，让夹角控制在3°以内。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220623003907.png"></p><h2 id="环境光照-Ambient-Term"><a href="#环境光照-Ambient-Term" class="headerlink" title="环境光照 Ambient Term"></a>环境光照 Ambient Term</h2><p>环境光来自四面八方，与光照方向l和观测方向v都没有关系，是一个常数。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220623101513.png"></p><h2 id="最终3种光照"><a href="#最终3种光照" class="headerlink" title="最终3种光照"></a>最终3种光照</h2><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220623102155.png"></p><h1 id="着色频率-Shading-Frequencies"><a href="#着色频率-Shading-Frequencies" class="headerlink" title="着色频率 Shading Frequencies"></a>着色频率 Shading Frequencies</h1><p>下面介绍3种着色方式，对应着不同的着色频率。</p><h2 id="逐三角面着色-flat-shading"><a href="#逐三角面着色-flat-shading" class="headerlink" title="逐三角面着色 flat shading"></a>逐三角面着色 flat shading</h2><p>每个三角面着一次色。</p><h2 id="逐顶点着色-Gouraud-shading"><a href="#逐顶点着色-Gouraud-shading" class="headerlink" title="逐顶点着色 Gouraud shading"></a>逐顶点着色 Gouraud shading</h2><p>每个三角面的3个顶点进行着色，着色完后三角面进行内部<strong>插值</strong>着色。顶点的法线方向，是通过这个顶点周围所有的面的法线平均或者加权平均算出来的。</p><p>插值可以用<strong>重心坐标插值</strong>，但是需要用投影前的重心坐标，而不是投影拉伸到屏幕后的，因为深度不同。取到重心坐标之后可以转换为uv坐标，这样就能取到对应的着色系数了。之后再把系数带入漫反射公式，算出光照下该像素的最终颜色。</p><h2 id="逐像素着色-Phong-shading"><a href="#逐像素着色-Phong-shading" class="headerlink" title="逐像素着色 Phong shading"></a>逐像素着色 Phong shading</h2><p>在顶点插值完之后，对每个像素都进行单独计算着色。</p><h2 id="并不是频率越高越好"><a href="#并不是频率越高越好" class="headerlink" title="并不是频率越高越好"></a>并不是频率越高越好</h2><p>当面数比较多的时候，哪怕是逐三角面着色也会有很好的效果。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220623121534.png"></p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;&lt;strong&gt;目前为止我们学会了什么？&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;相机和物体 -&amp;gt; 变换坐标到原点 -&amp;gt; 拉伸后映射成2D图像 -&amp;gt; 对2D图像进行光栅化：滤波、采样、后处理，最终变成屏幕上的像素点&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https:/</summary>
      
    
    
    
    <category term="Game" scheme="https://codingcodingk.github.io/categories/Game/"/>
    
    <category term="Graphics" scheme="https://codingcodingk.github.io/categories/Game/Graphics/"/>
    
    
    <category term="图形学" scheme="https://codingcodingk.github.io/tags/%E5%9B%BE%E5%BD%A2%E5%AD%A6/"/>
    
    <category term="Graphics" scheme="https://codingcodingk.github.io/tags/Graphics/"/>
    
    <category term="games101" scheme="https://codingcodingk.github.io/tags/games101/"/>
    
  </entry>
  
  <entry>
    <title>Games101 - 光栅化</title>
    <link href="https://codingcodingk.github.io/Tech/Graphics/games101-4/"/>
    <id>https://codingcodingk.github.io/Tech/Graphics/games101-4/</id>
    <published>2022-06-29T12:50:17.268Z</published>
    <updated>2022-06-29T12:50:17.268Z</updated>
    
    <content type="html"><![CDATA[<h1 id="学之前你必须知道"><a href="#学之前你必须知道" class="headerlink" title="学之前你必须知道"></a>学之前你必须知道</h1><h2 id="视锥"><a href="#视锥" class="headerlink" title="视锥"></a>视锥</h2><p>这是透视相机的概念，用来衡量视距的大小。一般用2种属性来描述一个视锥：</p><ol><li>  near面的宽高比</li><li>  垂直视角 Vertical Field of View</li></ol><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220621104507.png"></p><p>有了上面2个确定值，然后再自己定义一个near的值（近平面与相机的距离），就能确定成像了，视锥完成。</p><h2 id="屏幕上的概念"><a href="#屏幕上的概念" class="headerlink" title="屏幕上的概念"></a>屏幕上的概念</h2><p><strong>分辨率</strong>，也就是1080p、2k等，就是像素的多少。</p><p><strong>屏幕 Raster</strong>，德语中的屏幕的意思，而屏幕是一个光栅成像设备。</p><p><strong>光栅化 Rasterize</strong>，把东西画在屏幕上的过程。</p><p><strong>像素 Pixel</strong>，全名picture element，是最小单位的小正方形，它的颜色由Red、Green、Blue三个色(0~255)来混和成。</p><h2 id="屏幕发展过程"><a href="#屏幕发展过程" class="headerlink" title="屏幕发展过程"></a>屏幕发展过程</h2><p><strong>过去方式</strong></p><ol><li>  示波器</li><li>  阴极射线管 Cathode Ray Tube：和示波器原理相同，用很多电子打在屏幕上。通过扫描一样的方式，一条一条画横线，组成图像。</li></ol><p>隔行扫描 Raster Scan：上面的画横线，优化的话还会在一帧只画1、3、5…奇数行，下一帧再只画偶数行。这样省去了一半的工作量，也能一定程度欺骗到肉眼。</p><p><strong>现代方式</strong></p><ol><li>  液晶显示器 LCD（Liquid Crystal Display）：通过液晶扭曲光的方向，让其可以通过对应的光栅。</li><li>  控制发光二极管的显示器 LED（Light Emitting diode array）</li><li>  电墨水：通过电子控制只有黑、白像素的电墨水，实现电纸书的页面刷新。</li></ol><p>帧缓存技术 Frame Buffer（Memory for a Raster Display）：将显卡中的一块内存区域中，存储的内容反映到屏幕上。</p><h1 id="光栅化一个Cube"><a href="#光栅化一个Cube" class="headerlink" title="光栅化一个Cube"></a>光栅化一个Cube</h1><h2 id="视口变换-Viewport"><a href="#视口变换-Viewport" class="headerlink" title="视口变换 Viewport"></a>视口变换 Viewport</h2><p>光栅化之前要做的事。将一个 <code>[-1,1] x [-1,1]</code> 的2维平面，转换成<code>[width,0] x [0,height]</code>的笛卡尔坐标系内来表示。这叫做视口变换，看下：</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220621115856.png"></p><p>最后算出，变换矩阵如下：</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220621121741.png"></p><h2 id="光栅化"><a href="#光栅化" class="headerlink" title="光栅化"></a>光栅化</h2><p>我们将原图形分解成很多个三角面，投影到屏幕的像素点上。</p><h3 id="首先，怎么判断一个像素点与三角形的位置关系？"><a href="#首先，怎么判断一个像素点与三角形的位置关系？" class="headerlink" title="首先，怎么判断一个像素点与三角形的位置关系？"></a>首先，怎么判断一个像素点与三角形的位置关系？</h3><p>靠前面的知识，已经可以做到任何一个点都确定性地投影到屏幕上了。</p><p>那么如果想判断一个像素点是否在一个三角面内呢？也就是问，<strong>如何确定这个像素的着色？</strong></p><p>可以通过判断这个像素点的中心点与三角形的位置关系来确定，为了方便，就定义一个判断函数<code>Inside()</code>。而这个函数就是进行3次叉积。</p><h3 id="然后，哪些像素点需要判断呢？"><a href="#然后，哪些像素点需要判断呢？" class="headerlink" title="然后，哪些像素点需要判断呢？"></a>然后，哪些像素点需要判断呢？</h3><p>如果需要把整个屏幕的点都进行一次<code>Inside()</code>判断，实在是没有必要，那么哪些需要、哪些不需要，这就涉及到<strong>包围盒</strong>了。</p><p>简单介绍一个AABB包围盒，它就是取三角面3个顶点的<code>Min(x), Min(y), Max(x), Max(y),</code>作为他的盒子。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220621151747.png"></p><h3 id="最后，成像完有锯齿-Jaggies-怎么解决？"><a href="#最后，成像完有锯齿-Jaggies-怎么解决？" class="headerlink" title="最后，成像完有锯齿 (Jaggies) 怎么解决？"></a>最后，成像完有锯齿 (Jaggies) 怎么解决？</h3><p>又叫做反走样（Aliasing ）、抗锯齿。</p><p>在解决之前先理解为什么会有这类问题？取哪些像素点来表示这个三角面，叫做<strong>采样（Sample）</strong>。而采样会有3种<strong>瑕疵（Artifacts）</strong></p><ol><li>  锯齿 Jaggies</li><li>  摩尔纹 Moire</li><li>  运动速度过快 Wagon wheel effect</li></ol><p>产生原因是信号变化的速度太快，而采样速度跟不上了。</p><p><strong>那么怎么抗锯齿？</strong></p><p>可以对三角面做预处理，比如滤波、模糊，之后再进行采样。</p><h1 id="抗锯齿-Anti-Aliasing"><a href="#抗锯齿-Anti-Aliasing" class="headerlink" title="抗锯齿 Anti-Aliasing"></a>抗锯齿 Anti-Aliasing</h1><p>前面了解了为什么会产生锯齿，以及大致的抗锯齿技术（滤波、模糊）。这里细说。</p><p><strong>频域 Frequency Domain</strong>：描述信号在频率方面特性时用到的一种坐标系。想象一下cos2Π的图。</p><p><strong>滤波 Filtering</strong>：把某个时段内，特殊的频率给过滤掉。</p><p><strong>傅里叶变换</strong>：变幻的具体内容不要深究，只要知道，可以通过傅里叶变换把一张图变换为频域图，这样就能知道哪些地方的信号量多、少。中心定义为低频、周围定义为高频。最后，得到的频域图，是可逆的！</p><p>// TODO 下面的都只简单理解，更为深入要去学习 数字图像处理</p><h2 id="先傅里叶变换，再过滤一张图-FFP"><a href="#先傅里叶变换，再过滤一张图-FFP" class="headerlink" title="先傅里叶变换，再过滤一张图 FFP"></a>先傅里叶变换，再过滤一张图 FFP</h2><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220621180700.png"></p><p>中心定义为低频、周围定义为高频。当颜色变化差异巨大的时候（比如人物边缘），会产生高频信息。</p><p>为什么会有十字线？没有学过信号学不能看懂这张频谱图，只能理解为傅里叶变换前会把这一张图复制很多份拼起来，来取样。拼起来之后每张图的4个边的边界之间的信息差异巨大，就有巨大的高频信息，而这十字线的高亮就是边界的映射。</p><h3 id="高通率波"><a href="#高通率波" class="headerlink" title="高通率波"></a>高通率波</h3><p>High-pass，就是只有高频能通过，低频直接忽略的滤波方式。可以看到，逆变换回来的图几乎只保留了边界信息（人物描边），这是因为<strong>边界处的信号量差异很大，这就是高频的信息</strong>。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220621182236.png"></p><h3 id="低通滤波"><a href="#低通滤波" class="headerlink" title="低通滤波"></a>低通滤波</h3><p>滤掉高频，也就是滤掉边界信息，那么就会像下面这样。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220621182524.png"></p><h3 id="高低滤波"><a href="#高低滤波" class="headerlink" title="高低滤波"></a>高低滤波</h3><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220622105657.png"></p><h2 id="卷积定理"><a href="#卷积定理" class="headerlink" title="卷积定理"></a>卷积定理</h2><p>只简单概述结论：</p><p>在空间域中，对一张图进行卷积滤波 = 对一张图进行傅里叶变换、再对其乘以卷积核的傅里叶变换、最后逆傅里叶变换回去</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220622111008.png"></p><h1 id="采样-Sample"><a href="#采样-Sample" class="headerlink" title="采样 Sample"></a>采样 Sample</h1><p>稀疏采样会产生更多混叠，导致走样；密集采样更少混叠，所以映射结果更好一些。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220622114336.png"></p><h2 id="怎么较少走样，也就是如何反走样？"><a href="#怎么较少走样，也就是如何反走样？" class="headerlink" title="怎么较少走样，也就是如何反走样？"></a>怎么较少走样，也就是如何反走样？</h2><p>本质上，就是想办法增大傅里叶副本之间的距离来减少混叠。</p><p><strong>1.增加采样率</strong>（更高分辨率的屏幕）</p><p><strong>2.抗锯齿</strong>，通过对图像模糊预处理（使用傅里叶来低通滤波）再采样</p><h2 id="常规采样流程"><a href="#常规采样流程" class="headerlink" title="常规采样流程"></a>常规采样流程</h2><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220622115530.png"></p><p>先用低通滤波器模糊原图，然后再采样。</p><h3 id="抗锯齿：模糊操作"><a href="#抗锯齿：模糊操作" class="headerlink" title="抗锯齿：模糊操作"></a>抗锯齿：模糊操作</h3><p>取像素颜色时，根据三角面的在该像素块中覆盖的面积，来设置灰度。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220622120331.png"></p><p>这个理论很简单，但是实现比较困难，于是采用一种类似的方法，对一个像素块再进行划分、进行更多的采样，也就是**多重采样抗锯齿 MSAA (MultiSampling Anti-Aliasing) **来实现。</p><p>下图就是一个MSAA的例子：把一个像素格分成了2x2，来确定0%、25%、50%、75%、100%。</p><p><img src="https://cdn.jsdelivr.net/gh/CodingCodingK/CodingCodingK.github.io.ImageHost/img/20220622121659.png"></p><p>工业界会优化，想要2x2的效果不会这么单纯的分割成4个正方形，而是一些其他不规则图形，甚至可能多个像素块复用一个点，这么做都是为了减少需要检测的点数量。</p><h3 id="其他抗锯齿方法"><a href="#其他抗锯齿方法" class="headerlink" title="其他抗锯齿方法"></a>其他抗锯齿方法</h3><p>FXAA（Fast Approximate AA）：快速近似抗锯齿，是一种后处理，会对有锯齿的地方进行修正。</p><p>TAA（Temporal AA）：利用时间而不是空间采样，对于静止的连续帧，上一帧有效的采样点才继续采样。</p><p>DLSS（DeepLearning SuperSimple）：当低分辨率拉伸成高分辨率时，会有很多锯齿，此时用深度学习来猜测描绘。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;学之前你必须知道&quot;&gt;&lt;a href=&quot;#学之前你必须知道&quot; class=&quot;headerlink&quot; title=&quot;学之前你必须知道&quot;&gt;&lt;/a&gt;学之前你必须知道&lt;/h1&gt;&lt;h2 id=&quot;视锥&quot;&gt;&lt;a href=&quot;#视锥&quot; class=&quot;headerlink&quot; titl</summary>
      
    
    
    
    <category term="Game" scheme="https://codingcodingk.github.io/categories/Game/"/>
    
    <category term="Graphics" scheme="https://codingcodingk.github.io/categories/Game/Graphics/"/>
    
    
    <category term="图形学" scheme="https://codingcodingk.github.io/tags/%E5%9B%BE%E5%BD%A2%E5%AD%A6/"/>
    
    <category term="Graphics" scheme="https://codingcodingk.github.io/tags/Graphics/"/>
    
    <category term="games101" scheme="https://codingcodingk.github.io/tags/games101/"/>
    
  </entry>
  
  <entry>
    <title>Games101 - 遮挡与深度</title>
    <link href="https://codingcodingk.github.io/Tech/Graphics/games101-5/"/>
    <id>https://codingcodingk.github.io/Tech/Graphics/games101-5/</id>
    <published>2022-06-29T12:50:17.268Z</published>
    <updated>2022-06-29T12:50:17.269Z</updated>
    
    <content type="html"><![CDATA[<p>之前几节课学了怎么映射一个三角面到画面上，这节学多个三角面之间的遮挡关系处理，谁在前谁在后，也就是<strong>可见性</strong>怎么处理。</p><h1 id="实现遮挡关系"><a href="#实现遮挡关系" class="headerlink" title="实现遮挡关系"></a>实现遮挡关系</h1><h2 id="画家算法"><a href="#画家算法" class="headerlink" title="画家算法"></a>画家算法</h2><p>由远及近依次画（光栅化）。近的物体覆盖远的物体，就可以实现遮挡关系。</p><p>但是画家算法也存在一些问题，比如存在一些不可依赖深度排序解决的问题。所以不会直接用画家算法。</p><h2 id="深度缓存-Z-Buffer"><a href="#深度缓存-Z-Buffer" class="headerlink" title="深度缓存 Z-Buffer"></a>深度缓存 Z-Buffer</h2><p>这是工业界采用的算法。这里的z不是z轴，而是深度 depth，是摄像机位置到所求点的距离。</p><p>最后，只渲染每个像素上depth最浅的那个颜色。当物体发生运动后，会同步更新深度，有更小值出现就重新赋色。</p><p>深度缓存的复杂度是O(n)，而不是排序的O(nlogn)，因为深度缓存并不排序只求最小值。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;之前几节课学了怎么映射一个三角面到画面上，这节学多个三角面之间的遮挡关系处理，谁在前谁在后，也就是&lt;strong&gt;可见性&lt;/strong&gt;怎么处理。&lt;/p&gt;
&lt;h1 id=&quot;实现遮挡关系&quot;&gt;&lt;a href=&quot;#实现遮挡关系&quot; class=&quot;headerlink&quot; title</summary>
      
    
    
    
    <category term="Game" scheme="https://codingcodingk.github.io/categories/Game/"/>
    
    <category term="Graphics" scheme="https://codingcodingk.github.io/categories/Game/Graphics/"/>
    
    
    <category term="图形学" scheme="https://codingcodingk.github.io/tags/%E5%9B%BE%E5%BD%A2%E5%AD%A6/"/>
    
    <category term="Graphics" scheme="https://codingcodingk.github.io/tags/Graphics/"/>
    
    <category term="games101" scheme="https://codingcodingk.github.io/tags/games101/"/>
    
  </entry>
  
</feed>
